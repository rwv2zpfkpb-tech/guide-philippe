> **Pflicht:** Diese Datei muss nach **jeder** strukturellen Änderung sofort aktualisiert werden — immer, ohne Ausnahme: neue Routen, neue Tabellen/Migrationen, neue Server Actions, neue Komponenten, geänderte Auth-/Berechtigungslogik, neue Env-Variablen. Sie ist der primäre Kontext für KI-Assistenten — veraltete Inhalte führen zu falschen Empfehlungen. Lieber zu viel aktualisieren als zu wenig.

# Guide Philippe — Projektübersicht

Restaurant-Guide-App. Admins pflegen Restaurants, eingeloggte + freigeschaltete Nutzer hinterlassen Kommentare. **Kein anonymer Zugriff:** die gesamte App ist login-pflichtig, neue Registrierungen müssen zusätzlich von einem Admin freigeschaltet werden (s. „Auth & Berechtigungen").

## Tech-Stack

| Schicht | Technologie |
|---|---|
| Framework | Next.js 16 (App Router, React 19) |
| Datenbank + Auth | Supabase (PostgreSQL + Row Level Security) |
| Styling | Tailwind CSS v4 |
| Karten | `@vis.gl/react-google-maps` + Google Places API (New) |
| Sprache | TypeScript strict |

## Verzeichnisstruktur

```
app/
  layout.tsx              – Root-Layout (Cormorant Garamond + DM Sans Fonts, Theme-Cookie, Body-Wrapper, `appleWebApp`-Metadata für iOS-Homescreen-Titel)
  page.tsx                – Startseite (Server Component, lädt Restaurants). Im Normal-Modus (keine Ortssuche, kein aktiver Filter/keine Namenssuche) zeigt die Seite **nicht** mehr das volle unfilterte Restaurant-Grid (früher immer alle veröffentlichten Restaurants, bei wachsendem Bestand unübersichtlich) — stattdessen nur Hero-Suche + optional „Auswahl" (`getFeaturedRestaurants()`) + optional „Neu hinzugefügt" (`getRecentRestaurants()`, jetzt die 6 neuesten statt zeitbasiert „letzte 30 Tage") + `<InstallPwaInstructions>`. Das Restaurant-Grid rendert nur noch, sobald `hasFilters` (Küche/Preis/Bewertung-Filter oder Namenssuche `q`) aktiv ist — dann als „Suchergebnisse"
  loading.tsx             – Suspense-Fallback für die Startseite
  error.tsx               – Error Boundary für die Startseite (fängt z.B. transiente Supabase-Fetch-Fehler ab), zeigt Retry-Button
  manifest.ts             – Next.js-Dateikonvention, generiert `/manifest.webmanifest` (PWA-Metadaten, Icons)
  icon.tsx                – Next.js-Dateikonvention: Favicon (32×32) via `next/og` `ImageResponse`, „GP"-Wortmarke (`lib/pwa-icon.tsx`)
  apple-icon.tsx          – wie `icon.tsx`, aber 180×180 für iOS „Zum Home-Bildschirm" (Next hängt automatisch `<link rel="apple-touch-icon">` ein)
  icons/192/route.tsx, icons/512/route.tsx – Route Handlers für die im Manifest referenzierten 192px/512px-PNG-Icons (ebenfalls `ImageResponse`)
  globals.css
  login/
    page.tsx              – Server Component: redirect wenn eingeloggt
    LoginForm.tsx          – Client Component: Login/Signup-Tabs, plus zwei weiteren (nicht in der Tab-Leiste sichtbaren) Zuständen: „forgot" (ein „Passwort vergessen?"-Link unter dem Passwort-Feld des Login-Tabs wechselt zu einem reinen E-Mail-Formular, das `requestPasswordReset` auslöst, s. „Auth & Berechtigungen") und „resend" (Bestätigungslink erneut anfordern, `resendConfirmationEmail` — erreichbar über einen Footer-Link unter dem Login-/Signup-Formular, einen Link auf dem Signup-Erfolgsscreen sowie von `app/auth/confirm`s Fehlerscreen aus, s. Roadmap-Schritt 30). Signup-Formular verlangt die E-Mail-Adresse zweimal (`email` + `emailConfirm`) sowie das Passwort zweimal (`password` + `passwordConfirm`) — beide Paare serverseitig in `signUp()` verglichen, s.u. Die vier Felder sind dafür (nur im Signup-Tab) controlled; ein Live-Abgleich hebt das jeweilige „bestätigen"-Feld rot hervor (`Field`s neuer `error`-Prop), sobald es befüllt ist, aber vom Original abweicht — verschwindet automatisch wieder bei Übereinstimmung. Klick auf „Konto erstellen" bricht bei einer Abweichung clientseitig ab (`e.preventDefault()`) und fokussiert gezielt das zu korrigierende Feld, statt den Server-Roundtrip abzuwarten; die serverseitige Prüfung in `signUp()` bleibt als Fallback (z.B. JS deaktiviert) bestehen. Da die Felder über React-State (nicht über den DOM) persistieren, bleiben alle Eingaben bei einem abgebrochenen oder fehlgeschlagenen Versuch erhalten. Platzhaltertexte im Signup-Formular spielen auf den Namensgeber der App an (`Philippe` / `philippe@guidephilippe.de`) statt generischer Beispieltexte. **Ladeanimation:** alle Submit-Buttons dieser Datei (Anmelden/Konto erstellen, Link schicken, Link senden) sowie `ConfirmButton.tsx`/`ResetPasswordForm.tsx` zeigen während des Pending-States zusätzlich zum bisherigen Text-Wechsel einen `.gp-spinner-sm`-Inline-Spinner (`app/globals.css`, gleiches Muster wie im Admin-Dashboard) — vorher nur Opacity-Dimmen + Text „…", kein sichtbares Lade-Icon. **„Bestätigungslink erneut anfordern"-Cooldown:** eine 60-Sekunden-Sperre (rein clientseitig, kein Server-Rate-Limit) verhindert wiederholtes Anfordern — `startResendCooldown()` setzt beim `onSubmit` beider Resend-Formulare (Haupt-Tab-Formular **und** ein neues „Erneut senden"-Mini-Formular auf dem „Link verschickt"-Erfolgsscreen, das dieselbe `resendAction` mit der zuvor eingegebenen Adresse als verstecktem Feld erneut auslöst) `resendAvailableAt = Date.now() + 60_000`; ein sekündlich tickender `useEffect` berechnet daraus `resendCooldownSeconds` für die Button-Beschriftung („Erneut in Xs"/„Erneut senden in Xs") und deaktiviert den Button, bis die Zeit abgelaufen ist. Bewusst **kein** `useEffect`, der direkt auf `resendState`/`Date.now()` reagiert — sowohl `setState` direkt im Effekt-Body als auch `Date.now()` während des Renders sind React-Compiler-Lint-Fehler (`react-hooks/set-state-in-effect`/`react-hooks/purity`); die Sperre startet stattdessen im `onSubmit`-Event-Handler (dort ist `Date.now()` unproblematisch).
  auth/confirm/
    page.tsx              – Server Component: Ziel des Signup-Bestätigungslinks. Liest `token_hash`/`type` aus der URL (kein PKCE-`code`-Exchange mehr, s. „Klick-gated Verifizierung" unten), zeigt bei vorhandenem Token einen „Konto bestätigen"-Button (`ConfirmButton`), sonst direkt den Fehler-Screen
    ConfirmButton.tsx     – Client Component: „Konto bestätigen"-Button, löst beim Klick `confirmEmailToken` (Server Action) aus — die eigentliche Token-Verifizierung passiert erst hier, nicht beim Laden der Seite
  auth/reset-password/
    page.tsx              – Server Component: gemeinsames Ziel für „Passwort vergessen" (Login-Seite) **und** „Passwort ändern" (eingeloggt, `UserMenu`) — beide lösen dieselbe Server Action (`requestPasswordReset`) aus, s. „Auth & Berechtigungen". Liest `token_hash` aus der URL und reicht ihn an `ResetPasswordForm` durch (kein PKCE-`code`-Exchange mehr, s. „Klick-gated Verifizierung" unten); fehlt der Token, sofort „Link ungültig"
    ResetPasswordForm.tsx – Client Component: „Neues Passwort"/„Passwort bestätigen"-Formular (plus verstecktem `token_hash`-Feld), ruft `updatePassword` (Server Action) auf — verifiziert den Recovery-Token erst beim Abschicken dieses Formulars, nicht beim Laden der Seite
  pending/
    page.tsx              – Server Component: Screen für eingeloggte, aber noch nicht admin-freigeschaltete Accounts (`profiles.status = 'pending'/'rejected'`). Ziel des proxy-Redirects, s.u.
  offline/
    page.tsx              – Client Component, rein statisch (kein Datenabruf): Offline-Fallback-Seite, von `public/sw.js` beim Precaching in den Service-Worker-Cache gelegt und bei fehlgeschlagener Navigation ohne Netzwerk-Roundtrip direkt aus dem Cache ausgeliefert (umgeht `proxy.ts` komplett, da kein echter Request stattfindet). Öffentlicher Pfad (`PUBLIC_PATHS`, s. „Auth & Berechtigungen") für den seltenen Fall eines direkten/Online-Aufrufs
  restaurant/[id]/
    page.tsx              – Detailseite (Places-Details, Fazit + Kategorie-Bewertungen, Aufenthalts-Historie, Kommentare). Oben ein `<BackButton fallbackHref="/">` statt eines festen „Alle Restaurants"-Links (s. „Zurück-Navigation" unten). Zeigt ein goldenes „Entwurf — nur für Admins sichtbar"-Badge über dem `<h1>`-Namen, wenn `restaurant.status === "draft"` (erreichbar über den Admin-Tabellen-Link ohnehin schon immer, seit Roadmap-Schritt 25 zusätzlich über die öffentliche Suche mit aktiviertem Toggle). Fazit wird als Headline (erster Satz) + restlicher Fließtext gerendert (`ReviewContent`), nicht mehr als Headline + vollständig wiederholtem Text. Die Kategorie-Karten (Service/Location/Geschmack/Preis-Leistung) stehen als einspaltige Liste untereinander statt als 2×2-Grid — die App ist für längere Fließtexte je Kategorie gedacht, ein schmales Grid-Feld war dafür zu eng. Öffnungszeiten stehen in einem `<details>`-Block (`.oh-details`-CSS in `globals.css`), standardmäßig eingeklappt, mit rotierendem Chevron als Ausklapp-Indikator; hat Google keine gespeicherten Öffnungszeiten (kein `google_place_id`, fehlgeschlagener Lookup/Sync, oder Google kennt sie nicht), zeigt eine einfache Box stattdessen `restaurant.opening_hours` (manueller Freitext-Fallback), falls gesetzt. Adresse/Telefon/Website/Öffnungszeiten kommen seit Roadmap-Schritt 18 **direkt aus der DB** (`restaurant.address`/`.phone`/`.website`/`.google_opening_hours`) statt live von Google bei jedem Seitenaufruf — nur Fotos werden noch live gezogen (`getPlaceDetails()`, s.u.). „Jetzt geöffnet, bis HH:MM Uhr"/„Geschlossen" wird per `getOpeningStatus()` (`lib/openingHours.ts`) lokal aus dem gespeicherten Wochenplan berechnet statt von Google übernommen — die Endzeit der aktuell laufenden Öffnungs-Range wird mit angezeigt, damit klar ist, wie lange noch geöffnet ist. Telefon: „Anrufen"-Button mit `tel:`-Link, Website: Link in neuem Tab, beide neben `<NavigateButton>` unter der Identity-Zeile, nur gerendert wenn ein Wert vorhanden ist. Preis-Level (`<PriceLevelDots>`) steht **nicht** mehr klein in der Meta-Zeile neben Adresse/Öffnungszeiten, sondern groß/fett (`1.75rem`, Cormorant) direkt gegenüber dem Cuisine-Eyebrow-Label — beide teilen sich eine `justify-content: space-between`-Zeile ganz oben im linken Identity-Block, damit der Preis nicht neben der kleinschriftigen Meta-Zeile untergeht. Spoon-Verdict-Box + Dots sind farbcodiert (`SPOON_RATING_COLORS`); auf schmalen Viewports (`.restaurant-hero-row`/`.restaurant-hero-verdict`, `max-width: 640px`) stapelt sich die Identity-Zeile statt die Verdict-Box isoliert in eine sonst leere Zeile umbrechen zu lassen
    loading.tsx
    CommentForm.tsx        – Client Component: Kommentar abgeben (0–5 Sterne, 150-Zeichen-Limit)
  actions/                – Alle Server Actions ("use server")
    auth.ts               – signIn / signUp / signOut / requestPasswordReset / updatePassword / confirmEmailToken / resendConfirmationEmail
    profiles.ts           – getPendingProfiles / approveProfile / rejectProfile (Admin-Schutz, Registrierungs-Freischaltung)
    restaurants.ts        – CRUD Restaurants (Admin-Schutz)
    reviews.ts             – createReview / updateReview — Aufenthalte (Fazit + Kategorie-Bewertungen) eines Restaurants, Admin-Schutz
    comments.ts            – CRUD Kommentare (Auth + RLS-Schutz)
    csvImport.ts           – previewCsvImport / confirmCsvImport — Google-Takeout-CSV-Import mit Abgleich gg. bestehende Restaurants (Admin-Schutz)
    places.ts              – Google Places Details (Server-only API-Key)
  admin/
    dashboard/
      page.tsx            – Server Component: Auth-Guard + Datenladen
      AdminDashboard.tsx  – Client Component: gesamte Admin-UI
  api/
    auth/email/route.ts   – Route Handler: Supabase "Send Email" Auth-Hook, verschickt Auth-Mails via Resend (s.u.) — bewusste Ausnahme von „Kein API-Layer", da Supabase dies als HTTP-Webhook aufruft

components/
  Header.tsx, Footer.tsx   – Layout-Rahmen (Server Components). Header rendert für eingeloggte Nutzer `<UserMenu>` statt separater Sign-out-/Passwort-Buttons (s.u.). Header hat kein festes `height` — nur `minHeight: 64` auf seinem inneren Flex-Container, der bei Platzmangel (`flexWrap`) auf schmalen Viewports in eine zweite Zeile umbricht und dadurch höher wird, s. `HeaderHeightObserver.tsx`
  HeaderHeightObserver.tsx – Client Component (unsichtbar, `null`-Render), in `app/layout.tsx` direkt neben `<Header>` gemountet: misst per `ResizeObserver` die tatsächliche Höhe des `<header>`-Elements und schreibt sie als `--header-height`-CSS-Custom-Property auf `document.documentElement`. Grund: `SearchResultsView.tsx`s `.sr-outer` brauchte früher einen hartkodierten `calc(100vh - 64px)`, was auf schmalen Viewports (Header bricht um, s.o.) zu niedrig war — `.sr-outer` ragte dadurch unbemerkt über den sichtbaren Viewport hinaus, wodurch die ganze Seite (nicht nur die interne Trefferliste) leicht scrollbar wurde und der fixierte „Karte anzeigen"-Button den letzten Treffer überdeckte. Jetzt `calc(100vh - var(--header-height, 64px))` (Fallback 64px nur für den allerersten Render vor dem ersten `ResizeObserver`-Callback)
  ServiceWorkerRegister.tsx – Client Component (unsichtbar, `null`-Render), in `app/layout.tsx` neben `<HeaderHeightObserver>` gemountet: registriert `public/sw.js` per `navigator.serviceWorker.register()` in einem `useEffect` (No-op auf Browsern ohne Service-Worker-Support, Fehler werden verschluckt — progressive enhancement, kein Feature-Erfordernis). Läuft auf jeder Seite inkl. `/login`, deshalb müssen `/sw.js` und `/offline` in `PUBLIC_PATHS` (s. „Auth & Berechtigungen") stehen, sonst würde die Registrierungsanfrage eines ausgeloggten Besuchers eine HTML-Redirect-Antwort statt des tatsächlichen Skripts bekommen
  UserMenu.tsx             – Client Component: Kebab-Menü (Drei-Punkte-Icon, `IconDotsVertical`) rechts im Header, hinter dem „Passwort ändern" (löst `requestPasswordReset` mit der eigenen E-Mail aus) und „Abmelden" versteckt sind — hält den Header selbst auf schmalen Viewports schlank (nur Username + ein Icon-Button sichtbar). Unter dem „Passwort ändern"-Button steht klein „Schickt dir einen Link per E-Mail", damit vor dem Klick klar ist, dass kein Inline-Formular folgt, sondern ein E-Mail-Versand ausgelöst wird; der Erfolgszustand ersetzt den Button durch „E-Mail mit Link verschickt — bitte Postfach prüfen." statt nur „Link verschickt". Schließt bei Klick außerhalb (`mousedown`-Listener auf `document`)
  ThemeToggle.tsx          – Client Component: Hell-/Dunkelmodus-Umschalter
  NavigationProgress.tsx   – Client Component: Ladebalken bei Routenwechsel
  BackButton.tsx           – Client Component: `router.back()`-Button mit `fallbackHref`-Prop (Deep-Link/neuer Tab ohne In-App-History → normaler `<Link>` statt History-Pop), s. „Zurück-Navigation" unten
  DeleteCommentButton.tsx  – Client Component: kapselt das Inline-„Löschen"-Formular eines Kommentars (`deleteComment`) auf der Restaurant-Detailseite. Nur wegen `useFormStatus()` als eigene Datei ausgelagert — die Detailseite selbst ist eine Server Component, `useFormStatus` braucht einen Client-Baum, um den Button während des Requests auf „Löscht…" umzuschalten statt keinerlei Feedback zu geben
  InstallPwaInstructions.tsx – Client Component: PWA-Installationsanleitung (iOS Safari „Zum Home-Bildschirm" / Android Chrome „App installieren"), erkennt per `navigator.userAgent` die passende Plattform und hebt sie hervor, zeigt aber beide. Ganz unten auf der Landing-Page (`app/page.tsx`), rein informativ (kein `beforeinstallprompt`-Handling)
  HorizontalScrollRow.tsx  – Client Component: kapselt die horizontal scrollbare Karten-Reihe der Landing-Page („Auswahl"/„Neu hinzugefügt" in `app/page.tsx`). Bugfix (2026-07-19): eine reine `overflowX: auto`-Flex-Zeile reagiert nur auf eine Trackpad-Geste oder gezogenen Scrollbar — ein normales vertikales Mausrad tut nichts (kein Browser wandelt `deltaY` automatisch in `scrollLeft` um), wodurch Karten außerhalb der sichtbaren Breite für reine Maus-Nutzer unerreichbar waren (per Playwright reproduziert: `scrollLeft` blieb nach `mouse.wheel()` bei 0, obwohl der Container weiter scrollbar war). Registriert dafür einen **nicht-passiven** `wheel`-Listener per `addEventListener(..., { passive: false })` (ein normaler React-`onWheel`-Handler ist für Wheel-Events immer passiv verdrahtet, `preventDefault()` darin ist wirkungslos — die Seite hätte trotzdem zusätzlich vertikal mitgescrollt) und addiert `deltaY` auf `scrollLeft`, nur wenn die Geste überwiegend vertikal ist (eine echte horizontale Trackpad-Geste bleibt unangetastet). Zusätzlich zwei Pfeil-Buttons (rotiertes `IconChevronDown`), die nur erscheinen, wenn in die jeweilige Richtung noch etwas zu scrollen ist (`canScrollLeft`/`canScrollRight`, aus `scrollLeft`/`scrollWidth`/`clientWidth` abgeleitet) — Klick scrollt per `scrollBy({ behavior: "smooth" })`.
  icons.tsx                – Kleines Set monochromer SVG-Icons (Heroicons-outline-Stil, 24×24, `currentColor`) — ersetzt alle dekorativen Emojis in der App (s. „Keine Emojis außer Spoon-Rating" unten). Spoon-Rating-Emoji (`lib/ratings.ts`) bleiben bewusst die einzige Ausnahme. Enthält u.a. `IconPhone`/`IconGlobe` (Anrufen-/Website-Buttons auf der Restaurant-Detailseite), `IconStar` (optional `filled`-Prop — „Auswahl"-Toggle im Admin-Dashboard), `IconDotsVertical` (Kebab-Menü-Trigger, `UserMenu.tsx`) und `IconLocate` (Crosshair-Glyphe, „Standort verwenden"-Button in `LocationSearch.tsx`)
  HeroSearch.tsx           – Client Component: hält den lokal gestagten Filter-Zustand der Landing-Page-Suche (s. „Landing-Page-Suche" unten), rendert LocationSearch + FilterBar
  FilterBar.tsx            – Client Component: Küche/Preis/Bewertung-Filter. **Controlled** (kein eigener Router-/URL-Zugriff mehr) — Mehrfachauswahl je Kategorie, Zustand kommt von HeroSearch als Props. Preis/Bewertung bleiben Chip-Reihen (kleine feste Wertemenge), Küche nutzt seit Kurzem `<CuisineFilterDropdown>` (s.u.) statt einer Chip-Reihe — die Zahl unterschiedlicher Küchen wächst mit dem Restaurant-Bestand und sprengte irgendwann jede Chip-Zeile
  CuisineFilterDropdown.tsx – Client Component: durchsuchbares Mehrfachauswahl-Dropdown für Küche — Button (zeigt Anzahl aktiver Auswahl) öffnet ein Popover mit Suchfeld + Checkbox-Liste (`max-height` + `overflow-y: auto`), schließt bei Klick außerhalb (`mousedown`-Listener, gleiches Muster wie `UserMenu.tsx`). Ersetzt die Küche-Chip-Reihe überall dort, wo die Zahl möglicher Küchen mit dem Restaurant-Bestand wächst und nicht mehr in eine Chip-Zeile passt: `FilterBar.tsx` (Landing-Page-Hero), `SearchResultsView.tsx` (eigene Filter-Zeile) und das Admin-Dashboard-Filter-Panel (s.u.) — dieselbe Komponente, unterschiedliche `cuisines`/`selected`/`onToggle`/`onClear`-Props je Call-Site
  LocationSearch.tsx       – Client Component: Orts-/Restaurantsuche (Google Places). `size="large"` (Landing-Page-Hero): löst erst beim Klick auf „Suchen" aus, s.u. `size="compact"` (Suchleiste in SearchResultsView): navigiert weiterhin sofort bei Auswahl/Enter. Zwischen Klick/Auswahl und dem eigentlichen `router.push` liegt ein Google-Places/Geocoder-Request (Koordinaten auflösen) — dieser Zwischenschritt zeigt einen `resolving`-State (Spinner statt Lupe im Eingabefeld, „Sucht…" auf dem Suchen-Button, Eingabe gesperrt), da er selbst noch vor der Navigation liegt und damit nicht vom routenweiten `loading.tsx` abgedeckt ist. Zusätzlicher runder „Standort verwenden"-Button (`IconLocate`) direkt neben dem Eingabefeld, in **beiden** Größenvarianten: ruft `navigator.geolocation.getCurrentPosition()` auf, reverse-geocodiert die Koordinaten per `google.maps.Geocoder` nur für ein lesbares Label (fällt bei Fehler auf „Mein Standort" zurück) und navigiert dann wie eine normale Ortsauswahl — mit einem zusätzlichen `own_location=1`-Query-Param. `navigateToLocation()` (gemeinsame Funktion für Orts-, „Im Guide"-Restaurant- und „Standort verwenden"-Auswahl) schreibt zwar weiterhin `ne_lat`/`ne_lng`/`sw_lat`/`sw_lng` (aus Googles `place.viewport`, oder ±0.5° Fallback ohne Viewport) in die URL, das ist aber **reine Round-Trip-Metadaten** — kein Server-Code liest diese Werte mehr als DB-Filter (s. Roadmap-Schritt 15/16: ein hartes Bounds-Filtering auf Googles je nach Ortstyp völlig unterschiedlich großem Viewport — Stadt vs. einzelne Adresse/Straße — schloss kuratierte Restaurants unvorhersehbar aus; jede Ortssuche lädt jetzt alle passenden veröffentlichten Restaurants und nutzt den gesuchten Ort nur für Karten-Center + clientseitige Distanz-Sortierung). Eigener `locating`-State (Spinner im Button, Eingabefeld gesperrt) getrennt vom bestehenden `resolving`-State; Geolocation-Fehler (Berechtigung verweigert/Timeout) zeigen eine kleine Inline-Fehlermeldung unter der Eingabezeile. Setzt voraus, dass `next.config.ts`s `Permissions-Policy` `geolocation=(self)` erlaubt (s. „HTTP-Security-Header" unten). **Kostenoptimierung (2026-07-19, Roadmap-Schritt 26):** Die Orts-Autocomplete (`AutocompleteSuggestion.fetchAutocompleteSuggestions()`) lief bisher ohne `AutocompleteSessionToken` — jeder debouncte Tastenanschlag wurde einzeln abgerechnet, zusätzlich zum separaten Place-Details-Request (`fetchFields()`) beim Auflösen der Auswahl. Neuer `sessionTokenRef` (erzeugt beim ersten Tastenanschlag einer Suche, an jeden `fetchAutocompleteSuggestions()`-Call derselben Suche mitgegeben) wird nach dem Auflösen einer Auswahl auf `null` zurückgesetzt, damit die nächste Suche ein frisches Token bekommt. `Prediction` hält dafür zusätzlich die rohe `placePrediction`-Referenz (statt nur Text/ID) — `resolvePlace()` ruft `.toPlace()` **auf dieser Prediction** auf (statt einer neu konstruierten `Place`-Instanz), da Google das Session-Token dabei automatisch in den folgenden `fetchFields()`-Call übernimmt und so Autocomplete + Details als eine abgerechnete Sitzung bündelt statt als zwei getrennte volle Requests. Per Playwright gegen die echte Google-API verifiziert (abgefangene `AutocompletePlaces`/`GetPlace`-RPC-Requests trugen identisches Token, nächste Suche danach ein neues) — Navigation/Funktionalität unverändert.
  SearchResultsView.tsx    – Client Component: Liste + Karte für Ortssuche. Desktop: klassisches Nebeneinander (Liste 420px fest + Karte flex). Mobile (< 900px, s. `.sr-viewport`/`.sr-list-panel`/`.sr-map-panel` in `globals.css`): Liste und Karte liegen nebeneinander in einem doppelt breiten Viewport, ein fixierter Toggle-Button verschiebt den Viewport per CSS-`transform` um 50% (Liste ↔ Karte) statt hart umzuschalten — beide Panels bleiben gemountet (Karteninstanz bleibt erhalten). Eigene Filter-Chips sind wie auf der Landing-Page gestaged (s. „Landing-Page-Suche" unten) — Klick ändert nur lokalen State, erst „Übernehmen" navigiert; die ganze Komponente ist mit `key={JSON.stringify({...activeFilters, ownLocation})}` verdrahtet, damit der gestagte State (inkl. Sortierung, s.u.) nach jeder Navigation bzw. jedem Wechsel zu/von „Standort verwenden" zurückgesetzt wird (kein `useEffect`-Sync). Kein Bild-Platzhalter mehr in den Listenkarten (s. „Kein Self-Hosting von Bildern" unten) — Spoon-Rating stattdessen farbcodiert (`SPOON_RATING_COLORS`). Jede Trefferzeile (`ResultCard`) zeigt im eingeklappten Zustand zusätzlich die Luftliniendistanz zum Suchzentrum (`haversineDistanceKm()` + `formatDistanceKm()`, s. `lib/geo.ts`) unterhalb von Spoon-Emoji/Preis — Restaurants ohne `lat`/`lng` erreichen diese Liste ohnehin nie (s. Roadmap-Schritt 17). Jede Zeile ist außerdem ein aufklappbares Element (kein direkter `<Link>` mehr auf die Detailseite): Klick togglet ein Panel mit Adresse (`restaurant.address`) und Öffnungszeiten-Status inkl. Schließzeit („Jetzt geöffnet, bis 22:30 Uhr", `getOpeningStatus(restaurant.google_opening_hours)`, `lib/openingHours.ts`) plus zwei Buttons „Zur Restaurant-Seite" und `<NavigateButton>` — seit Roadmap-Schritt 18 kommen beide direkt aus der DB, kein lazy `getPlaceDetails()`-Call mehr beim Aufklappen (dadurch entfällt auch der vorherige „Lädt…“-Zwischenzustand, die Daten stehen sofort bereit). Immer nur eine Zeile gleichzeitig aufgeklappt — der `expanded`-State (`expandedId: string | null`) liegt dafür in `SearchResultsViewInner` (nicht mehr lokal in `ResultCard`); Öffnen einer neuen Zeile klappt automatisch die vorher offene ein. **Sortier-Zeile** („Sortieren": Entfernung/Preis/Bewertung, einfache `Chip`-Reihe wie die Filter-Chips, aber Single-Select statt Mehrfachauswahl) direkt unter der Trefferzahl-Zeile — wirkt **sofort** (kein „Übernehmen" nötig, da rein clientseitiges Re-Sortieren der bereits geladenen Liste ohne Refetch, s. `haversineDistanceKm()` aus `lib/geo.ts`). Kein „Standard"-Eintrag mehr (frühere Server-Reihenfolge nach `created_at`) — „Entfernung" ist immer bereits vorausgewählt, unabhängig von `ownLocation`, da jede Ortssuche (getippt, ausgewählt oder „Standort verwenden") ohnehin ein Suchzentrum hat. „Entfernung" sortiert nach Luftlinie zu `center` (Suchzentrum — bei „Standort verwenden" identisch mit dem eigenen Standort), „Preis" aufsteigend nach `price_level` (fehlende Werte ans Ende), „Bewertung" absteigend nach `spoon_rating`. Zeigt neben dem Namen ein „Entwurf"-Badge, wenn `restaurant.status === "draft"` (nur erreichbar mit aktiviertem „Entwürfe in der Suche zeigen"-Toggle, s. Roadmap-Schritt 25). Reicht `ownLocation ? center : null` als `myLocation`-Prop an `MapView` weiter (s.u.). Küche-Filter nutzt `<CuisineFilterDropdown>` (s.o.) statt einer Chip-Reihe. **Filter-Sektion einklappbar** (Roadmap-Schritt 27): Küche-/Preis-/Bewertungs-Zeilen + „Übernehmen"/„Filter zurücksetzen" stehen hinter einem klickbaren „Filter"-Header (State `filtersOpen`) mit rotierendem `IconChevronDown` und einem Zähler-Badge, sobald `activeFilters` (nicht der noch ungespeicherte `pending`-Zustand) mindestens einen Filter enthält — nahmen sonst dauerhaft vertikalen Platz weg, der der eigentlichen Trefferliste fehlte. Default eingeklappt, aber offen, wenn beim Laden bereits ein Filter aktiv ist. Die „Sortieren"-Zeile bleibt davon unberührt, immer sichtbar. `.sr-outer` (Wrapper von Liste+Karte) nutzt `calc(100vh - var(--header-height, 64px))` statt eines hartkodierten `64px` — s. `HeaderHeightObserver.tsx` oben; zusätzlich hat `.sr-results-list` (`globals.css`) ein `padding-bottom` in Höhe des fixierten `.sr-mobile-toggle`-Buttons, damit sich die letzte Trefferzeile immer vollständig über den Button hinausscrollen lässt statt dauerhaft dahinter zu verschwinden
  RestaurantCard.tsx       – Karte in der Restaurant-Grid-Ansicht. Kein Bild-Platzhalter (s. „Kein Self-Hosting von Bildern" unten) — stattdessen farbcodierter oberer Rand + Label passend zur Spoon-Rating-Stufe (`SPOON_RATING_COLORS` aus `lib/ratings.ts`). Zeigt ein goldenes „Entwurf"-Badge, wenn `restaurant.status === "draft"` — normalerweise unerreichbar (Entwürfe werden serverseitig ausgefiltert), außer ein Admin hat den „Entwürfe auch in der öffentlichen Suche zeigen"-Toggle aktiviert (Roadmap-Schritt 25)
  PriceLevelDots.tsx       – Gemeinsame Preisanzeige (0 = „Kostenlos", 1–4 = €-Symbole)
  RatingDots.tsx           – Generische gefüllte/leere Kreis-Anzeige (`value`/`min`/`max`, optional `onChange` für klickbare Admin-Picker, optional `color` für farbcodierte Dots) — für Spoon-Rating (min=0, max=3, `color` aus `SPOON_RATING_COLORS`) und Kategorie-Bewertungen (min=1, max=5 — 5 statt 6 wählbare Werte)
  StarRating.tsx           – Nutzer-Sterne-Durchschnitt mit Halb-Stern-Füllung (CSS-Clip-Technik), neben „Reader Experiences"
  NavigateButton.tsx       – Client Component: „Route planen"-Button auf der Restaurant-Detailseite **und** (aufgeklappt) in `SearchResultsView`s Trefferzeilen. Erkennt beim Klick per `navigator.userAgent`, ob iOS/Android/Desktop: iOS → Apple-Maps-Universal-Link (`maps.apple.com/?daddr=...`), Android → `geo:`-URI (öffnet die installierte Karten-App per Intent), sonst (Desktop) → Google-Maps-Routenplaner im neuen Tab (`google.com/maps/dir/?api=1&destination=...&destination_place_id=...`)
  admin/
    PlacesAutocomplete.tsx – Google Places Autocomplete (Client). Liefert bei Auswahl neben Name/Place-ID/Koordinaten auch einen best-effort Cuisine-Vorschlag aus Googles Place-Type (`primaryTypeDisplayName`/`primaryType`/`types`), s. „Admin-Dashboard" unten
  map/
    MapView.tsx            – Kartenansicht (Client). Marker-Farben kommen aus `SPOON_RATING_COLORS` (`lib/ratings.ts`) statt bespoke Hex-Werten — damit auch im Dark Mode korrekt statt fix eingefärbt. Wechselt reaktiv (per `MutationObserver` auf `<html data-theme>` + `matchMedia`-Listener) zwischen zwei Google-Maps-Map-IDs für Light/Dark, s. „Karten-Dark-Mode" unten. `<Map>` nutzt bewusst unkontrolliertes `defaultCenter`/`defaultZoom` (vis.gl wendet das nur beim Mount an, damit manuelles Pannen/Zoomen während einer Session nicht vom `center`-Prop überschrieben wird) — der `key` der `<Map>` kombiniert deshalb Map-ID **und** Center-Koordinaten, damit ein Remount erzwungen wird, sobald sich der gesuchte Ort ändert (z.B. neue Suche über die Suchleiste innerhalb von `SearchResultsView`); ohne diesen Teil des Keys blieb die Karte nach einer Anschluss-Suche auf dem alten Ort stehen, während die Liste bereits die neuen Treffer zeigte. Optionaler `myLocation`-Prop (`{ lat, lng } | null`, von `SearchResultsView` nur bei `ownLocation=true` gesetzt) rendert einen zusätzlichen `AdvancedMarker` als „Dein Standort"-Punkt — bewusst fixes Google-Blau (`#4285F4`) statt `SPOON_RATING_COLORS`/App-Tokens, da es kein Bewertungs-Marker ist, sondern die universelle „blauer Punkt"-Konvention aus nativen Karten-Apps nachbildet (Ausnahme von „Farben nie hartkodieren" unten, analog zu den von Google selbst gerenderten Kartenkacheln). Klick auf einen Marker öffnet ein `InfoWindow` (`headerDisabled`, kein Google-eigener Header/Schließen-X — Schließen läuft über erneuten Marker-Klick oder Klick auf die Karte) mit Name (größer, `text-lg`, Cormorant, plus „Entwurf"-Badge wenn `restaurant.status === "draft"`, s. `MapRestaurant.status`/Roadmap-Schritt 25), Spoon-Rating-Emoji+Label und einem „Details ansehen →"-Link zur Detailseite; Name/Link-Farbe über `var(--c-ink)`/`var(--c-gold)` statt hartkodierter Hex-Werte. Googles eigenes InfoWindow-Chrome (die weiße Sprechblase samt Pfeil-Spitze) ignoriert React/CSS-Custom-Properties nicht, aber rendert standardmäßig **immer weiß**, unabhängig vom Theme — `app/globals.css` überschreibt deshalb global `.gm-style-iw-c`/`.gm-style-iw-d` (Hintergrund) und `.gm-style-iw-tc::after` (Pfeil-Spitze) mit `var(--c-surface)`. Google setzt auf `.gm-style-iw-d` außerdem inline `overflow: scroll` (nicht `auto`) — das zeigte permanent eine leere, unskinnte (helle) Scrollbar quer über die Karte, da der Inhalt nie tatsächlich Scroll braucht; per `overflow: hidden !important` unterdrückt.

lib/
  ratings.ts               – Single Source of Truth für Spoon-Rating-Emoji/Labels, Kategorie-Labels/-Reihenfolge und `computeAverageRating()`
  cuisine.ts               – `guessCuisine(primaryTypeDisplayName, primaryType, types)`: best-effort Cuisine-Vorschlag aus einem Google-Places-Typ. Geteilt zwischen `components/admin/PlacesAutocomplete.tsx` (Client, Places-JS-SDK-Objekte) und `app/actions/places.ts`s `resolvePlaceForImport` (Server, REST-API-JSON) — beide füttern dieselbe Feld-Kombination hinein, die Heuristik soll nur an einer Stelle leben
  pwa-icon.tsx              – `gpMarkElement(size)`: gemeinsame „GP"-Wortmarken-JSX für `app/icon.tsx`/`app/apple-icon.tsx`/`app/icons/*/route.tsx` (via `next/og` `ImageResponse`, kann keine CSS-Custom-Properties lesen — Hex-Werte matchen `lib/auth-emails.ts`)
  resend.ts                – getResendClient() Factory + RESEND_FROM_EMAIL (Server-only)
  auth-emails.ts           – Deutsche HTML-Templates für Supabase-Auth-Mails (signup, recovery, ...)
  admin.ts                 – PRIMARY_ADMIN_EMAIL (hardcoded Haupt-Admin, s. „Auth & Berechtigungen")
  geo.ts                   – `haversineDistanceKm(a, b)`: Luftlinien-Distanz zwischen zwei lat/lng-Punkten (Haversine-Formel). Kein PostGIS/Geography-Column in Supabase — Distanz-Sortierung/-Filterung passiert deshalb clientseitig bzw. im Server Component gegen die bereits geladene Trefferliste, nicht per DB-Query. `MAX_SEARCH_RADIUS_KM = 30` — Radius, den `app/page.tsx` bei jeder Ortssuche als Cutoff anwendet (s. Roadmap-Schritt 17); bewusst großzügig (deckt eine ganze Metropolregion ab) und unabhängig von Googles unterschiedlich großem Viewport je Ortstyp. `formatDistanceKm(km)` — Anzeige-String („850 m" unter 1km, sonst „3,2 km" mit Komma statt Punkt), verwendet in `SearchResultsView.tsx`s `ResultCard` für die Luftliniendistanz je Trefferzeile
  openingHours.ts          – `getOpeningStatus(weekdayDescriptions, coords?, now?)`: berechnet „geöffnet/geschlossen" lokal aus den gespeicherten Google-„Tag: Zeiten"-Strings (`restaurants.google_opening_hours`) statt per Live-API-Call (s. Roadmap-Schritt 18). Parst Zeit-Ranges per Regex (mehrere Komma-getrennte Ranges pro Tag, „Geschlossen"/„24 Stunden geöffnet" als Sonderfälle), vergleicht gegen die aktuelle Uhrzeit (`now`-Parameter für Tests, Default `new Date()`). **Zeitzone (2026-07-20):** die Google-Zeitangaben sind immer in der lokalen Zeit des Restaurant-Standorts — `now.getHours()`/`.getDay()` würden dagegen die Zeitzone des **Servers** liefern (auf Vercel z.B. UTC), was den Status nahe Öffnungs-/Schließgrenzen falsch berechnet hätte. `coords` (`{ lat, lng } | null`, von den Call-Sites aus `restaurant.lat`/`.lng` durchgereicht) wird per `tz-lookup` (offline Geo→IANA-Timezone-Lookup, kein Netzwerk-Call) in eine IANA-Zeitzone aufgelöst (`resolveTimeZone()`), „jetzt" wird darüber per `Intl.DateTimeFormat` in Wochentag+Uhrzeit dieser Zeitzone umgerechnet (`getLocalDateParts()`) — funktioniert dadurch für Restaurants in beliebigen Zeitzonen, nicht nur Deutschland. Fehlen Koordinaten (z.B. manuell erfasstes Restaurant ohne Kartenpin), Fallback auf `Europe/Berlin` (Default-Locale dieses Guides). Gibt `{ open: boolean | null; until: string | null; opensAt: string | null }` zurück statt eines reinen Booleans — `until` ist die Endzeit der gerade aktiven Zeit-Range (z. B. „22:30 Uhr"), damit „Jetzt geöffnet" auf Detailseite/Suchergebnisliste als „Jetzt geöffnet, bis 22:30 Uhr" angezeigt werden kann; bleibt `null` bei 24h-durchgehend-geöffnet (kein sinnvolles „bis") oder wenn geschlossen/keine Daten. `opensAt` (neu) ist umgekehrt nur gesetzt, wenn `open === false`: die nächste Öffnungszeit — noch heute nur die Uhrzeit („18:00 Uhr", z. B. bei einer Mittagspause), an einem anderen Tag inkl. Wochentagsname („Montag, 08:00 Uhr"); dafür sucht `findNextOpening()` zuerst nach späteren Zeit-Ranges am selben Tag, dann tageweise vorwärts (max. 7 Tage) nach dem nächsten Tag mit Öffnungszeiten. Beide Detailseite und `SearchResultsView.tsx` zeigen bei „Geschlossen" jetzt zusätzlich „, öffnet wieder {opensAt}", falls vorhanden. `open: null` wenn keine Daten vorhanden (dann auch `opensAt: null`). Reine Funktion, läuft sowohl in der Restaurant-Detailseite (Server Component) als auch in `SearchResultsView.tsx` (Client Component)
  googleSync.ts             – `GOOGLE_DATA_STALE_DAYS = 180` (~6 Monate) + `isGoogleDataStale(syncedAt, now?)`: bestimmt, ab wann `restaurants.google_synced_at` als abgelaufen gilt (s. Roadmap-Schritt 19). Reine Funktion, `null`/fehlender Zeitstempel gilt als sofort abgelaufen (nie synchronisiert)

utils/supabase/
  server.ts        – createClient() für Server Components / Actions
  client.ts        – createClient() für Client Components
  admin.ts         – createAdminClient() (Service-Role-Key, nur Server)
  proxy.ts         – updateSession(request, nonce, csp) — Session-Refresh + `PUBLIC_PATHS` (Routen ohne Login-Zwang, s. „Auth & Berechtigungen") + CSP-Nonce-Weiterreichung an Server Components (s. „HTTP-Security-Header")
  auth-helpers.ts  – requireAuth() / requireAdmin()

types/
  database.ts      – Alle DB-Typen, händisch gepflegt (Supabase-Schema)

proxy.ts           – Läuft auf jeder Route außer statischen Assets (Next.js 16: ehem. middleware.ts). Generiert pro Request den CSP-Nonce, s. „HTTP-Security-Header"
public/
  sw.js                      – Service Worker (2026-07-18, s. Roadmap-Schritt 8). Bewusst **kein** vollständiger Offline-Datencache — die App ist durchgängig Supabase-/Auth-Cookie-getrieben (Restaurant-Listen, Kommentare, Admin-Daten), ein Caching dieses HTML riskiert veraltete oder session-falsche Inhalte auf einem geteilten Gerät. Scope: Navigationen laufen immer network-first (`fetch(request).catch(() => caches.match("/offline"))`) — nur bei tatsächlich fehlgeschlagenem Netzwerk-Request (keine Konnektivität) liefert der Service Worker die vorab gecachte `app/offline/page.tsx` statt des Browser-eigenen Netzwerkfehler-Screens. Ein kleines Set eigener, nicht-personalisierter, selten wechselnder statischer Assets (`/icons/192`, `/icons/512`, `/icon`, `/apple-icon`, `/manifest.webmanifest`) wird zusätzlich cache-first bedient. Registriert von `components/ServiceWorkerRegister.tsx`
  map-style.json            – Custom Google Maps Stil (Light Mode), legacy `featureType`/`elementType`/`stylers`-Array-Schema — nur der lokale JS-Fallback (`styles`-Prop), wenn keine Map-ID gesetzt ist
  map-style-dark.json       – Dunkles Pendant zu map-style.json (gleiches Legacy-Schema), s. „Karten-Dark-Mode" unten
  map-style-cloud-light.json – Gleicher Stil wie map-style.json, aber im **anderen** JSON-Schema (`variant`/`styles[].id`/`geometry`/`label`) für den Cloud-Console-„Map Styles"-Import-Wizard (Map-ID-Styling) — die beiden Schemas sind NICHT austauschbar, s. „Karten-Dark-Mode" unten
  map-style-cloud-dark.json  – Dunkles Pendant im Cloud-Schema
```

## Datenbank-Schema (Supabase)

Typen sind in [types/database.ts](types/database.ts) definiert. Nach Schema-Änderungen neu generieren:
```
npx supabase gen types typescript --project-id <id> > types/database.ts
```

### Tabellen

**`restaurants`** — Kernentität
- `id` uuid PK
- `name` text (Pflicht)
- `google_place_id` text | null — für Live-Daten von Google. `null` bei manuell erfassten Restaurants (s. „Manuelle Restaurant-Erfassung" unten) — kein Live-Lookup (Öffnungszeiten/Fotos) möglich, Admin-Tabelle zeigt „No place ID"-Badge
- `lat`, `lng` float | null
- `address` text | null (Migration `20260716000001_restaurant_address.sql`) — Adresse aus der Places-Autocomplete-Auswahl oder manuell erfasst; dient der Restaurant-Detailseite als Fallback, wenn kein `google_place_id` vorhanden ist oder der Live-Lookup fehlschlägt
- `phone` text | null, `website` text | null (Migration `20260716000002_restaurant_contact_info.sql`) — von Google Places befüllt (Autocomplete-Auswahl, CSV-Import, „Von Google synchronisieren", s.u.), aber die App liest **nur noch den gespeicherten Wert** (kein Live-Call mehr bei jedem Seitenaufruf, s. Roadmap-Schritt 18). Manuell erfasste Restaurants (kein `google_place_id`) tragen hier direkt den admin-eingegebenen Wert
- `opening_hours` text | null (Migration `20260716000002_restaurant_contact_info.sql`) — rein manuelles Freitextfeld (z. B. „Mo–Fr 12–22 Uhr"), Fallback wenn `google_opening_hours` (s.u.) leer ist. Wird auf der Detailseite nur angezeigt, wenn keine gespeicherten Google-Öffnungszeiten vorhanden sind
- `google_opening_hours` text[] | null (Migration `20260717000001_google_opening_hours.sql`) — Googles `regularOpeningHours.weekdayDescriptions` (7 deutsche „Tag: Zeiten"-Strings, Mo–So), rein Google-abgeleitet und nicht admin-editierbar (im Gegensatz zu `opening_hours`). Befüllt bei Places-Autocomplete-Auswahl/CSV-Import/„Von Google synchronisieren"; `getOpeningStatus()` (`lib/openingHours.ts`) berechnet daraus lokal den „Jetzt geöffnet" (inkl. Schließzeit)/„Geschlossen"-Status statt einen Live-API-Call zu machen
- `google_synced_at` timestamptz | null (Migration `20260717000002_google_synced_at.sql`) — wann `address`/`phone`/`website`/`google_opening_hours` zuletzt tatsächlich mit Google abgeglichen wurden. Älter als `GOOGLE_DATA_STALE_DAYS` (`lib/googleSync.ts`, ~6 Monate) oder `null` (nie synchronisiert) gilt als abgelaufen — s. `refreshGooglePlaceDataIfStale()` unten (Roadmap-Schritt 19)
- `cuisine` text | null
- `price_level` 0–4 | null (0 = kostenlos, 1–4 = € bis €€€€)
- `spoon_rating` 0–3 (Kern-Bewertungssystem, s.u.) — **kein direkt editierbares Feld mehr**, sondern per DB-Trigger (`sync_restaurant_spoon_rating`) immer aus dem `restaurant_reviews`-Eintrag mit dem höchsten `(visited_at, created_at)` für dieses Restaurant abgeleitet
- `status` `'draft'|'published'` (Default `'published'`, Migration `20260715000007_restaurant_status.sql`) — `draft` ist für normale Nutzer per RLS komplett unsichtbar (s. „Auth & Berechtigungen"), im Admin-Dashboard aber voll sichtbar/editierbar (Badge „Entwurf"). Neu importierte CSV-Einträge (s. `csvImport.ts`) landen immer als `draft`, bis ein Admin sie vervollständigt und veröffentlicht
- `featured` boolean (Default `false`, Migration `20260716000003_restaurant_featured.sql`) — schaltet ein Restaurant für die „Auswahl"-Reihe auf der Landing-Page frei (s. `getFeaturedRestaurants()` unten), unabhängig von der zeitbasierten „Neu hinzugefügt"-Reihe. Admins toggeln dies per Stern-Icon direkt in der Restaurant-Tabelle (`setFeatured()`, kein Umweg über das Edit-Panel nötig)
- `official_review` **entfernt** (Migration `20260715000006`) — ersetzt durch `restaurant_reviews.fazit` + `restaurant_review_categories`

**`restaurant_reviews`** — ein redaktioneller Aufenthalt („Fazit" + Endbewertung). Jedes Restaurant hat immer ≥1 Zeile (Invariante, s. Backfill in Migration `20260715000006`)
- `id` uuid PK, `restaurant_id` uuid FK → restaurants (on delete cascade)
- `visited_at` date (Default heute) — Datum des Aufenthalts, bestimmt „aktuell" vs. „Historie"
- `spoon_rating` 0–3 — Endbewertung dieses Aufenthalts
- `fazit` text — ersetzt das frühere `restaurants.official_review`

**`restaurant_review_categories`** — bis zu 4 unabhängige, optionale Unterbewertungen je Aufenthalt
- `id` uuid PK, `review_id` uuid FK → restaurant_reviews (on delete cascade)
- `category` `'service'|'location'|'geschmack'|'preis_leistung'` (fix, s. `REVIEW_CATEGORY_ORDER` in `lib/ratings.ts`), unique je `(review_id, category)`
- `heading` text | null, `body` text | null — leer = Kategorie wird auf der Detailseite ausgeblendet
- `rating` 0–5 | null

**`profiles`** — Erstellt automatisch via Supabase Auth Trigger
- `id` uuid (= auth.users.id)
- `username` text | null
- `role` `'user'` | `'admin'`
- `status` `'pending'` | `'approved'` | `'rejected'` — neue Registrierungen starten `pending`, erst nach Admin-Freischaltung (`approveProfile`) `approved`. Ohne `approved` überall gesperrt (s. „Auth & Berechtigungen")

**`comments`** — Nutzerkommentare zu Restaurants
- `restaurant_id` uuid FK → restaurants
- `user_id` uuid FK → profiles
- `content` text — max. 150 Zeichen (nur App-seitig validiert in `addComment`/`updateComment`, kein DB-Constraint)
- `secondary_rating` int 0–5 | null — 0 ist ein gültiger, von `null` verschiedener Wert (analog `price_level`, s. `PriceLevelDots.tsx`-Hinweis unten)

**`data_sources`** — Tracking externer Datenquellen (geplant)
- `freq` `'manual'|'hourly'|'daily'|'weekly'`
- `status` `'pending'|'syncing'|'synced'|'error'`

### Spoon-Rating-System
```
0 = 🫗  Not Recommended
1 = 🥄  Remembering
2 = 🍴  Worth Mentioning
3 = 🍽️  Absolute Recommendation
```

### Supabase-Funktionen
- `is_admin()` — RLS-Hilfsfunktion, prüft ob aktueller User `role = 'admin'` hat.
- `is_approved()` — RLS-Hilfsfunktion, prüft ob aktueller User `profiles.status = 'approved'` hat.

## Auth & Berechtigungen

**Kein anonymer Zugriff.** `proxy.ts` gated jede Route: nicht eingeloggt → `/login`, eingeloggt aber nicht `approved` → `/pending`. Öffentlich erreichbar bleiben nur `/login`, `/auth/confirm`, `/auth/reset-password`, `/pending`, `/api/auth/email` (Supabase-Webhook, kein Nutzer-Session-Kontext) sowie `/manifest.webmanifest`, `/icon`, `/apple-icon`, `/icons/*` (PWA-Metadaten — der Browser-Tab-Favicon muss auch auf `/login` selbst laden, und Chrome/Safari fragen das Manifest teils vor dem Login ab) und `/sw.js`, `/offline` (Service Worker + dessen Offline-Fallback-Seite, s. `components/ServiceWorkerRegister.tsx`/`public/sw.js`).

### Zugriffsebenen

| Ebene | Supabase-Zustand | Zugang |
|---|---|---|
| Pending/Rejected | eingeloggt, `status ≠ 'approved'` | nur `/pending`-Screen + Logout |
| User | eingeloggt + `status = 'approved'` | Restaurant-Liste lesen (nur `status = 'published'`), Kommentare erstellen/bearbeiten/löschen |
| Admin | eingeloggt + `role = 'admin'` (Admins werden bei der Migration automatisch auf `approved` gesetzt) | Restaurants CRUD (inkl. `draft`-Einträge), CSV-Import, Registrierungen freischalten/ablehnen, Nutzerverwaltung |

`restaurants` mit `status = 'draft'` sind per RLS (`20260715000007_restaurant_status.sql`) für `approved`-Nutzer unsichtbar — nur Admins (`is_admin()`) sehen sie. Damit sind unfertige/private Einträge (z.B. frisch aus dem CSV-Import) nie versehentlich öffentlich sichtbar.

Neue Registrierungen landen mit `status = 'pending'` in der Datenbank (Spalten-Default, s. Migration `20260715000001_registration_approval.sql`) und müssen im Admin-Dashboard (Sektion „Registrierungen ausstehend") freigeschaltet werden, bevor sie über `/pending` hinauskommen.

**Freischalten setzt eine bestätigte E-Mail-Adresse voraus:** `approveProfile()` (`app/actions/profiles.ts`) prüft serverseitig `auth.users.email_confirmed_at` (via `createAdminClient()`) und wirft einen Fehler, wenn die Adresse noch nicht über den Signup-Bestätigungslink bestätigt wurde — verhindert, dass jemand mit einer E-Mail-Adresse registriert (und freigeschaltet) wird, die er gar nicht kontrolliert. `ProfileWithEmail` (Rückgabetyp von `getPendingProfiles()`/`getAllProfiles()`) trägt dafür ein zusätzliches `emailConfirmed: boolean`-Feld — aus demselben `listUsers()`-Aufruf, den `attachEmails()` ohnehin schon macht, kein zusätzlicher Request. Im Admin-Dashboard ist der „Freischalten"-Button für solche Accounts deaktiviert und zeigt ein „E-Mail nicht bestätigt"-Badge statt eines stillen Fehlschlags erst nach Klick.

### Hardcoded Haupt-Admin (`lib/admin.ts`)

`PRIMARY_ADMIN_EMAIL = "uhllucas@icloud.com"` (Site-Owner) ist **fest im Code** hinterlegt, nicht nur in der Datenbank:
- Migration `20260715000004_hardcode_primary_admin.sql` setzt dieses Konto auf `role = 'admin'`, `status = 'approved'` (idempotent, sicher erneut ausführbar)
- `assertNotPrimaryAdmin()` in `app/actions/profiles.ts` blockiert `demoteFromAdmin`/`deleteUserAccount` für dieses Konto **immer** — unabhängig von der Anzahl übriger Admins (anders als `assertNotLastAdmin`, das nur bei genau einem verbleibenden Admin greift)
- Im Admin-Dashboard (Nutzerverwaltung) zeigt diese Zeile ein „Haupt-Admin"-Badge und hat **keine** Demote-/Lösch-Buttons (auch UI-seitig unterdrückt, nicht nur serverseitig blockiert)
- Ändern des Haupt-Admins erfordert bewusst eine Code-Änderung (`lib/admin.ts`) + neue Migration — kein Env-Var, damit es nicht versehentlich per Konfigurationsfehler geändert werden kann

### Wie Auth-Prüfung funktioniert

In Server Actions immer `requireAuth()`, `requireApproved()` oder `requireAdmin()` aus `utils/supabase/auth-helpers.ts` verwenden — diese werfen bei Verstoß einen Error.

```ts
// Für eingeloggte Nutzer (unabhängig vom Freischaltungsstatus)
const { user } = await requireAuth();

// Für eingeloggte UND admin-freigeschaltete Nutzer (z.B. addComment)
const { user } = await requireApproved();

// Für Admins
await requireAdmin(); // wirft 'Forbidden' wenn kein Admin
```

Daneben gibt es `isCurrentUserAdmin()` (`utils/supabase/auth-helpers.ts`) — **non-throwender** Rollen-Check (gibt `false` statt einen Error zu werfen, auch für ausgeloggte Nutzer), gedacht für Stellen, an denen sich das Verhalten für Admins nur optional ändert, ohne die ganze Funktion hinter `requireAdmin()` zu sperren (z.B. das „Entwürfe auch in der öffentlichen Suche zeigen"-Opt-in in `app/actions/restaurants.ts`, s. dort).

Die RLS-Policies in Supabase sind **zusätzliche Absicherung** — nicht der einzige Schutz. `restaurants`/`comments`-SELECT und `comments`-INSERT sind in RLS ebenfalls an `is_approved()` gebunden.

### Session-Refresh + Zugriffs-Gate (Proxy)

`proxy.ts` (Next.js-16-Nachfolger von `middleware.ts`) ruft `updateSession()` auf jeder Route auf — hält Supabase-JWT frisch **und** entscheidet über Redirects (`/login` bzw. `/pending`, s.o.). Läuft **nicht** auf `_next/static`, `_next/image` und Bilddateien. Proxy läuft standardmäßig im Node.js-Runtime (nicht mehr Edge). Seit der CSP-Einführung (s.u.) generiert `proxy.ts` außerdem pro Request einen Base64-Nonce (`crypto.randomUUID()`) und reicht ihn als `nonce`/`csp`-Parameter an `updateSession()` (`utils/supabase/proxy.ts`) durch — `updateSession()` baut bei **jedem** `NextResponse.next()` (initial und erneut in Supabase's `setAll`-Cookie-Callback) die `request.headers`-Override-Option frisch aus dem zu diesem Zeitpunkt aktuellen `request.headers` neu auf, statt einen einmal geclonten `Headers`-Snapshot wiederzuverwenden — sonst würde die von Supabase im selben Request nachgezogene Session-Cookie-Aktualisierung nicht an das Server-Component-Rendering weitergereicht (stale Session für den Rest des Requests).

### HTTP-Security-Header (`next.config.ts` + `proxy.ts`)

`headers()` in `next.config.ts` setzt app-weit `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin` und ein restriktives `Permissions-Policy` (`camera=(), microphone=(), geolocation=(self), usb=()` — Kamera/Mikrofon/USB bleiben komplett gesperrt, da nirgends genutzt; Geolocation ist seit dem „Standort verwenden"-Button in `LocationSearch.tsx` für die eigene Origin erlaubt, war vorher ebenfalls `()`. Ohne `(self)` schlägt `navigator.geolocation.getCurrentPosition()` mit einem stillen „Permissions policy violation"-Fehler fehl, bevor der Browser überhaupt nach der Nutzer-Erlaubnis fragt).

**Content-Security-Policy** (2026-07-18, umgesetzt in `proxy.ts`, nicht in `next.config.ts` — braucht einen frischen Nonce pro Request, den statische `headers()`-Konfiguration nicht generieren kann): folgt Googles offizieller „Strict CSP"-Empfehlung für die Maps-JavaScript-API (developers.google.com/maps/documentation/javascript/content-security-policy) kombiniert mit Next.js' offiziellem Nonce-Pattern (`node_modules/next/dist/docs/01-app/02-guides/content-security-policy.md`):
- `script-src 'self' 'nonce-<random>' 'strict-dynamic' https: 'unsafe-eval' blob:` — der Nonce erlaubt Next' eigene Framework-/Chunk-Scripts (automatisch von Next.js angewendet, sobald es den Nonce im `Content-Security-Policy`-Response-Header erkennt) sowie das eine handgeschriebene Inline-`<script>` in `app/layout.tsx` (Theme-Flash-Vermeidung, liest den Nonce serverseitig über `headers().get("x-nonce")`, s.u.). `'strict-dynamic'` lässt zusätzlich das von `@vis.gl/react-google-maps` dynamisch per `document.createElement("script")` injizierte `<script src="https://maps.googleapis.com/...">`-Tag zu, ohne diese Domain namentlich in eine Allowlist aufnehmen zu müssen — das injizierende Script läuft selbst innerhalb eines bereits vertrauenswürdigen (Next-gebundelten) Scripts, `strict-dynamic` vererbt dieses Vertrauen. `unsafe-eval`/`blob:` sind Googles eigene Anforderung für die Maps-SDK, nicht für App-eigenen Code.
- `style-src 'self' 'unsafe-inline' https://fonts.googleapis.com` — **bewusst kein Nonce hier**, obwohl `script-src` einen nutzt: die App verwendet durchgängig `style={{...}}`-Inline-Attribute (s. „Dark Mode" unten) — CSP-Nonces gelten nur für `<style>`-Elemente, nie für `style=""`-Attribute auf beliebigen Elementen. Ein Nonce in `style-src` hätte `'unsafe-inline'` für diese Direktive browserseitig automatisch ungültig gemacht (CSP-Spezifikation: Nonce/Hash-Präsenz deaktiviert `unsafe-inline` in derselben Direktive) und damit app-weit jedes Inline-`style`-Attribut blockiert — das gesamte visuelle Design wäre gebrochen.
- `img-src`/`font-src`/`connect-src`/`frame-src`/`worker-src` — `https://*.googleapis.com`, `https://*.gstatic.com`, `https://*.google.com`, `https://*.googleusercontent.com` (Places-Fotos laufen über `lh3.googleusercontent.com`, per Playwright-Test verifiziert) nach Googles Allowlist-Empfehlung; `connect-src` enthält zusätzlich `NEXT_PUBLIC_SUPABASE_URL`. Resend (E-Mail-Versand) taucht bewusst **nicht** auf — läuft ausschließlich serverseitig (Server Action/Route Handler), nie als Browser-Request, also nicht CSP-relevant.
- Der Nonce wird über `headers().get("x-nonce")` in `app/layout.tsx` gelesen und explizit als `nonce`-Prop auf das eine Inline-`<script>` gesetzt (React wendet Nonces nicht automatisch auf handgeschriebene `dangerouslySetInnerHTML`-Scripts an, nur auf eigene Framework-Scripts). React gibt den Nonce-Attributwert clientseitig absichtlich als leeren String zurück (Schutz gegen Nonce-Scraping per XSS) — das erzeugt eine harmlose, erwartete Hydration-Warnung im Dev-Modus, per `suppressHydrationWarning` auf demselben `<script>`-Tag unterdrückt (analog zum bestehenden Muster beim `data-theme`-Attribut).
- Per Playwright-Test (Login, Karten-/Ortssuche inkl. Kachel-Rendering, Restaurant-Detailseite inkl. Google-Fotos, Admin-Dashboard inkl. Places-Autocomplete) verifiziert: keine CSP-Verstöße in der Browser-Konsole.

### E-Mail-Versand (Resend statt Supabase-SMTP)

Supabase Auth erzeugt Bestätigungslinks/Token weiterhin selbst — aber **nicht mehr per eigenem SMTP**, sondern über den [Send Email Hook](https://supabase.com/docs/guides/auth/auth-hooks/send-email-hook), **und** die eigentliche Token-Verifizierung läuft seit 2026-07-20 bewusst **nicht** mehr über GoTrues gehosteten `/auth/v1/verify`-Endpunkt (s. „Klick-gated Verifizierung" unten für den Grund):

1. Supabase ruft bei jeder Auth-Mail (`signup`, `recovery`, `magiclink`, `invite`, `email_change`, `reauthentication`) `POST /api/auth/email` auf (`app/api/auth/email/route.ts`) — signiert nach dem [Standard-Webhooks](https://www.standardwebhooks.com/)-Format.
2. Die Route verifiziert die Signatur mit dem Paket `standardwebhooks` (Secret: `SEND_EMAIL_HOOK_SECRET`, Format `v1,whsec_...`).
3. Aus `token_hash` / `email_action_type` / `redirect_to` wird eine Bestätigungs-URL gebaut, die **direkt auf die eigene App zeigt** (`${redirect_to}?token_hash=...&type=...` — `redirect_to` ist bereits `${origin}/auth/confirm` bzw. `${origin}/auth/reset-password`, s.u.), **nicht** auf GoTrues `${SUPABASE_URL}/auth/v1/verify`-Endpunkt.
4. Das passende deutsche HTML-Template kommt aus `lib/auth-emails.ts`, verschickt wird über `lib/resend.ts` (`getResendClient()`, Absender `RESEND_FROM_EMAIL`).
5. Bei `reauthentication` gibt es keinen Link, sondern einen 6-stelligen Code (`email_data.token`) zum manuellen Eingeben.

Konfiguration in Supabase Dashboard → Authentication → Hooks → Send Email Hook → URL auf `/api/auth/email` zeigen lassen, Secret dort kopieren und als `SEND_EMAIL_HOOK_SECRET` setzen. Ohne konfiguriertes `RESEND_API_KEY`/`SEND_EMAIL_HOOK_SECRET` antwortet die Route mit HTTP 500 (bricht den Auth-Flow bewusst ab, statt E-Mails zu verschlucken).

**Klick-gated Verifizierung (Fix für „Link bereits abgelaufen" beim allerersten Klick, 2026-07-20):** GoTrues `/auth/v1/verify`-Endpunkt verifiziert (und verbraucht damit) den Einmal-Token bei einem einfachen GET-Request. E-Mail-Sicherheits-Scanner (Microsoft Outlook „Safe Links", andere Firmen-Mail-Gateways) rufen jeden Link in einer eingehenden Mail automatisiert per GET auf, um ihn zu prüfen — das verbrauchte den Token, bevor der Nutzer selbst überhaupt geklickt hatte, und der echte Klick landete dann sofort auf „Link ungültig/abgelaufen". Fix: `app/api/auth/email/route.ts` verlinkt nicht mehr GoTrues Auto-Verify-Endpunkt, sondern die eigene `/auth/confirm`- bzw. `/auth/reset-password`-Seite mit dem rohen `token_hash` (+ `type`) als Query-Params. Diese Seiten verifizieren den Token **nicht** beim bloßen Laden (GET, das Scanner auslösen), sondern erst, wenn der Nutzer aktiv einen Button klickt bzw. das Passwort-Formular abschickt — beides eine Server Action (POST), die `supabase.auth.verifyOtp({ token_hash, type })` aufruft (`app/actions/auth.ts`: `confirmEmailToken` für Signup/Invite/E-Mail-Änderung, `updatePassword` für Recovery). Scanner laden die Seite zwar, klicken aber nie den Button/senden nie das Formular ab — der Token bleibt bis zum echten Klick gültig.

**Bestätigungsscreen nach Signup** (`app/auth/confirm/page.tsx` + `ConfirmButton.tsx`): `signUp()` in `app/actions/auth.ts` setzt `emailRedirectTo: ${origin}/auth/confirm`. Die Seite liest `token_hash`/`type` aus der URL und zeigt einen „Konto bestätigen"-Button (`ConfirmButton`, Client Component); Klick löst `confirmEmailToken` aus, das `verifyOtp` aufruft und bei Erfolg zur Startseite redirected (eingeloggt) bzw. bei Fehler eine Fehlermeldung inline zeigt. Fehlt `token_hash` ganz (Seite direkt ohne Link aufgerufen), zeigt sie sofort den Fehler-Screen mit Link zum Login. **Wichtig:** `${origin}/auth/confirm` muss weiterhin in Supabase Dashboard → Authentication → URL Configuration → Redirect URLs eingetragen sein (validiert von GoTrue beim `signUp()`-Aufruf selbst, unabhängig davon, wie die Mail den Link später rendert).

**Passwort zurücksetzen/ändern** (`app/auth/reset-password/page.tsx` + `ResetPasswordForm.tsx`): gleiches Klick-gated Muster wie oben, aber für den `recovery`-Auth-Mail-Typ — verifiziert wird erst beim Abschicken des „neues Passwort"-Formulars, nicht beim Laden der Seite. Beide Auslöser landen auf demselben Ziel:
- **Passwort vergessen** (ausgeloggt): „Passwort vergessen?"-Link im Login-Tab von `LoginForm.tsx` → `requestPasswordReset(email)` → `supabase.auth.resetPasswordForEmail(email, { redirectTo: `${origin}/auth/reset-password` })`.
- **Passwort ändern** (eingeloggt): „Passwort ändern" im `<UserMenu>`-Kebab-Menü im Header → dieselbe `requestPasswordReset`-Action, aber mit der eigenen Session-E-Mail statt eines Formularfelds — es gibt bewusst **kein** Formular mit altem/neuem Passwort inline, nur den E-Mail-Link-Weg.
- `requestPasswordReset` meldet in beiden Fällen immer Erfolg zurück (unabhängig davon, ob die E-Mail existiert oder der Supabase-Call fehlschlägt) — verhindert, dass sich per Fehlermeldung erraten lässt, welche E-Mails ein Konto haben.
- Die Seite liest `token_hash` aus der URL und reicht ihn als verstecktes Formularfeld an `ResetPasswordForm` durch; fehlt er, zeigt sie sofort „Link ungültig". Das Formular ruft `updatePassword(password, confirmPassword, token_hash)` (`app/actions/auth.ts`) auf — die Action verifiziert den `token_hash` per `verifyOtp({ type: "recovery" })` und ruft danach `supabase.auth.updateUser({ password })` auf. Schlägt `verifyOtp` fehl (z.B. Token durch einen doppelten Submit bereits verbraucht), aber es existiert bereits eine Session aus einem vorherigen erfolgreichen Verify auf derselben Seite, läuft `updateUser` trotzdem durch statt einen falschen „Link ungültig"-Fehler zu zeigen.
- **Wichtig:** `/auth/reset-password` muss wie `/auth/confirm` in `PUBLIC_PATHS` (`utils/supabase/proxy.ts`) stehen — vor dem Verify existiert noch keine Session, ein Gate würde den Link samt `?token_hash=`-Query-Param zu `/login` umleiten.

## Server Actions (`app/actions/`)

Alle Actions haben `"use server"` und laufen auf dem Server. API-Keys verlassen den Server nie.

### `auth.ts`
- `signIn` / `signUp` / `signOut` — s. „Auth & Berechtigungen". `signUp` verlangt zusätzlich `emailConfirm` (zweite E-Mail-Eingabe im Formular) und `passwordConfirm` (zweite Passwort-Eingabe) und bricht mit `{ error }` ab, wenn die E-Mail-Felder (getrimmt) oder die Passwort-Felder nicht übereinstimmen — noch bevor ein Supabase-Call ausgelöst wird
- `confirmEmailToken(tokenHash, type)` — löst den Signup-/Invite-/E-Mail-Änderungs-Bestätigungslink ein (`supabase.auth.verifyOtp({ token_hash, type })`), aufgerufen vom „Konto bestätigen"-Button auf `app/auth/confirm/page.tsx` (s. „Klick-gated Verifizierung" oben). Redirected bei Erfolg zur Startseite, sonst `{ error }`
- `resendConfirmationEmail(email)` — schickt per `supabase.auth.resend({ type: "signup", email })` einen neuen Signup-Bestätigungslink (s. Roadmap-Schritt 30), immer `{ success: true }` unabhängig vom tatsächlichen Ergebnis (gleiches Anti-Enumeration-Muster wie `requestPasswordReset`)
- `requestPasswordReset(email)` — löst den `recovery`-Auth-Mail-Versand aus (s. „Passwort zurücksetzen/ändern" oben), immer `{ success: true }` unabhängig vom tatsächlichen Ergebnis
- `updatePassword(password, confirmPassword, tokenHash?)` — verifiziert bei vorhandenem `tokenHash` zuerst den Recovery-Token (`verifyOtp({ type: "recovery" })`, s. „Klick-gated Verifizierung" oben) und setzt danach ein neues Passwort für die **aktuelle Session** (`supabase.auth.updateUser`), validiert Mindestlänge (8) + Übereinstimmung serverseitig

### `restaurants.ts`
- `getRestaurants(filters?)` — Liste, optional gefiltert nach `cuisine`, `price_level`, `spoon_rating`. Alle drei sind **Arrays** (Mehrfachauswahl je Kategorie, z.B. mehrere Küchen gleichzeitig) — intern per Supabase `.in()` statt `.eq()`. Filtert explizit `.eq("status", "published")` — s. Hinweis zu `getRecentRestaurants`/`getFeaturedRestaurants` unten, derselbe Grund gilt hier
- `getCuisines()` — Alle distinct `cuisine`-Werte **ungefiltert bzgl. Such-Filtern** (ignoriert aktive `cuisine`/`price_level`/`spoon_rating`-Filter), aber wie die drei Funktionen unten explizit auf `status = 'published'` eingeschränkt. Wichtig: die Küche-Filterchips in `FilterBar`/`SearchResultsView` müssen aus dieser Funktion stammen statt aus dem (evtl. bereits gefilterten) `getRestaurants()`-Ergebnis — sonst verschwinden Küche-Chips beim Kombinieren mehrerer Filter, weil dann nur noch Küchen der übrig gebliebenen Treffer auftauchen
- `getRecentRestaurants(limit = 6)` — die `limit` neuesten veröffentlichten Restaurants nach `created_at`, kein zeitlicher Cutoff mehr (früher „letzte 30 Tage" — bei einer ruhigeren Phase des Guides hätte das sonst wochenlang eine leere/fast leere Reihe gezeigt). Speist die „Neu hinzugefügt"-Reihe auf der Landing-Page (`app/page.tsx`)
- `getFeaturedRestaurants()` — alle Restaurants mit `featured = true`, alphabetisch; speist die „Auswahl"-Reihe auf der Landing-Page (unabhängig von „Neu hinzugefügt")

  **Alle vier oben filtern standardmäßig explizit `.eq("status", "published")`, nicht nur implizit über RLS.** Grund: RLS blendet Entwürfe zwar für normale Nutzer aus, aber `is_admin()` erlaubt Admins vollen SELECT-Zugriff — ohne den expliziten Filter hätte ein Admin, der (nicht im `/admin/dashboard`, sondern) ganz normal die Startseite/Suche/„Neu hinzugefügt" besucht, dort versehentlich Entwürfe zwischen den echten Einträgen gesehen. `getRestaurantById()` filtert bewusst **nicht** so — Admins müssen einen Entwurf über den Tabellen-Link in der Admin-Tabelle direkt aufrufen können (RLS lässt das für Admins ohnehin zu, Nicht-Admins bekommen dort weiterhin per RLS keine Zeile zurück).

  **Opt-in-Ausnahme (Roadmap-Schritt 25): „Entwürfe auch in der öffentlichen Suche zeigen"-Toggle** im Admin-Dashboard (`AdminDashboard.tsx`, State `showDraftsInSearch`) — standardmäßig **aus**, ein Admin muss ihn explizit per Checkbox aktivieren. Rein clientseitiger Cookie (`gp-show-drafts=1`, gleiches Muster wie `gp-theme`/`ThemeToggle.tsx`, 1 Jahr Laufzeit). Alle vier Funktionen oben prüfen den Cookie über die private Helper-Funktion `shouldIncludeDrafts()` (`restaurants.ts`) und lassen den `.eq("status", "published")`-Filter nur weg, wenn **zusätzlich** `isCurrentUserAdmin()` (`utils/supabase/auth-helpers.ts`, non-throwender Rollen-Check gegen `profiles.role`) den aktuellen Nutzer tatsächlich als Admin bestätigt — ein manuell per DevTools gesetzter Cookie bleibt bei Nicht-Admins dadurch wirkungslos. Sichtbare Entwürfe tragen dann überall ein „Entwurf"-Badge (`RestaurantCard.tsx`, `SearchResultsView.tsx`s `ResultCard`, `MapView.tsx`s `InfoWindow`, Restaurant-Detailseite), damit sie nicht mit echten veröffentlichten Einträgen verwechselt werden.
- `setFeatured(id, featured)` — Admin-only, Quick-Toggle für das Stern-Icon in der Admin-Tabelle (kein Umweg über `updateRestaurant`/Edit-Panel)
- `getRestaurantById(id)` — Einzelnes Restaurant inkl. verschachtelter Comments + Profiles sowie `reviews` (alle Aufenthalte inkl. `categories`, client-seitig nach `visited_at`/`created_at` absteigend sortiert — `reviews[0]` ist immer der aktuelle Aufenthalt, s. `restaurant_reviews` oben)
- `createRestaurant(payload, review)` — Admin-only, legt Restaurant **und** dessen ersten Aufenthalt in einem Aufruf an (ruft intern `createReview` aus `reviews.ts`), `revalidatePath("/", "layout")`. `payload` enthält **kein** `spoon_rating`/`official_review` mehr — das kommt aus `review`
- `updateRestaurant(id, payload)` — Admin-only, revalidiert Startseite + Detailseite (nur Stammdaten: Name/Küche/Preis/Places-Felder — Bewertungsinhalte laufen über `reviews.ts`)
- `deleteRestaurant(id)` — Admin-only
- `deleteRestaurants(ids)` — Admin-only, Bulk-Löschen (ein `.in("id", ids)`-Request statt einer Schleife) — Backend für die Mehrfachauswahl im Admin-Dashboard (s.u.)
- `syncGooglePlaceData()` — Admin-only (s. Roadmap-Schritt 18). Lädt alle Restaurants mit `google_place_id`, ruft **sequenziell** (nicht `Promise.all`, um Google nicht zu bursten — gleiches Muster wie `confirmCsvImport`) `getPlaceDetails()` pro Zeile auf. `address`/`phone`/`website` werden nur befüllt, wenn das Feld noch leer ist (schützt manuelle Korrekturen), `google_opening_hours` wird immer überschrieben (rein Google-abgeleitet, nichts zu schützen); stempelt `google_synced_at` auf jetzt. Ein einzelner fehlgeschlagener Lookup bricht den Lauf nicht ab (`catch` pro Zeile, landet im `failed`-Zähler der Rückgabe `{ restaurants, failed }`). Backend des „Von Google synchronisieren"-Buttons im Admin-Dashboard (s.u.) — läuft **immer** für alle Restaurants, unabhängig von `google_synced_at` (explizite Admin-Aktion überstimmt das automatische Verfallsdatum)
- `refreshGooglePlaceDataIfStale(restaurantId)` — **kein** `requireAdmin()` (s. Roadmap-Schritt 19): läuft für jeden Besucher der Restaurant-Detailseite, da die ganze App ohnehin login-pflichtig ist (s. „Auth & Berechtigungen"). Nimmt seit dem Sicherheits-Fix in Roadmap-Schritt 24 bewusst **nur die ID** entgegen und lädt Restaurant (RLS-gebundener Client) **und** Google-Daten (eigener `getPlaceDetails()`-Aufruf) selbst neu, statt — wie ursprünglich — fertige `restaurant`/`details`-Objekte vom Aufrufer zu übernehmen; s. Roadmap-Schritt 24 für den Grund (jede exportierte Funktion einer `"use server"`-Datei ist ein öffentlicher HTTP-Endpunkt, unabhängig von tatsächlicher Client-Referenzierung). No-op, solange `google_synced_at` noch frisch ist (`isGoogleDataStale()`, `lib/googleSync.ts`) oder kein `google_place_id` gesetzt ist — dann auch kein zusätzlicher Places-Request. Schreibt über `createAdminClient()` (Service-Role), da normale Nutzer laut RLS (`restaurants: admin update`) keine `restaurants`-Zeile schreiben dürfen — sicher, weil ausschließlich selbst frisch von Google abgerufene Felder persistiert werden (kein user-editierbarer Input mehr). Gibt bei einem Schreibfehler das unveränderte Restaurant zurück (stiller Fallback, nächster Seitenaufruf versucht es erneut) statt die Seite abzubrechen; `null`, wenn die ID gar nicht (mehr) sichtbar ist

Bilder werden **nicht** in der DB gespeichert — kommen live von der Google Places API. Die Adresse (`restaurants.address`) wird seit Migration `20260716000001_restaurant_address.sql` dagegen **zusätzlich** persistiert (aus der Places-Autocomplete-Auswahl oder manueller Eingabe) — nötig, damit manuell erfasste Restaurants (kein `google_place_id`, s. „Manuelle Restaurant-Erfassung" unten) und ein fehlschlagender Live-Lookup trotzdem eine Adresse anzeigen können.

### `csvImport.ts`
Import einer Google-Takeout-„Gespeicherte Orte"-Liste (Spalten `Title`/`Titel`, `Note`/`Notiz`, `URL`) mit Abgleich gg. bestehende Restaurants — zweistufiger Flow (Preview → Confirm), damit der Admin vor dem eigentlichen Import sieht, was neu ist und was bereits existiert:
- `previewCsvImport(csvText)` — Admin-only, reiner Dry-Run (keine DB-Schreibzugriffe, keine Places-API-Aufrufe). Parst die CSV client-unabhängig serverseitig (eigener kleiner RFC4180-artiger Parser, keine Library-Abhängigkeit — behandelt gequotete Felder/eingebettete Kommas/Zeilenumbrüche), extrahiert pro Zeile eine `google_place_id` aus der `URL`-Spalte (`place_id:...` oder `query_place_id=...`-Muster) sowie optional eine `note` aus der `Note`/`Notiz`-Spalte (nur zur späteren Fazit-Vorbefüllung, kein Pflichtfeld), und gleicht dann gegen alle bestehenden Restaurants ab: zuerst per `google_place_id`, sonst per case-insensitivem Namensvergleich. Erkennt die Titel-Spalte sowohl als `Title` (EN) als auch `Titel` (DE, je nach Kontosprache des Google-Takeout-Exports); wirft einen Fehler, wenn weder `Title`/`Titel` noch `URL`-Spalte gefunden wird (falsches Export-Format). **Bekannte Einschränkung:** moderne Google-Maps-„Gespeicherte Orte"-Exporte enthalten in der `URL`-Spalte oft Share-Links im Format `.../data=!4m2!3m1!1s0x...:0x...` (ein Feature-/CID-Hash) statt eines klassischen `place_id:...`/`query_place_id=...`-Musters — daraus lässt sich hier keine `google_place_id` extrahieren (wird aber in `confirmCsvImport` per Namenssuche nachgeholt, s.u.). Betroffene Zeilen landen trotzdem korrekt als „Neu" (Matching fällt auf Namensvergleich zurück) und zeigen im Preview einen „Maps ↗"-Link zur manuellen Kontrolle. Der `mapsUrl`-Wert wird vor der Rückgabe per `sanitizeMapsUrl()` auf `http`/`https`-Schema geprüft (sonst `null`) — er landet ungefiltert als echtes `<a href>` im Preview-Modal, und ohne diese Prüfung hätte eine präparierte CSV (z.B. eine gefälschte „Google-Takeout"-Datei) dort eine `javascript:`-URI unterbringen können, die in der authentifizierten Admin-Session ausgeführt würde, sobald der Admin draufklickt
- `confirmCsvImport(selection, bulkSpoonRating?)` — Admin-only, `selection` ist die vom Admin im Preview bestätigte Teilmenge der als „neu" markierten Zeilen (`{ name, googlePlaceId, note }[]`). Legt für jede Zeile **einzeln** (nicht per Batch-Insert, um Restaurant↔Notiz eindeutig zuzuordnen) ein Restaurant mit `status: 'draft'` an sowie einen Platzhalter-Aufenthalt (`spoon_rating: bulkSpoonRating ?? 1`, `visited_at: heute`) in `restaurant_reviews` — hält die Invariante „≥1 Review pro Restaurant" ein. `bulkSpoonRating` (0–3) lässt den Admin im Preview-Schritt des `ImportModal` ein Spoon-Rating auswählen, das für **alle** importierten Zeilen gilt — nützlich, wenn die CSV-Liste selbst schon einer einzigen Bewertungsstufe entspricht (z.B. eine Google-Maps-Liste, die nur „Worth Mentioning"-Orte enthält), statt danach jeden Platzhalter-Aufenthalt einzeln im Edit-Panel korrigieren zu müssen. Pro Zeile wird zusätzlich `resolvePlaceForImport(name, googlePlaceId)` (s. `places.ts`) aufgerufen, um `google_place_id` + `lat`/`lng` **sowie Adresse, Cuisine-Vorschlag, Telefon, Website und Öffnungszeiten** schon beim Import zu befüllen (bestmöglicher Vorschlag per direkter ID-Auflösung oder Textsuche nach dem Namen — schlägt die Auflösung fehl oder wird die aufgelöste `google_place_id` wegen einer Kollision verworfen, s.u., bleiben alle Felder `null`, kein Abbruch des Imports; s. Roadmap-Schritt 18, spart den sonst nötigen Folge-Sync beim ersten Bearbeiten). Ein vorhandener CSV-Kommentar (`note`) wird direkt als `fazit` des Platzhalter-Aufenthalts übernommen statt leer zu bleiben. Der Admin prüft/vervollständigt Kartendaten/Fazit/Kategorien/Cuisine/Preis über den normalen Edit-Panel-Flow (inkl. Places-Autocomplete zur Korrektur) und entfernt dort das „Als Entwurf speichern"-Häkchen, sobald der Eintrag fertig ist
- Matching-Heuristik ist bewusst einfach (exakter `google_place_id`- oder Namens-Treffer) — gedacht für den persönlichen, überschaubaren Restaurant-Bestand dieser App, nicht für Datensätze mit vielen gleichnamigen Filialen

### `reviews.ts`
- `createReview(restaurantId, payload)` — Admin-only. Legt einen neuen Aufenthalt an (`restaurant_reviews`) + bis zu 4 Kategorie-Zeilen (`restaurant_review_categories`, immer alle 4 fixen Kategorien, leere bleiben mit `null`-Feldern bestehen). Der DB-Trigger `sync_restaurant_spoon_rating` aktualisiert danach automatisch `restaurants.spoon_rating`, wenn dieser Aufenthalt der aktuellste ist. Wird sowohl beim Anlegen eines neuen Restaurants (erster Aufenthalt) als auch beim Admin-Häkchen „Als neuen Aufenthalt speichern" verwendet
- `updateReview(reviewId, restaurantId, payload)` — Admin-only, korrigiert einen bestehenden Aufenthalt in-place (Upsert der Kategorie-Zeilen über `onConflict: "review_id,category"`) — Standardfall im Admin-Dashboard beim Speichern (kein neuer historischer Eintrag)
- Beide `revalidatePath("/", "layout")` + `revalidatePath("/restaurant/[id]")`

### `profiles.ts`
- `getPendingProfiles()` — Admin-only, Liste aller `status = 'pending'`-Accounts
- `getAllProfiles()` — Admin-only, Liste **aller** Accounts (für Nutzerverwaltung)
- `approveProfile(profileId)` / `rejectProfile(profileId)` — Admin-only, setzt `status`. `approveProfile` wirft zusätzlich einen Fehler, wenn `auth.users.email_confirmed_at` noch leer ist (s. „Auth & Berechtigungen")
- `promoteToAdmin(profileId)` / `demoteFromAdmin(profileId)` — Admin-only, setzt `role`
- `deleteUserAccount(profileId)` — Admin-only, löscht den `auth.users`-Eintrag komplett (via `createAdminClient()`) — `profiles`/`comments` hängen an `on delete cascade` und verschwinden automatisch mit
- Sicherheitsnetze für `demoteFromAdmin`/`deleteUserAccount`: kein Self-Target möglich (`assertNotSelf`), letzter verbleibender Admin kann nicht demoted/gelöscht werden (`assertNotLastAdmin`), `PRIMARY_ADMIN_EMAIL` (s. „Auth & Berechtigungen") kann nie demoted/gelöscht werden (`assertNotPrimaryAdmin`) — verhindert, dass sich die App am Ende ganz ohne Admin oder ohne Haupt-Admin wiederfindet
- `getPendingProfiles()`/`getAllProfiles()` liefern zusätzlich `email` (via `createAdminClient()`, service-role), da `profiles` keine E-Mail-Spalte hat. Intern über einen einzelnen `adminClient.auth.admin.listUsers({ perPage: 1000 })`-Aufruf (Lookup-Map nach `id`) statt einem `getUserById()`-Aufruf pro Profil — Letzteres feuerte bei N Accounts N parallele Admin-API-Requests auf jedem Dashboard-Load, was mit wachsender Nutzerzahl nicht skaliert (s. „theoretisch 100+ Accounts" bei der Nutzerverwaltung unten)

### `comments.ts`
- `addComment(restaurantId, content, secondaryRating)` — Auth required. `secondaryRating` 0–5, `content` max. 150 Zeichen (nach `.trim()`) — beides serverseitig validiert (wirft `Error`)
- `updateComment(commentId, restaurantId, content, secondaryRating)` — Auth required; gleiche Validierung wie `addComment`; RLS blockiert Fremdzugriff
- `deleteComment(commentId, restaurantId)` — Auth required; RLS erlaubt nur Owner oder Admin
- `adminDeleteComment(commentId, restaurantId)` — Admin-only

### `places.ts`
- `getPlaceDetails(placeId, { photosOnly? })` — Ruft Google Places API (New) auf: `formattedAddress`, `regularOpeningHours`, bis zu 3 `photoUris`, sowie `phone`/`website` (`internationalPhoneNumber`/`websiteUri`). Request mit `languageCode=de&regionCode=DE`, damit `weekdayDescriptions` bereits als deutsche „Tag: Zeiten"-Strings zurückkommen (statt Englisch). Seit Roadmap-Schritt 18 nur noch mit vollem Feldset aufgerufen, wo ohnehin schon ein Admin aktiv mit Google interagiert (Admin-Edit-Panel-Fotovorschau + `syncGooglePlaceData()`/`refreshGooglePlaceDataIfStale()`) — die Restaurant-Detailseite ruft es zwar weiterhin auf (für `photoUris`, die einzige Info, die nie persistiert wird), verwendet aus der Antwort aber nur noch die Fotos, nicht mehr Adresse/Öffnungszeiten/Kontakt (die kommen aus `restaurant.*`). **Kostenoptimierung (2026-07-19, Roadmap-Schritt 26):** `photos` allein ist Google-seitig „Basic Data" (günstigster Places-Details-SKU), `formattedAddress`/`regularOpeningHours`/Telefon/Website sind „Contact Data" — deren Kombination im selben Field-Mask-Request bepreist die **gesamte** Anfrage im teureren Tier, obwohl die Detailseite (mit Abstand der Call-Site mit dem höchsten Volumen — ein Aufruf pro öffentlichem Seitenaufruf) diese Felder gar nicht mehr liest. Die Detailseite ruft deshalb jetzt mit `{ photosOnly: true }` auf (Field-Mask nur `photos`); Admin-Edit-Panel/Sync-Actions bleiben unverändert beim vollen Feldset, das sie zum Befüllen der DB-Spalten brauchen.
- `resolvePlaceForImport(name, placeId)` — Für den CSV-Import (`csvImport.ts`): löst zu einem Namen (+ evtl. bereits extrahierter Place-ID) `google_place_id` + `lat`/`lng` **sowie Adresse, `cuisine`-Vorschlag (per `guessCuisine()`, `lib/cuisine.ts` — dieselbe Heuristik wie `PlacesAutocomplete.tsx`), Telefon, Website und Öffnungszeiten** auf. Bei vorhandener `placeId` erst direkte Detail-Abfrage (Places API `places/{id}`, Feldmaske `id,location,formattedAddress,primaryTypeDisplayName,primaryType,types,internationalPhoneNumber,websiteUri,regularOpeningHours`, mit `languageCode=de&regionCode=DE` — sonst kommt der Cuisine-Vorschlag auf Englisch zurück); schlägt das fehl (z.B. weil die extrahierte ID gar keine echte Place-ID war, s. CID-Einschränkung bei `csvImport.ts`) oder war `placeId` von vornherein `null`, Fallback auf Textsuche (`places:searchText` mit `textQuery: name`, dieselbe Feldmaske, `languageCode`/`regionCode` hier im POST-Body statt als Query-Params) — genommen wird der erste Treffer. Liefert `null`, wenn beides fehlschlägt (kein Fehler, Import läuft mit leeren Kartendaten weiter)
- Immer `cache: "no-store"` — Öffnungszeiten und Foto-URLs ändern sich täglich
- API-Key in `GOOGLE_PLACES_API_KEY` (Server-only, nie `NEXT_PUBLIC_`)

Kein Self-Hosting von Bildern (kein `storage.ts` mehr, s. Roadmap Schritt 3) — die Restaurant-Detailseite zeigt genau die ersten 3 `photoUris` (1 Hero + 2 Thumbnails) direkt von Google. **Nur die Detailseite zeigt Bilder** — `RestaurantCard` (Grid/„Neu hinzugefügt") und `SearchResultsView`s Listenkarten haben bewusst keinen Bild-Platzhalter mehr (früher ein diagonal schraffiertes Placeholder-Quadrat); sie sind textbasiert und color-coden stattdessen die Spoon-Rating-Stufe (`SPOON_RATING_COLORS`).

## Supabase-Clients

| Client | Wo verwendet | Key |
|---|---|---|
| `utils/supabase/server.ts` → `createClient()` | Server Components, Server Actions, Route Handlers | `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` |
| `utils/supabase/client.ts` → `createClient()` | Client Components | `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` |
| `utils/supabase/admin.ts` → `createAdminClient()` | Nur Server, für Service-Role-Operationen in der Nutzerverwaltung (E-Mail-Lookup, Account-Löschung in `profiles.ts`) | `SUPABASE_SERVICE_ROLE_KEY` (geheim!) |

## Umgebungsvariablen

```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=          # nur Server, nie ans Frontend
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=    # für Maps-Komponenten im Browser
NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID=     # optional: Cloud-Console-Map-ID für Light Mode (AdvancedMarker-Support), s. "Karten-Dark-Mode"
NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID_DARK= # optional: zweite Map-ID für Dark Mode, verknüpft mit public/map-style-dark.json in der Cloud Console
GOOGLE_PLACES_API_KEY=              # nur Server, für Places API
RESEND_API_KEY=                     # nur Server, für Auth-Mail-Versand
RESEND_FROM_EMAIL=                  # muss auf verifizierter Resend-Domain liegen
SEND_EMAIL_HOOK_SECRET=             # Standard-Webhooks-Secret aus Supabase Send Email Hook
```

## Admin-Dashboard (`/admin/dashboard`)

- **Server Component** (`page.tsx`): Auth-Guard (redirect zu `/login` wenn nicht eingeloggt, redirect zu `/` wenn kein Admin), lädt initiale Restaurant-Liste (inkl. `reviews:restaurant_reviews(fazit, visited_at, created_at)`-Join, daraus wird serverseitig eine `fazitById: Record<string,string>`-Map — aktuellstes Fazit je Restaurant — gebaut und als `initialFazitById`-Prop durchgereicht, s.u. „Filter-Panel") + `getPendingProfiles()` + `getCuisines()` (als `cuisineSuggestions`-Prop, s. Cuisine-Combobox unten), rendert `<AdminDashboard>`
- **Client Component** (`AdminDashboard.tsx`): oben ein `<BackButton fallbackHref="/">`. Sektion „Registrierungen ausstehend" (Freischalten/Ablehnen via `approveProfile`/`rejectProfile`, immer sichtbar — „Freischalten" ist deaktiviert + zeigt ein „E-Mail nicht bestätigt"-Badge, solange `profile.emailConfirmed` false ist, s. „Auth & Berechtigungen"), Sektion „Nutzerverwaltung" (Zum Admin machen/Admin-Status entfernen/Löschen via `promoteToAdmin`/`demoteFromAdmin`/`deleteUserAccount`, `UserActionModal` mit zweistufiger Bestätigung) — **standardmäßig eingeklappt** (State `userMgmtOpen`, Klick auf den Header mit Konten-Anzahl + Chevron expandiert sie), da theoretisch 100+ Accounts registriert sein könnten und das Dashboard sonst bei jedem Besuch voll ausgeklappt rendern würde. **Filter-Panel** (immer sichtbar, kein Auf-/Zuklapp-Button mehr) mit fünf Zeilen: Suche (Textfeld — bewusst hier statt oben im Toolbar gruppiert, da sie inhaltlich ein weiterer Filter ist, kein Datei-/Datenbank-Vorgang wie „Erweiterte Funktionen"/„Neu hinzufügen"; durchsucht Name/Küche sowie Adresse (`restaurant.address`) und das aktuelle Fazit (`fazitById[restaurant.id]`, s.o.) — z. B. findet „Berlin" auch Einträge, bei denen nur die Adresse oder der Fazit-Text „Berlin" enthält), Status (Chips „Alle"/„Nur Entwürfe"/„Nur veröffentlicht" — Ersatz für den früheren einzelnen „Nur Entwürfe"-Boolean-Toggle), Küche (`<CuisineFilterDropdown>`, gespeist aus **allen** Restaurants im geladenen State unabhängig vom Status — bewusst **nicht** die `cuisineSuggestions`-Prop/`getCuisines()`, die nur veröffentlichte Restaurants zählt und bei einem Bestand, der größtenteils aus Entwürfen besteht, fast alle Küchen ausließ), Preis- und Bewertungs-Chips. „Filter zurücksetzen" (erscheint nur, wenn tatsächlich etwas gesetzt ist) leert jetzt auch das Suchfeld mit. Die Kopfzeile über der Tabelle zeigt statt der reinen Gesamtzahl `„{gefiltert} von {gesamt} Einträgen"`, sobald Suche/Filter die Liste tatsächlich einschränken (sonst weiterhin nur die Gesamtzahl) — ein Zähler, wie viele Einträge den aktuell gesetzten Filterbedingungen entsprechen. Slide-Over-Panel zum Erstellen/Bearbeiten (inkl. Sichtbarkeits-Checkbox „Als Entwurf speichern" sowie einem debounced Autosave-Entwurf in `localStorage`, der bei einem versehentlichen Reload/Tab-Schließen wiederhergestellt werden kann, s. Roadmap-Schritt 31), „Erweiterte Funktionen"-Modal (`ImportModal`, s.u.), Delete-Modal, Toast-Benachrichtigungen. Der Restaurant-**Name** in der Tabelle ist selbst ein `<Link target="_blank">` zur öffentlichen Detailseite (ersetzt den früheren separaten „Ansehen"-Link); Edit/Delete-Buttons sind **immer sichtbar** (kein `opacity-0 group-hover:opacity-100` mehr — auf Mobile gibt es keinen Hover-Zustand, das Ein-/Ausblenden war dort nur hinderlich)
- **„Entwürfe auch in der öffentlichen Suche zeigen"-Toggle** (Roadmap-Schritt 25): eigene Box direkt unter dem `<BackButton>`, oberhalb von „Registrierungen ausstehend" — Checkbox + State `showDraftsInSearch` (`useState(false)`, nach dem Mount per `useEffect` aus `document.cookie` synchronisiert, gleiches SSR-sicheres Default-dann-Sync-Muster wie `ThemeToggle.tsx`). Setzt/löscht rein clientseitig den Cookie `gp-show-drafts` (kein Server Action nötig — die eigentliche Absicherung passiert serverseitig in `app/actions/restaurants.ts` über `isCurrentUserAdmin()`, s. „Server Actions" oben). Betrifft **nicht** diese Tabelle hier (die zeigt Entwürfe dank RLS + `is_admin()` ohnehin immer), sondern die normale Website (Startseite/Suche/Karte) beim nächsten Seitenaufruf — Standardzustand ist aus, muss explizit aktiviert werden.
- **Mehrfachauswahl + Bulk-Löschen**: Jede Tabellenzeile hat eine Checkbox (Spalte ganz links, State `selectedIds: Set<string>`), Header-Checkbox (`toggleSelectAllOnPage()`) wählt/deselektiert alle Zeilen der **aktuell sichtbaren Seite** (`pageItems`, s. Paginierung unten) — nicht mehr alle gefilterten Treffer über alle Seiten hinweg (Standardverhalten paginierter Tabellen, z.B. Gmail/GitHub — ein „Alle auswählen" auf allen 245 gefilterten Zeilen wäre beim Anklicken einer sichtbaren 20er-Seite überraschend gewesen). Auswahlen auf verschiedenen Seiten addieren sich (Union/Differenz statt Ersetzen), bleiben also beim Blättern erhalten. Sobald `selectedIds.size > 0`, erscheint über der Tabelle eine Leiste („X ausgewählt" + „Auswahl aufheben" + „Löschen"). „Löschen" öffnet dasselbe `DeleteModal` wie der Einzel-Löschen-Button in jeder Zeile — die Komponente ist auf `restaurants: Restaurant[]` verallgemeinert (1 Eintrag → Name in der Bestätigung, mehrere → Anzahl) und ruft je nach Fall `deleteRestaurant`/`deleteRestaurants` (`app/actions/restaurants.ts`) auf.
- **Paginierung** (2026-07-18): die Restaurant-Tabelle/-Kartenliste rendert nicht mehr alle gefilterten Treffer auf einmal (bei 245+ Einträgen unübersichtlich und lang scrollend), sondern nur noch `pageItems` — ein `filtered.slice()` für die aktuelle Seite. State `page`/`pageSize` (Default `pageSize = 20`, wählbar aus `PAGE_SIZE_OPTIONS = [10, 20, 50, 100]` über ein `<select>` in der Pagination-Leiste unter der Tabelle). `pageCount`/`currentPage` sind aus `filtered.length`/`pageSize`/`page` **abgeleitet** (`currentPage = Math.min(page, pageCount)`) statt per `useEffect` zurückgesetzt — schrumpft ein Filter die Trefferliste, während eine hintere Seite offen ist, klemmt die Anzeige automatisch auf die letzte gültige Seite statt eine leere Seite zu zeigen. Jeder Filter-/Such-Handler (Statuschips, Küche-Dropdown, Preis-/Bewertungs-Chips über `toggleFilterValue()`, Suchfeld, „Filter zurücksetzen") ruft zusätzlich `setPage(1)` auf, damit eine neue Filterung immer bei Seite 1 beginnt. Eine gemeinsame Pagination-Leiste (Pro-Seite-Auswahl + „X–Y von Z" + „Zurück"/„Seite N von M"/„Weiter") steht einmal unter der Tabelle und gilt für Mobile-Kartenliste und Desktop-Tabelle gleichermaßen (beide rendern `pageItems`, nicht mehr `filtered`). Per Playwright (Desktop + Mobile-Viewport) verifiziert: Seitenzahl-Berechnung bei 245 Einträgen (13 Seiten à 20, 5 Seiten à 50), Navigation vor/zurück, Seitengrößen-Wechsel, Seiten-Reset bei Filteränderung, seitenübergreifend erhaltene Auswahl.
- **Restaurant-Liste auf Mobile als Karten statt Tabelle** (`< sm`): die `<table>` (s.o.) ist auf schmalen Viewports komplett `hidden` — vorher lief sie über den rechten Bildschirmrand hinaus und war nicht scrollbar, wodurch Preis/Bewertung/Platz-ID/Bearbeiten/Löschen unerreichbar waren. Stattdessen rendert ein paralleler `sm:hidden`-Block dieselben `filtered`-Daten als gestapelte Karten (Checkbox + Name + Entwurf-Badge + Auswahl-Stern oben, Küche/Preis/Bewertung als 3-Spalten-Grid, Platz-ID darunter, „Bearbeiten"/„Löschen" als volle Buttons am Kartenfuß) — nutzt dieselben State-Handler (`toggleSelected`, `handleToggleFeatured`, `openEdit`, `setDeleteTargets`) und Badge-Komponenten (`PriceBadge`, `SpoonBadge`) wie die Desktop-Tabelle, keine Datenduplizierung. Eigenes „Alle auswählen"-Checkbox-Label oberhalb der Karten, da die Tabellen-Header-Checkbox auf Mobile nicht sichtbar ist. Per Playwright (iPhone-13-Viewport) verifiziert.
- **„Erweiterte Funktionen"** (ein einzelner Button im Toolbar neben „Neu hinzufügen" — bündelt alles, was kein Anlegen eines neuen Eintrags ist): öffnet `ImportModal` auf einem neuen ersten Schritt „choice" (zwei/drei Kacheln „CSV-Import"/„CSV-Export"/„Von Google synchronisieren" statt vormals drei getrennter Toolbar-Buttons). Der Toolbar-Button selbst zeigt weiterhin einen Inline-Spinner (`gp-spinner-sm`), solange `syncing` läuft, auch nachdem das Modal wieder geschlossen ist.
  - **CSV-Import** (Kachel „CSV-Import" → Modal-Schritt „pick"): Datei-Auswahl, liest die Datei client-seitig per `file.text()` und schickt den Rohtext an `previewCsvImport`; Schritt „preview" (oben ein **optionaler** Spoon-Rating-Chip-Picker „Spoon-Rating für neue Einträge" — State `importBulkSpoonRating: 0|1|2|3|null`, Default `null` = keine Auswahl; erneuter Klick auf einen bereits aktiven Chip deselektiert ihn wieder. Gilt, falls gesetzt, für **alle** neuen Zeilen dieses Imports, praktisch wenn die CSV-Liste bereits einer einzigen Bewertungsstufe entspricht; darunter Liste „Neu" mit Checkboxen, standardmäßig alle an, plus Liste „Bereits vorhanden" nur informativ/nicht auswählbar mit Verweis auf den passenden bestehenden Namen; Zeilen ohne extrahierte Place-ID zeigen einen „Maps ↗"-Link zur manuellen Zuordnung später). „Importieren" ruft `confirmCsvImport` mit der ausgewählten Teilmenge (inkl. `note`) + `importBulkSpoonRating ?? undefined` — ohne Auswahl fällt `confirmCsvImport` serverseitig auf den bisherigen Default (1, „Remembering") zurück; die Zeilen landen ohnehin als `draft` und lassen sich später im Edit-Panel korrigieren (dort macht die Pflichtfeld-Hervorhebung, s.u., ein fehlendes bewusstes Rating sichtbar). Neue Restaurants (immer `status: 'draft'`, mit per `resolvePlaceForImport` bestmöglich vorbefüllten Kartendaten und Notiz-als-Fazit, s. `csvImport.ts`) werden vorne in die Tabelle eingefügt. `confirmCsvImport` patcht den zurückgegebenen `spoon_rating`-Wert in-memory auf das verwendete Rating (dieselbe Korrektur wie bei `createRestaurant`) — sonst zeigt die Tabelle direkt nach dem Import kurzzeitig das falsche (Default-)Emoji, weil der zurückgegebene Datensatz vor dem `sync_restaurant_spoon_rating`-Trigger erfasst wurde
  - **CSV-Export** (Kachel „CSV-Export", `handleExportCsv` in `AdminDashboard.tsx`): baut clientseitig aus dem bereits geladenen `restaurants`-State (**alle** Status, nicht nur die aktuell gefilterte Teilmenge — „gesamte Datenbank") + `fazitById` eine CSV-Datei (Name, Küche, Preis, Spoon-Rating, Status, Featured, Adresse, Telefon, Website, Öffnungszeiten, aktuelles Fazit, Place-ID, Koordinaten, `created_at`) und löst direkt einen Browser-Download aus (`Blob` + temporärer `<a download>`, UTF-8-BOM für korrekte Umlaute in Excel). Kein Server-Roundtrip nötig, da das Dashboard beim initialen Laden ohnehin schon jedes Restaurant unabhängig vom Status lädt. Schließt das Modal sofort nach dem Auslösen des Downloads.
  - **„Von Google synchronisieren"** (Kachel, vormals eigener Toolbar-Button, s. Roadmap-Schritt 18): ruft `syncGooglePlaceData()` (`app/actions/restaurants.ts`) auf, `syncing`-State mit Spinner (`gp-spinner-sm`, jetzt auf dem „Erweiterte Funktionen"-Button statt einem eigenen). Aktualisiert Adresse/Telefon/Website/Öffnungszeiten für **alle** Restaurants mit `google_place_id` in der DB, merged die zurückgegebenen Zeilen in den lokalen `restaurants`-State (kein Reload nötig), Toast zeigt Anzahl aktualisiert/fehlgeschlagen. Sowohl einmaliges Backfill für Bestandsdaten als auch jederzeit erneut auslösbar, falls sich etwas bei Google ändert (z. B. eine neue Telefonnummer). Schließt das Modal sofort nach dem Auslösen — Rückmeldung kommt per Toast, unabhängig davon, ob das Modal noch offen ist
- **„Auswahl"-Toggle**: Stern-Icon (`IconStar`) ganz links in der Actions-Spalte jeder Tabellenzeile, klickbar unabhängig vom Edit-Panel — ruft `setFeatured(id, !r.featured)` (`app/actions/restaurants.ts`) auf und aktualisiert die Zeile optimistisch (Rollback bei Fehler). Gefüllter goldener Stern = Restaurant erscheint in der „Auswahl"-Reihe auf der Landing-Page (`getFeaturedRestaurants()`, s. `app/page.tsx`)
- **Entwurf/Veröffentlicht** (`status`-Feld): Tabellenzeilen mit `status === 'draft'` zeigen ein goldenes „Entwurf"-Badge neben dem Namen; der Status-Filter („Alle"/„Nur Entwürfe"/„Nur veröffentlicht", s. „Filter-Panel" oben) filtert die Tabelle clientseitig auf diese Zeilen. Im Edit-Panel steuert eine Checkbox („Als Entwurf speichern (für Nutzer unsichtbar, bis veröffentlicht)") `form.status`, Default bei neuen Restaurants ist `'published'`
- Benutzt `useTransition` für optimistische Updates
- Google Places Autocomplete im Edit-Panel — befüllt Name, `google_place_id`, lat/lng, `address` **und** einen best-effort `cuisine`-Vorschlag automatisch (s. `PlacesAutocomplete.tsx` oben). Das Cuisine-Feld ist eine eigene `CuisineCombobox`-Komponente (freies Text-Input + echtes gefiltertes Dropdown, kein natives `<input list>` mehr — dessen Browser-Pfeil/Filterverhalten war inkonsistent und wirkte wie ein Dropdown, das „nicht funktioniert"): Vorschläge kommen aus `cuisineSuggestions` (= `getCuisines()`, tatsächlich bereits verwendete Küchen-Werte, keine hartkodierte Liste mehr), gefiltert per Substring-Match während der Eingabe (z. B. „ca" → „Café"); Klick auf einen Vorschlag übernimmt ihn, das Feld bleibt aber frei editierbar für neue Werte. Sobald eine `google_place_id` im Formular steht (frisch ausgewählt oder von einem bestehenden Restaurant geladen), lädt ein `useEffect` zusätzlich per `getPlaceDetails(placeId)` (`app/actions/places.ts`) eine Fotovorschau (bis zu 3 Bilder aus `photoUris`) sowie Telefonnummer/Website (nur wenn die Formularfelder noch leer sind, überschreibt also keine bereits gespeicherten/korrigierten Werte) direkt unter der Adress-Bestätigung — zweite Call-Site dieser Action neben der Restaurant-Detailseite. Derselbe Effekt schreibt außerdem `google_opening_hours` (`weekdayDescriptions`) ins Formular — dieses Feld hat keine eigene manuelle Edit-UI und wird deshalb immer überschrieben (nicht nur wenn leer), im Gegensatz zu Telefon/Website. Erst ein Klick auf „Speichern" persistiert diese Werte tatsächlich (s. Roadmap-Schritt 18)
- **Kontakt & Öffnungszeiten** (Abschnitt im Edit-Panel, unter Cuisine): drei Toggle-Chips („Telefonnummer"/„Website"/„Öffnungszeiten", State `visibleContactFields: Set<ContactField>` in `AdminDashboard`) blenden das jeweilige Eingabefeld ein/aus — so bleibt das Formular übersichtlich, wenn ein Restaurant z. B. keine Website hat. Telefon/Website-Chips werden automatisch aktiviert, sobald Google beim Places-Lookup einen Wert liefert; Öffnungszeiten ist immer rein manuell (kein Live-Pendant außer den Google-Öffnungszeiten weiter oben im Formular-Fluss). Deaktivieren eines Chips leert auch den zugehörigen Formularwert. Funktioniert identisch im Google-Such- und im manuellen Erfassungs-Modus (s. „Manuelle Restaurant-Erfassung" unten) — genau die Felder, die für einen Eintrag relevant sind, lassen sich so gezielt hinzufügen. Direkt über den Toggle-Chips zeigt ein kleiner Hinweis-Block (grüner Haken vs. neutrales Uhr-Icon, State `hasLiveOpeningHours` — aus demselben `getPlaceDetails()`-Aufruf wie die Fotovorschau, s.u.), ob Google für diesen Ort bereits Live-Öffnungszeiten liefert — macht sichtbar, dass die manuelle Eingabe hier nur ein Fallback ist, statt dass der Admin das erst auf der Live-Seite entdeckt.
- **Manuelle Restaurant-Erfassung** (Fallback, wenn Google Maps einen Ort nicht kennt): Button „Ort manuell erfassen" über dem Google-Suchfeld schaltet auf freie Text-Inputs um (Name, Adresse, optional Latitude/Longitude) — `google_place_id` bleibt `null`, was überall im Code bereits gracefully gehandhabt wird (kein Live-Lookup für Öffnungszeiten/Fotos, Admin-Tabelle zeigt „No place ID"). State `manualEntry` (in `AdminDashboard`, nicht in `EditPanel` selbst — das Panel bleibt beim Restaurant-Wechsel gemountet und wird nur per CSS ein-/ausgeblendet, ein lokaler State würde beim Restaurant-Wechsel nicht zurückgesetzt). `openEdit` setzt `manualEntry` automatisch auf `true`, wenn das geladene Restaurant kein `google_place_id` hat. Der „Kontakt & Öffnungszeiten"-Abschnitt (s.o.) steht unverändert auch im manuellen Modus zur Verfügung — dort ohne automatische Google-Vorbefüllung, der Admin wählt per Toggle-Chip gezielt aus, welche Zusatzfelder (Telefon/Website/Öffnungszeiten) dieser Eintrag überhaupt hat
- Slide-Over-Panel (`EditPanel`) ist auf Desktop breiter als früher (`max-w-lg` → `sm:max-w-xl lg:max-w-2xl`); die 4 Kategorie-Karten stehen einspaltig untereinander (kein 2-Spalten-Grid mehr) — genug Breite zum Schreiben längerer Fließtexte je Kategorie, dieselbe Begründung wie bei der öffentlichen Darstellung auf der Detailseite. Fotovorschau zeigt maximal 3 Bilder (`placePhotos.slice(0, 3)`, deckungsgleich mit der Detailseite)
- **Edit-Panel — Aktuelle Bewertung + Kategorien**: Beim Öffnen eines bestehenden Restaurants lädt `openEdit` erst per `getRestaurantById` den aktuellen Aufenthalt + Historie (`loadingEditId` sperrt währenddessen den Edit-Button der Zeile), bevor sich das Panel öffnet. Spoon-Rating/Datum/Fazit gehören zum aktuellen Aufenthalt; die 4 Kategorie-Karten (Service/Location/Geschmack/Preis-Leistung, s. `REVIEW_CATEGORY_ORDER`) sind unabhängige, optionale Sektionen mit je Überschrift/Text/`RatingDots`-Picker (1–5). Checkbox „Als neuen Aufenthalt speichern" entscheidet beim Speichern zwischen `updateReview` (Korrektur des aktuellen Aufenthalts, Standard) und `createReview` (neuer historischer Eintrag mit eigenem Datum) — s. `app/actions/reviews.ts`. Bestehende ältere Aufenthalte werden schreibgeschützt als „Bisherige Aufenthalte" gelistet. Bei neuen Restaurants startet `form.review.spoon_rating` als `null` (nicht mehr mit „Absolute Recommendation" vorbelegt) — erzwingt eine bewusste Auswahl statt eines unbemerkt übernommenen Defaults, s. Pflichtfelder unten.
- **Pflichtfelder im Edit-Panel** (`getMissingRequiredFields()`, `AdminDashboard.tsx`): Name, Adresse (aus Google-Auswahl oder manuell erfasst), Preis, Spoon-Rating und Fazit. Nicht ausgefüllte Pflichtfelder werden live optisch hervorgehoben (Label + Rahmen in `--c-burg`, Asterisk), sobald das Feld leer ist — unabhängig vom Entwurf/Veröffentlicht-Status. „Name" blockiert das Speichern immer (auch als Entwurf); die übrigen vier blockieren den Save-Button nur, wenn `form.status === 'published'` — als Entwurf lässt sich ein unvollständiger Eintrag weiterhin speichern (z.B. frisch aus dem CSV-Import) und später vervollständigen. Der Footer zeigt zusätzlich eine Textzeile mit den fehlenden Feldnamen. Ein leeres Spoon-Rating wird beim Speichern intern auf `1` („Remembering") gemappt (DB-Spalte ist `NOT NULL`), bleibt aber in der UI als „nicht ausgewählt" sichtbar, bis der Admin aktiv einen Wert wählt.
- **Fazit-Vorschau** (Edit-Panel, unter dem Fazit-Textfeld): zeigt live, wie der Text auf der Restaurant-Detailseite gesplittet wird (`splitFazit()`, dieselbe Logik wie `ReviewContent` in `app/restaurant/[id]/page.tsx`) — der erste Satz (bis zum ersten Punkt) erscheint groß/serif als Überschrift, der Rest darunter als normaler Fließtext. Macht dem Admin beim Schreiben sichtbar, welcher Teil des Fazits als Headline gerendert wird, statt das erst auf der Live-Seite zu entdecken.
- **Kein eigener Header mehr** — früher rendertete `AdminDashboard.tsx` einen eigenen `sticky`-Header (Branding, „Admin"-Badge, Public-Site-Link, `<ThemeToggle>`) zusätzlich zum globalen `<Header>` aus `app/layout.tsx`, wodurch auf `/admin/dashboard` zwei Leisten übereinander erschienen. Entfernt — die Seite nutzt jetzt ausschließlich den globalen `<Header>` (der bei `role: 'admin'` ohnehin schon einen „Admin"-Link zeigt). Alle Farben über CSS-Custom-Properties, kein hartkodiertes Tailwind-Farbschema mehr

## Komponenten

### Landing-Page-Suche (`HeroSearch.tsx` + `FilterBar.tsx` + `LocationSearch.tsx`)

Filter-Klicks auf der Startseite lösen **keine** Navigation/Suche mehr aus — sie werden nur lokal gestaged, bis der Nutzer explizit sucht:

- `HeroSearch.tsx` (Client Component, im Hero-Bereich von `app/page.tsx`) hält den Filter-Zustand (`FilterSelection`: `{ cuisine: string[]; price_level: number[]; spoon_rating: number[] }`) in `useState` und reicht ihn kontrolliert an `FilterBar` (Anzeige/Toggle) sowie an `LocationSearch` (`filters`-Prop, wird erst beim Suchen in die URL geschrieben) weiter.
- `FilterBar.tsx` ist rein **controlled** — bekommt `selected` + Toggle-/Clear-Callbacks als Props, macht selbst keine Router-/URL-Aufrufe mehr. Mehrfachauswahl je Kategorie (Klick fügt hinzu/entfernt aus dem Array, kein Single-Select mehr). Jede Kategorie (Küche/Preis/Bewertung) hat einen „Alle"-Chip, der aktiv ist, wenn für diese Kategorie nichts ausgewählt ist (= Standardzustand, entspricht „kein Filter") und beim Klick die Kategorie zurücksetzt.
- `LocationSearch.tsx` mit `size="large"` (nur im Hero verwendet): Eine Auswahl aus dem Autocomplete-Dropdown (Ort oder „Im Guide"-Restaurant) füllt nur das Eingabefeld und merkt sich die Auswahl (`pendingSelection`) — es wird **nicht** navigiert. Auch Enter ohne hervorgehobenen Dropdown-Eintrag tut nichts. Navigation (inkl. Anhängen der gestagten Filter als wiederholte Query-Params, z.B. `?cuisine=Italienisch&cuisine=Indisch`) passiert ausschließlich über den Klick auf den „Suchen"-Button (`handleSearch`). Das Ergebnis landet in `SearchResultsView` (Karte + Liste). `size="compact"` (Verfeinerungs-Suchleiste innerhalb von `SearchResultsView`) verhält sich weiterhin wie vorher: Auswahl/Enter navigiert sofort (kein sichtbarer Button, kein gestagter Filter-Zustand nötig). Der „Standort verwenden"-Button (s. Eintrag zu `LocationSearch.tsx` oben, Roadmap-Schritt 14) navigiert in **beiden** Größenvarianten sofort und unabhängig vom „Suchen"-Button/Autocomplete-Flow.
- `app/page.tsx` liest `cuisine`/`price_level`/`spoon_rating` als wiederholte Query-Params (`string | string[]`, per `toArray()`-Helper normalisiert) und reicht sie als Arrays an `getRestaurants()`/`SearchResultsView` weiter. Unterhalb der Suchleiste (nur im Normal-Modus, nicht während einer Ortssuche) zeigen zwei horizontal scrollbare Reihen — zuerst „Auswahl" (`getFeaturedRestaurants()`, admin-kuratiert per `featured`-Toggle, s. „Admin-Dashboard" unten), danach „Neu hinzugefügt" (`getRecentRestaurants()`, die 6 neuesten, kein zeitlicher Cutoff mehr) — beide nur gerendert, wenn die jeweilige Liste nicht leer ist. Beide Reihen-Container haben `paddingTop` (zusätzlich zum bisherigen `paddingBottom`) — `overflowX: "auto"` berechnet `overflow-y` implizit als `auto` mit (CSS-Spezifikation: „auto"/"hidden" statt „visible", sobald eine Achse nicht „visible" ist), wodurch der `card-hover`-Hebeeffekt (`translateY(-4px)` + Schatten) der Karten am oberen Zeilenrand ohne dieses Padding abgeschnitten wirkte. Das volle, unfilterte Restaurant-Grid darunter rendert nur noch, wenn `hasFilters` (bzw. `params.q`) aktiv ist — s. `page.tsx`-Eintrag oben.
- `SearchResultsView.tsx`s eigene Filter-Chips navigieren **nicht** mehr sofort bei Klick — sie sind wie auf der Landing-Page lokal gestaged (`pending`-State) und werden erst per „Übernehmen"-Button in die URL übernommen (s. Eintrag zu `SearchResultsView.tsx` oben). Das Staging-Verhalten gilt damit durchgängig für alle Filter-Chips der App, nicht mehr nur für die Landing-Page vor dem ersten Suchen.

### `components/admin/PlacesAutocomplete.tsx`
Client Component. Gibt bei Auswahl ein `PlaceSelection`-Objekt zurück:
```ts
{ name: string; placeId: string; address: string; lat: number; lng: number; cuisine: string | null }
```
`address` (formatierte Adresse aus der Places-Antwort, `formattedAddress`/`formatted_address` je nach API-Variante) landet direkt in `form.address` und wird beim Speichern in `restaurants.address` persistiert (s. „Bilder werden nicht in der DB gespeichert" oben). Im Edit-Panel dient sie zusätzlich als flüchtige Bestätigungs-Anzeige direkt unter der Google-Suchzeile („das ist der übernommene Eintrag": Name + Adresse, Fallback auf Koordinaten wenn keine Adresse verfügbar).

`cuisine` ist ein best-effort Vorschlag, kein garantierter Wert: aus `primaryTypeDisplayName` (neue Places-API) bzw. `primaryType`/`types` (beide API-Varianten, `_restaurant`-Suffix wird abgeschnitten und snake_case prettifiziert) abgeleitet, `null` wenn nichts Brauchbares gefunden wird (z.B. nur generische Typen wie `restaurant`/`food`/`point_of_interest`). Überschreibt im Admin-Dashboard das `cuisine`-Formularfeld bei Auswahl, das Feld bleibt aber ein freies Text-Input (kein festes Dropdown mehr) — der Admin kann den Vorschlag jederzeit korrigieren.

### `components/map/MapView.tsx`
Client Component. Erwartet Restaurants mit lat/lng für Kartenanzeige. Marker-Füllfarbe kommt aus `SPOON_RATING_COLORS` (`lib/ratings.ts`, CSS-Custom-Properties) statt fixer Hex-Werte — vorher hartkodierte Marker-Farben blieben im Dark Mode unverändert, was gegen „Farben nie hartkodieren" (s. „Dark Mode" unten) verstieß.

### Karten-Dark-Mode
Google ignoriert das `styles`-JSON-Prop **immer**, sobald eine Map-ID gesetzt ist (nötig für die runden `AdvancedMarker`-Emoji-Pins) — eine einzelne Map-ID kann sich also nicht per JS auf Light/Dark umstellen. Lösung: zwei Map-IDs, je eine pro Theme, ausgewählt zur Laufzeit:
- `MapView.tsx` beobachtet `<html data-theme>` per `MutationObserver` (ThemeToggle.tsx mutiert das Attribut direkt ohne Event) + `matchMedia("(prefers-color-scheme: dark)")` für den Systemfall, und wählt daraus `NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID` (hell) oder `NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID_DARK` (dunkel).
- Die `<Map>`-Komponente bekommt `key={mapId}` — Google-Map-IDs sind auf einer bestehenden Instanz unveränderlich, daher erzwingt der Key-Wechsel einen sauberen Remount statt eines stillen No-ops.
- Ist `NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID_DARK` nicht gesetzt, fällt Dark Mode auf die helle Map-ID zurück (kein Fehler). **Manueller Schritt in der Google Cloud Console nötig**, um Dark Mode tatsächlich zu sehen: eine zweite Map-ID anlegen und dort einen Kartenstil verknüpfen, dann `NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID_DARK` setzen.
- Ist gar keine Map-ID gesetzt (weder hell noch dunkel), greift der lokale Fallback: `styles={mapStyle}`/`styles={mapStyleDark}` (`public/map-style.json`/`public/map-style-dark.json`) direkt im Code — funktioniert dann aber ohne `AdvancedMarker`-Custom-Pins.
- **Zwei unterschiedliche, NICHT austauschbare JSON-Schemas** (per Trial & Error gefunden, 2026-07-16 — Cloud Console meldete „Syntax-Fehler" beim Versuch, das falsche Schema zu importieren):
  - `public/map-style.json`/`map-style-dark.json` — Legacy-Schema (`featureType`/`elementType`/`stylers`-Array). Nur für den lokalen JS-`styles`-Prop-Fallback (oben) relevant, wird **nie** in die Cloud Console eingefügt.
  - `public/map-style-cloud-light.json`/`map-style-cloud-dark.json` — Cloud-based-Maps-Styling-Schema (`{ "variant": "light"|"dark", "styles": [{ "id", "geometry": { "fillColor", "strokeColor", "strokeWidth" }, "label": { "textFillColor", "textStrokeColor", "visible" } }] }`). **Das ist das Format, das die Cloud Console → „Map Styles" → JSON-Import-Wizard erwartet** — dort den Inhalt der jeweiligen `map-style-cloud-*.json` einfügen, um eine Map-ID mit dem passenden Stil zu verknüpfen. `strokeWidth` darf hier (anders als evtl. vermutet) durchaus Dezimalwerte haben (z.B. `0.4`) — der Import-Fehler kam vom falschen Schema, nicht von Nachkommastellen.
  - Bei Änderungen an der Optik (Farben, Straßenbreiten etc.) **beide** Schema-Paare parallel pflegen, sonst laufen JS-Fallback und Cloud-Console-Stil auseinander.

### Zurück-Navigation (`components/BackButton.tsx`)
`router.back()`-Button statt fester `<Link href="/">`s, damit z.B. von der Restaurant-Detailseite aus die vorherige Suchergebnisliste (inkl. Scroll-Position/Filter) exakt wiederhergestellt wird statt immer zur Startseite zu springen. Erkennt per `document.referrer` (same-origin?) + `window.history.length`, ob überhaupt eine sinnvolle In-App-History existiert — sonst (Deep-Link, neuer Tab) rendert er stattdessen einen normalen `<Link href={fallbackHref}>`. Eingebaut auf der Restaurant-Detailseite (`fallbackHref="/"`) und im Admin-Dashboard (`fallbackHref="/"`); bewusst **nicht** auf `/login`, `/pending`, `/auth/confirm` (kein sinnvolles Rückziel, Teil des Auth-Flows).

### `components/PriceLevelDots.tsx`
Reine Darstellungskomponente: `level === 0` rendert Text „Kostenlos", `level` 1–4 rendert „€"-Zeichen (abgedunkelt oberhalb von `level`), `level === null` rendert nichts. Einzige Stelle mit dieser Logik — nicht erneut inline implementieren. **Wichtig:** `0` ist ein gültiger, von `null` verschiedener Wert — Call-Sites und Filter-Logik müssen `== null`/`!== undefined` statt truthy-Checks verwenden (sonst verschwindet der „Kostenlos"-Fall).

### `components/RatingDots.tsx`
Generische gefüllte/leere Kreis-Anzeige, `max - min + 1` Kreise, Kreis `i` (von `min` bis `max`) gefüllt wenn `i <= value`. Ersetzt die früher inline duplizierte Spoon-Punkte-Logik auf der Restaurant-Detailseite (jetzt `<RatingDots value={restaurant.spoon_rating} max={3} color={...} />`, `min` default `0`) und wird für die 1–5-Kategorie-Bewertungen verwendet (`min={1} max={5}` — bewusst 5 statt 6 wählbare Werte, `0` ist keine gültige Kategorie-Bewertung mehr). Optionaler `color`-Prop färbt gefüllte Dots (Default `var(--c-gold)`) — für Spoon-Ratings wird `SPOON_RATING_COLORS[rating].text` übergeben, damit die Dots zur Bewertungsstufe passen statt immer Gold zu sein. Ohne `onChange` rein darstellend (`<span>`s, für Server Components/Detailseite), mit `onChange` klickbare `<button>`s (Admin-Editor) — dieselbe Datei funktioniert in beiden Kontexten, solange die Interaktivität nur innerhalb eines bereits-Client-Baums (z.B. `AdminDashboard.tsx`) verdrahtet wird.

### `components/StarRating.tsx`
Rein lesende Durchschnitts-Sterne-Anzeige (0–5) mit Teilfüllung — pro Stern zwei überlagerte Glyphen (grauer Grundstern + goldener Stern, dessen `width` per `overflow:hidden` auf den Bruchteil geclippt wird), sodass z.B. ein Durchschnitt von 3,5 drei volle + einen halb gefüllten Stern zeigt. Nutzt `computeAverageRating()` aus `lib/ratings.ts` als Eingabe.

## `lib/ratings.ts`
Single Source of Truth für Bewertungs-bezogene Konstanten/Helper — wird von `RestaurantCard`, der Detailseite, `SearchResultsView`, `MapView`, `FilterBar` und `AdminDashboard` importiert, bei neuen Stellen immer von hier importieren statt neu zu deklarieren:
- `SPOON_RATINGS`, `SPOON_RATING_ORDER` — Spoon-Rating-Emoji/Labels
- `SPOON_RATING_COLORS` — konsequente Farbcodierung je Rating-Stufe (`{ text, bg, border }`, CSS-Custom-Properties: 3=Gold (beste Bewertung), 2=Flieder (`--c-lilac`, zweitbeste), 1=Altrosa/Mauve (`--c-mauve`, „Remembering"), 0=Grau (Not Recommended)), verwendet auf der Detailseite (Verdict-Box + Dots), `RestaurantCard`, `SearchResultsView`, `MapView`-Marker und im Admin-`SpoonBadge` — bei neuen Stellen mit Spoon-Rating immer von hier importieren statt eigene Farben zu erfinden
- `REVIEW_CATEGORY_ORDER`, `REVIEW_CATEGORY_LABELS` — feste Reihenfolge/Labels der 4 Kategorie-Unterbewertungen (Service/Location/Geschmack/Preis-Leistung), Bewertung jeweils 1–5 (s. `RatingDots` oben)
- `computeAverageRating(ratings)` — Durchschnitt aus `comments.secondary_rating`-Werten (`null` wenn keine bewerteten Kommentare vorhanden), Eingabe für `StarRating`

## Dark Mode

Ausschließlich über CSS Custom Properties in `app/globals.css` — **kein** Tailwind `dark:`-Variant (nicht konfiguriert, würde nur auf OS-Präferenz reagieren statt auf die manuelle Nutzerwahl).

- Tokens: `--c-bg`, `--c-surface`, `--c-ink`, `--c-gold`/`-light`/`-mid`, `--c-burg`/`-light`, `--c-success`/`-light`, `--c-lilac`/`-light`, `--c-mauve`/`-light`, `--c-n50`…`--c-n700`. Je einmal für Light (`:root`), System-Dark (`@media (prefers-color-scheme: dark)`), explizit `[data-theme="dark"]` und explizit `[data-theme="light"]` definiert — bei neuen Tokens **immer alle vier Stellen** pflegen.
- Umschaltung: `ThemeToggle.tsx` setzt `document.documentElement.dataset.theme` + Cookie `gp-theme` (1 Jahr); Server liest das Cookie beim ersten Render, damit kein Flash/Mismatch entsteht.
- **Neue Komponenten/Farben immer über die Tokens**, nie hartkodierte Tailwind-Farbklassen (`text-stone-500`, `bg-white`, `#4a1520` o.ä.) oder eigene Hex-Werte — sonst bricht Dark Mode für diese Stelle. Zwei Muster im Code:
  - Server Components / viele Client Components: inline `style={{ color: "var(--c-ink)" }}` (s. `Header.tsx`, `FilterBar.tsx`)
  - Tailwind-lastige Components (z.B. `AdminDashboard.tsx`): Arbiträre Werte `text-[var(--c-ink)]`, `bg-[var(--c-surface)]`, `border-[var(--c-n200)]` etc.
- Rot/Grün/Gelb-Badges (Status, Danger-Buttons) nutzen **keine** eigenen Ampel-Farben, sondern die vorhandenen Marken-Tokens: „danger" → `--c-burg`/`-light`, „success/approved" → `--c-success`/`-light`, „pending/warn" → `--c-gold`/`-light` — hält die Palette klein und garantiert Dark-Mode-Konsistenz.

## Responsive Design & PWA-Ziel

**Die App muss durchgehend sowohl auf Desktop als auch auf Mobile gut aussehen und benutzbar sein** — nicht nur „funktioniert technisch", sondern layout- und bedienungsmäßig auf beiden Formfaktoren geprüft. Grund: geplant ist, die App später zusätzlich als installierbare Web-App (PWA) auf Mobilgeräten anzubieten — ein primär für Desktop gedachtes Layout wäre dafür ungeeignet.

- Bei jeder neuen/geänderten Komponente/Seite: Tailwind-Breakpoints (`sm:`/`md:`/`lg:`) bewusst einsetzen, nicht nur die Desktop-Ansicht im Kopf haben. Insbesondere Modals/Slide-Over-Panels (s. `AdminDashboard.tsx`), Tabellen (auf Mobile ggf. horizontal scrollbar oder alternative Kartenansicht) und die Landing-Page-Suche (`HeroSearch`/`FilterBar`/`LocationSearch`) im Blick behalten.
- Wo möglich per Browser-Test (Playwright) sowohl in einer Desktop- als auch einer Mobile-Viewport-Größe verifizieren, bevor ein UI-Task als abgeschlossen gilt (s. Hinweis unter „UI/Frontend-Änderungen" in den allgemeinen Arbeitsanweisungen).
- **PWA-Grundausstattung umgesetzt** (`app/manifest.ts`, `app/icon.tsx`/`app/apple-icon.tsx`, `app/icons/*/route.tsx`, `appleWebApp`-Metadata in `app/layout.tsx`, `<InstallPwaInstructions>` auf der Landing-Page). Icons werden zur Laufzeit via `next/og` `ImageResponse` aus `lib/pwa-icon.tsx` generiert, kein statisches Bild-Asset nötig. **Service Worker** (`public/sw.js`, seit 2026-07-18) ergänzt einen Offline-Fallback (`app/offline/`) für Navigationen ohne Netzwerk sowie einen Cache-first-Cache für die eigenen PWA-Icons/das Manifest — bewusst **kein** Offline-Cache für Restaurant-/Kommentar-Daten (auth-/session-getrieben, s. `public/sw.js`-Eintrag oben), die App braucht für ihre eigentlichen Inhalte also weiterhin eine Verbindung.

## Wichtige Muster

- **Server-first**: Datenabruf in Server Components, Mutationen als Server Actions
- **Kein API-Layer**: Keine Route Handlers für CRUD — direkt Server Actions. Ausnahme: `app/api/auth/email/route.ts`, weil Supabase dies als HTTP-Webhook aufruft (Server Actions sind dafür nicht ansprechbar)
- **revalidatePath nach Mutation**: Immer aufrufen um Next.js Cache zu leeren
- **Typen aus `types/database.ts`**: Nie inline tippen — immer die exportierten Convenience-Typen verwenden (`Restaurant`, `Comment`, `Profile`, `DataSource`, `RestaurantReview`, `RestaurantReviewCategory`, `ReviewWithCategories`, `RestaurantWithComments`, `CommentWithProfile`)
- **Google-Places-Requests sparen, wo möglich**: Adresse/Telefon/Website/Öffnungszeiten werden **nicht mehr live** bei jedem öffentlichen Seitenaufruf von Google geladen, sondern in `restaurants.*` persistiert und von dort gelesen (s. Roadmap-Schritt 18) — befüllt bei Autocomplete-Auswahl/CSV-Import/„Von Google synchronisieren" sowie automatisch, sobald `restaurants.google_synced_at` älter als `GOOGLE_DATA_STALE_DAYS` (~6 Monate, `lib/googleSync.ts`) ist (`refreshGooglePlaceDataIfStale()`, Roadmap-Schritt 19 — piggybackt auf den für Fotos ohnehin nötigen Live-Call, kein zusätzlicher Request). **Einzige Ausnahme: Fotos** — die werden nie persistiert (Googles Foto-URIs sind signiert/kurzlebig) und deshalb weiterhin bei jedem Detailseiten-Aufruf live per `getPlaceDetails()` mit `cache: "no-store"` gezogen. Neue Stellen, die Google-Text-Infos brauchen, sollten aus der DB lesen statt einen eigenen Live-Call einzuführen
- **Google-APIs immer mit `languageCode=de&regionCode=DE`**: Sowohl serverseitig (REST-Aufrufe in `app/actions/places.ts`, `getPlaceDetails()` **und** `resolvePlaceForImport()`) als auch clientseitig (jedes `<APIProvider>` — `LocationSearch.tsx`, `MapView.tsx`, `AdminDashboard.tsx` — trägt `language="de" region="DE"`, wirkt für die ganze darunter geladene Maps-JS-API-Session inkl. Autocomplete/Geocoder/`Place.fetchFields`). Ohne das liefert Google Adressen/Öffnungszeiten/Cuisine-Typnamen (`primaryTypeDisplayName`, Basis für den Cuisine-Vorschlag) auf Englisch statt Deutsch. **Ausnahme:** die eigenen Spoon-Rating-Labels (`SPOON_RATINGS[...].label`/`labelShort`, `lib/ratings.ts`) bleiben bewusst Englisch — kein Google-Text, sondern App-eigene Bewertungsstufen-Namen
- **App-UI durchgehend Deutsch**: Alle Nutzertexte (Buttons, Labels, Platzhalter, Toasts, aria-labels) sind Deutsch — einzige bewusste Ausnahme sind die Spoon-Rating-Labels aus `lib/ratings.ts` (s.o.). Neue UI-Strings immer auf Deutsch anlegen, nicht aus Gewohnheit auf Englisch zurückfallen
- **Spoon-Ratings aus `lib/ratings.ts`**: Emoji/Label nie inline duplizieren — von dort importieren
- **Farben nie hartkodieren**: Immer CSS-Custom-Properties aus `app/globals.css` verwenden (s. „Dark Mode" oben) — sonst bricht Dark Mode an dieser Stelle
- **Keine Emojis außer Spoon-Rating**: Dekorative Emojis (Status-Icons, Buttons) sind aus der App entfernt — stattdessen `components/icons.tsx` verwenden. Einzige bewusste Ausnahme sind die Spoon-Rating-Glyphen aus `lib/ratings.ts` (`SPOON_RATINGS[...].emoji`). Reine Text-Glyphen wie `★` (Sterne-Bewertung) oder `✕` (Schließen-Symbol) zählen nicht als Emoji und bleiben erlaubt. Die Leer-/Fehlerzustände (`app/error.tsx`, „Keine Restaurants gefunden" in `app/page.tsx` und `SearchResultsView.tsx`) verwenden bewusst wieder 🫗 (dasselbe Glas wie `SPOON_RATINGS[0]`, „Not Recommended") statt `IconEmptyState` — fällt unter dieselbe Spoon-Rating-Ausnahme, `IconEmptyState` bleibt für andere (nicht-Fehler-/Leer-)Fälle nutzbar.
- **Responsive Pflicht**: Jede neue/geänderte UI muss sowohl auf Desktop als auch auf Mobile gut aussehen (s. „Responsive Design & PWA-Ziel" unten) — kein Feature gilt als fertig, wenn es nur auf einer Breakpoint-Größe getestet wurde
- **Kein horizontales Scrollen des Dokuments**: `app/globals.css` setzt `overflow-x: hidden` + `max-width: 100vw` auf `html, body` als Sicherheitsnetz — kein Teil der App darf horizontales Scrollen der Seite selbst auslösen oder erfordern. Absichtlich horizontal scrollbare Inhalte (z.B. die „Auswahl"-/„Neu hinzugefügt"-Reihen auf der Landing-Page, `overflowX: "auto"`) müssen dafür in ihrem **eigenen** Container scrollen, nie im `body`

## Entwicklung

```bash
npm run dev    # Startet auf http://localhost:3000
npm run build  # Produktions-Build
npm run lint   # ESLint
```

> Hinweis: `AGENTS.md` enthält wichtigen Hinweis zu Next.js 16 Breaking Changes — vor dem Schreiben von Next.js-Code lesen.

## Roadmap (Stand 2026-07-16)

Reihenfolge nach Abhängigkeiten, nicht nach Bedeutung. Status hier bei jedem abgeschlossenen Schritt aktualisieren.

1. **Zugriffsmodell: Login-Pflicht + Registrierungs-Freischaltung durch Admin** — ✅ umgesetzt, per Browser-Test (Playwright) verifiziert
   `profiles.status` (`pending`/`approved`/`rejected`), RLS auf `restaurants`/`comments` nur für `approved`, `proxy.ts` gated jede Route außer `/login`, `/auth/confirm`, `/pending`, `/api/auth/email`, Admin-Dashboard hat Freischalt-UI. **Bricht bewusst mit der bisherigen Doku-Aussage „Besucher sehen die Liste"** — Besucher-Ebene entfällt.
   Migrationen (alle gepusht, 2026-07-15):
   - `20260715000001_registration_approval.sql` — `profiles.status`, `is_approved()`, RLS-Umstellung
   - `20260715000002_fix_username_trigger.sql` — Bugfix: `handle_new_user()`-Trigger persistierte `username` aus dem Signup nie (nur `id` wurde inserted), gefunden beim Testen der Freischalt-Liste
   - `20260715000003_admin_profile_update.sql` — Bugfix: es fehlte eine RLS-Policy, die Admins erlaubt, `profiles` **anderer** Nutzer zu updaten (nur „self update" existierte) → Freischalten/Ablehnen aktualisierte 0 Zeilen, ohne Fehler zu werfen (dieselbe Policy trägt jetzt auch Promote/Demote in der Nutzerverwaltung, s.u.). `approveProfile`/`rejectProfile`/`promoteToAdmin`/`demoteFromAdmin` prüfen jetzt zusätzlich, dass tatsächlich eine Zeile getroffen wurde, statt einem stillen No-op zu vertrauen.

   **Nutzerverwaltung im Admin-Dashboard** (Sektion „Nutzerverwaltung", unter „Registrierungen ausstehend") — ✅ umgesetzt, per Browser-Test verifiziert: Admins können jeden Account zum Admin machen/Admin-Status entfernen oder komplett löschen, jede der drei Aktionen mit zweistufiger Bestätigung (Schritt 1: Ja/Abbrechen, Schritt 2: exakte E-Mail eintippen, Confirm-Button bis dahin disabled). Serverseitig in `app/actions/profiles.ts`: kein Self-Target möglich, letzter verbleibender Admin kann nicht demoted/gelöscht werden (`assertNotSelf`/`assertNotLastAdmin`).

   **Hardcoded Haupt-Admin** (`lib/admin.ts`, Migration `20260715000004_hardcode_primary_admin.sql`) — ✅ umgesetzt, per Browser-Test verifiziert: `uhllucas@icloud.com` ist fest als Admin hinterlegt und kann über die UI nie demoted/gelöscht werden (`assertNotPrimaryAdmin`, UI zeigt „Haupt-Admin"-Badge statt Action-Buttons). Details s. „Auth & Berechtigungen".
2. **Preis-Rating auf 0–4 erweitern** — ✅ umgesetzt (0 = kostenlos), noch nicht per Browser-Test verifiziert
   `PriceLevel` in `types/database.ts` ist jetzt `0|1|2|3|4`. Migration `20260715000005_price_level_zero.sql` (gepusht) lockert den DB-Constraint von `between 1 and 4` auf `between 0 and 4`. `PriceLevelDots.tsx` und `PriceBadge` (`AdminDashboard.tsx`) rendern `0` jetzt als „Kostenlos" statt es wie `null` zu behandeln. `PRICE_OPTIONS`/`PRICE_CHIPS` (Admin-Formular, `FilterBar`, `SearchResultsView`) haben einen neuen „Kostenlos"-Eintrag. Dabei mehrere truthy-Checks auf `price_level` gefunden und gefixt, die `0` fälschlich wie „kein Filter" behandelt hätten (`getRestaurants` in `restaurants.ts`, URL-Aufbau in `SearchResultsView.tsx`) — s. Hinweis bei `PriceLevelDots.tsx` oben.
3. **Bilder nur noch aus Google Maps referenzieren** — ✅ umgesetzt, noch nicht per Browser-Test verifiziert
   `app/actions/storage.ts` (nie an eine UI angebunden) entfernt, ebenso alle Zugriffs-Policies auf den `restaurant-images`-Bucket (Migration `20260715000006`) — der Bucket selbst bleibt als leere, policy-lose (= für niemanden außer dem Service-Role-Key erreichbare) Restleiche in Supabase bestehen, da `storage.objects`/`storage.buckets` nicht per direktem SQL `DELETE` änderbar sind (Supabase erzwingt dafür die Storage API, SQLSTATE 42501); bei Bedarf manuell im Dashboard löschen. Restaurant-Detailseite zeigt genau die ersten 3 `photoUris` aus `places.ts` (1 großes Hero-Bild + 2 Thumbnails, vorher waren es bis zu 4).
4. **Nutzer-Bewertung: Sterne 0–5 + 150-Zeichen-Kommentar + Durchschnittsanzeige** — ✅ umgesetzt, noch nicht per Browser-Test verifiziert
   `comments.secondary_rating`-Check-Constraint auf `between 0 and 5` erweitert (Migration `20260715000006`), `addComment`/`updateComment` validieren zusätzlich `content.length <= 150`. `CommentForm.tsx`: eigener „0"-Chip links neben den 5 Sternen (da 0 jetzt ein gültiger, von „nichts gewählt" verschiedener Wert ist, analog zum `price_level`-0-Muster), Live-Zeichenzähler. Durchschnitt über `computeAverageRating()` (`lib/ratings.ts`) aus den bereits geladenen `restaurant.comments`, visualisiert über `<StarRating>` (Halb-Stern-Füllung) neben der „Reader Experiences"-Überschrift.
5. **Unterkategorien-Bewertung** (Service / Location / Geschmack / Preis-Leistung, je 0–5, Überschrift + Absatz) — ✅ umgesetzt, noch nicht per Browser-Test verifiziert
   Neue Tabellen `restaurant_reviews` (ein Aufenthalt: Datum, Spoon-Rating, Fazit — ersetzt `restaurants.official_review`) und `restaurant_review_categories` (bis zu 4 optionale Zeilen je Aufenthalt, unique je `(review_id, category)`), s. Schema oben. `restaurants.spoon_rating` bleibt als schnell filterbare Spalte erhalten, wird aber per Trigger `sync_restaurant_spoon_rating` immer aus dem aktuellsten Aufenthalt abgeleitet statt direkt editiert. Kategorien sind im Admin-Editor unabhängige, optionale Sektionen (Überschrift + Text + `RatingDots`-Picker), auf der Detailseite als eigene Karten unterhalb des Fazits gerendert (nur befüllte Kategorien).
6. **Mehrfach-Aufenthalte: ausklappbare „Letzter Besuch"-Sektion** — ✅ umgesetzt, noch nicht per Browser-Test verifiziert
   Baut auf Schritt 5 auf: `getRestaurantById` liefert alle `restaurant_reviews` eines Restaurants absteigend nach `visited_at`/`created_at` sortiert; `reviews[0]` = aktueller Aufenthalt (Fazit + Kategorien direkt auf der Seite), `reviews.slice(1)` = Historie. Nur wenn mehr als ein Aufenthalt existiert, erscheint vor dem „Reader Experiences"-Trenner eine Sektion „Vorherige Aufenthalte" mit je einem nativen `<details>`-Element pro altem Aufenthalt (`<summary>`: Datum + Spoon-Emoji, ausgeklappt: volles damaliges Fazit + Kategorien über dieselbe `ReviewContent`-Render-Hilfsfunktion wie beim aktuellen Aufenthalt). Admin-seitig entscheidet die Checkbox „Als neuen Aufenthalt speichern" im Edit-Panel, ob eine Änderung den aktuellen Aufenthalt korrigiert (`updateReview`) oder einen neuen historischen Eintrag anlegt (`createReview`).

   Migration für Schritte 3–6 (lokal erstellt, **noch nicht gepusht** — `supabase db push` steht noch aus): `20260715000006_reviews_and_ratings.sql`.
7. **Admin/Editor: CSV-Import + Entwurf/Veröffentlicht-Status** — ✅ umgesetzt, noch nicht per Browser-Test verifiziert
   Bestehende Editieren/Löschen-Funktionalität war bereits vorhanden (`updateRestaurant`/`deleteRestaurant`, Edit-Panel + Delete-Modal im Admin-Dashboard). Neu:
   - **CSV-Import** (`app/actions/csvImport.ts`, `ImportModal` in `AdminDashboard.tsx`): Import einer Google-Takeout-„Gespeicherte Orte"-Liste, gleicht per `google_place_id` (aus der `URL`-Spalte extrahiert) bzw. Namen gegen bestehende Restaurants ab, zeigt eine Vorschau (Neu/Bereits vorhanden) und importiert die vom Admin bestätigte Auswahl als `draft`-Restaurants mit Platzhalter-Erstaufenthalt.
   - **`restaurants.status`** (`'draft'|'published'`, Migration `20260715000007_restaurant_status.sql`): Admins können jeden Eintrag (neu oder bestehend) über eine Checkbox im Edit-Panel auf „Entwurf" stellen — per RLS für normale Nutzer dann komplett unsichtbar, im Dashboard weiterhin voll sichtbar (Badge + „Nur Entwürfe"-Filter). Macht CSV-importierte Rohzeilen automatisch privat, bis ein Admin sie manuell vervollständigt und veröffentlicht.

   Migration: `20260715000007_restaurant_status.sql` (lokal erstellt, **noch nicht gepusht** — zusammen mit `20260715000006_reviews_and_ratings.sql` per `supabase db push` ausstehend).
8. **PWA-Deployment (installierbare Web-App für Mobile)** — ✅ umgesetzt (Grundausstattung 2026-07-16, Service Worker 2026-07-18), noch nicht per Browser-Test verifiziert
   `app/manifest.ts`, `app/icon.tsx`/`app/apple-icon.tsx`, `app/icons/192|512/route.tsx` (alle via `next/og` `ImageResponse`, s. `lib/pwa-icon.tsx`), `appleWebApp`-Metadata in `app/layout.tsx`, `<InstallPwaInstructions>` (iOS/Android-Anleitung) unten auf der Landing-Page. `utils/supabase/proxy.ts`s `PUBLIC_PATHS` musste um die neuen Manifest-/Icon-Routen erweitert werden, sonst hätte der Auth-Gate sie (inkl. Favicon auf `/login`) zu `/login` redirected. **Service Worker** (`public/sw.js`, `components/ServiceWorkerRegister.tsx`, `app/offline/`) ergänzt seit Schritt 22 einen Offline-Fallback + Cache-first-Caching der eigenen PWA-Icons — s. Schritt 22 für Details.
9. **Diverse UX-Verbesserungen** (2026-07-16) — ✅ umgesetzt, noch nicht per Browser-Test verifiziert
   - **Adresse persistiert** + **manuelle Restaurant-Erfassung**: `restaurants.address` (Migration `20260716000001_restaurant_address.sql`, **noch nicht gepusht** — `supabase db push` steht aus), Fallback-Texteingabe im Admin-Edit-Panel für Orte, die Google Maps nicht kennt (s. „Manuelle Restaurant-Erfassung" oben).
   - **Karten-Dark-Mode**: zweite Map-ID (`NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID_DARK`) + `public/map-style-dark.json`, s. „Karten-Dark-Mode" oben. Erfordert einen einmaligen manuellen Schritt in der Google Cloud Console, um sichtbar zu werden.
   - **Zurück-Button**: `components/BackButton.tsx` (`router.back()` mit Fallback-Link), auf der Restaurant-Detailseite und im Admin-Dashboard.
   - **Keine Emojis außer Spoon-Rating**: neues `components/icons.tsx`, alle dekorativen Emojis app-weit ersetzt (s. „Wichtige Muster" oben).
   - **Neue Spoon-Rating-Farbcodierung**: Gold (bester) → Flieder → Altrosa/Mauve → Grau (`--c-lilac`/`--c-mauve`-Tokens in `app/globals.css`, s. `lib/ratings.ts`).
   - **Admin-Tabelle**: Name verlinkt direkt zur öffentlichen Seite, Edit/Delete-Buttons permanent sichtbar (vorher nur bei Hover — auf Mobile nie erreichbar), Nutzerverwaltung standardmäßig eingeklappt.
   - **Suchergebnisliste**: „Bewertung"-Filterzeile war durch `marginLeft: auto` uneinheitlich eingerückt — jetzt wie Küche/Preis eine eigene, linksbündige Zeile. Trefferzeilen sind jetzt aufklappbar (Live-Öffnungszeiten + „Zur Restaurant-Seite"/`NavigateButton`) statt direkt zu navigieren.
10. **Weitere UX-Runde** (2026-07-16) — ✅ umgesetzt, noch nicht per Browser-Test verifiziert
    - **Kategorie-Karten einspaltig**: sowohl auf der Restaurant-Detailseite als auch im Admin-Edit-Panel stehen die 4 Kategorie-Karten jetzt untereinander statt im 2×2-/auto-fit-Grid — die App ist für längere Fließtexte je Kategorie gedacht, dafür war das Grid zu eng.
    - **Fotovorschau auf 3 Bilder begrenzt**: sowohl `getPlaceDetails()` (`app/actions/places.ts`, holt jetzt nur noch bis zu 3 statt 5 Foto-URIs von Google) als auch das Admin-Edit-Panel (`placePhotos.slice(0, 3)`) — deckungsgleich mit der Detailseite, die ohnehin nie mehr als 3 zeigte.
    - **🫗 als Fehler-/Leerzustand-Emoji wieder eingeführt**: `app/error.tsx`, „Keine Restaurants gefunden" in `app/page.tsx` und `SearchResultsView.tsx` zeigen wieder das Spoon-Rating-Glas (`SPOON_RATINGS[0].emoji`) statt `IconEmptyState` — war bei der „Keine Emojis"-Bereinigung versehentlich mitentfernt worden, obwohl Spoon-Rating-Glyphen explizit die erlaubte Ausnahme sind.
    - **Telefonnummer + Website**: neue Spalten `restaurants.phone`/`restaurants.website` (Migration `20260716000002_restaurant_contact_info.sql`, **noch nicht gepusht** — `supabase db push` steht aus), analog zu `address` standardmäßig live von Google Places bezogen (`getPlaceDetails()` liefert jetzt zusätzlich `phone`/`website`) mit persistiertem Fallback. Restaurant-Detailseite zeigt „Anrufen"/„Website"-Buttons neben `<NavigateButton>`.
    - **Manuelle Öffnungszeiten**: neue Spalte `restaurants.opening_hours` (rein manueller Freitext, dieselbe Migration) als Fallback, wenn Google keine Live-Öffnungszeiten liefert.
    - **„Kontakt & Öffnungszeiten"-Abschnitt im Admin-Edit-Panel**: Toggle-Chips für Telefon/Website/Öffnungszeiten (State `visibleContactFields`) — blendet die jeweiligen Felder nur ein, wenn relevant; automatisch aktiviert, sobald Google beim Places-Lookup einen Wert liefert. Funktioniert identisch im Google-Such- und im manuellen Erfassungs-Modus, macht damit „Ort manuell erfassen" gezielt erweiterbar um genau die Zusatzfelder, die ein Eintrag tatsächlich hat.
    - **Cuisine-Eingabe als echte Combobox**: neue `CuisineCombobox`-Komponente ersetzt das native `<input list>` (dessen Browser-eigener Dropdown-Pfeil/Filterverhalten uneinheitlich war). Vorschläge kommen jetzt aus `getCuisines()` (tatsächlich bereits verwendete Küchen, als `cuisineSuggestions`-Prop von `app/admin/dashboard/page.tsx` durchgereicht) statt einer hartkodierten Liste, gefiltert per Substring-Match beim Tippen.
    - **Mehrfachauswahl + Bulk-Löschen im Admin-Dashboard**: Checkbox-Spalte in der Restaurant-Tabelle + Auswahl-Leiste, neue Server Action `deleteRestaurants(ids)` (`app/actions/restaurants.ts`); `DeleteModal` ist jetzt auf `restaurants: Restaurant[]` verallgemeinert (1 vs. mehrere).
    - **Dark-Mode-Kartenstil geprüft**: Code/Env-Var-Setup (`MapView.tsx`, `NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID_DARK`) ist korrekt und folgt dem dokumentierten Zwei-Map-ID-Muster — das Problem lag nicht im Code. Ursache ist ein noch fehlender manueller Schritt in der Google Cloud Console (neuer Stil nur mit der hellen Map-ID verknüpft, nicht mit der dunklen), s. „Karten-Dark-Mode" oben.

    Migration: `20260716000002_restaurant_contact_info.sql` (lokal erstellt, **noch nicht gepusht**, zusammen mit den bereits offenen Migrationen aus Schritt 5/6/9 per `supabase db push`).
11. **Account-Verwaltung + weitere UX-Fixes** (2026-07-16) — ✅ umgesetzt, noch nicht per Browser-Test verifiziert
    - **Passwort vergessen/ändern über Supabase Auth** (s. „Auth & Berechtigungen" oben): neue Route `app/auth/reset-password/` + Server Actions `requestPasswordReset`/`updatePassword` (`app/actions/auth.ts`). Login-Seite hat einen „Passwort vergessen?"-Link; eingeloggte Nutzer finden „Passwort ändern" im neuen `<UserMenu>`-Kebab-Menü im Header. Beide lösen denselben E-Mail-Link-Flow aus (kein inline Alt-/Neu-Passwort-Formular).
    - **Header-Kebab-Menü** (`components/UserMenu.tsx`): „Passwort ändern" + „Abmelden" stehen nicht mehr als eigene Buttons im Header, sondern hinter einem Drei-Punkte-Icon (`IconDotsVertical`), das bei Klick außerhalb automatisch schließt — hält den Header auf schmalen Viewports schlank.
    - **„Auswahl"-Sektion auf der Landing-Page**: neue Spalte `restaurants.featured` (Migration `20260716000003_restaurant_featured.sql`, **noch nicht gepusht**), `getFeaturedRestaurants()`/`setFeatured()` (`app/actions/restaurants.ts`), Stern-Toggle (`IconStar`) in der Admin-Tabelle. `app/page.tsx` zeigt die Reihe oberhalb von „Neu hinzugefügt", nur wenn Einträge markiert sind.
    - **CSV-Import: Spoon-Rating für alle neuen Zeilen wählbar**: `confirmCsvImport(selection, bulkSpoonRating?)` — Chip-Picker im `ImportModal`-Preview-Schritt, praktisch bei CSV-Listen, die bereits einer einzigen Bewertungsstufe entsprechen. Dabei auch einen Bug gefunden und gefixt: der zurückgegebene Datensatz trug noch das Default-Rating (vor dem `sync_restaurant_spoon_rating`-Trigger), wodurch die Tabelle direkt nach dem Import kurz das falsche Emoji zeigte, bis ein Reload die korrekten Daten nachlud — jetzt wird der Rückgabewert in-memory korrigiert (dieselbe Technik wie in `createRestaurant`).
    - **Suchergebnis-Akkordeon**: in `SearchResultsView.tsx` ist immer nur eine Trefferzeile gleichzeitig aufgeklappt — der Expand-State wurde von `ResultCard` (lokal) in die Elternkomponente (`expandedId`) verschoben.
    - **Preis-Level auf der Restaurant-Detailseite**: stand vorher klein in der Meta-Zeile neben Adresse/Öffnungszeiten und ging dort unter; steht jetzt groß/fett direkt gegenüber dem Cuisine-Label in derselben Zeile.
    - **Kein horizontales Scrollen**: `overflow-x: hidden` + `max-width: 100vw` auf `html, body` (`app/globals.css`) als generelles Sicherheitsnetz, plus `overflow-wrap: break-word` und `img, video { max-width: 100% }`.

    Migration: `20260716000003_restaurant_featured.sql` (lokal erstellt, **noch nicht gepusht**, zusammen mit den bereits offenen Migrationen aus Schritt 5/6/9/10 per `supabase db push`).
12. **Ladeindikatoren, CSV-Import-Autofill, Site-Audit (Fehler/Performance/Security)** (2026-07-16) — ✅ umgesetzt, noch nicht per Browser-Test verifiziert. Keine Schema-Änderung, keine neue Migration.
    - **Ladeindikatoren für Aktionen mit spürbarer Wartezeit**: neue `.gp-spinner-sm`-CSS-Klasse (`app/globals.css`) für kleine Inline-Spinner in Buttons/Feldern (Ergänzung zum großen `.gp-spinner` aus `loading.tsx`). `LocationSearch.tsx` zeigt jetzt einen `resolving`-State während der Google-Places/Geocoder-Auflösung vor dem `router.push` (s. Eintrag oben). `DeleteCommentButton.tsx` (neu) gibt dem Kommentar-Löschen-Button per `useFormStatus()` ein „Löscht…"-Feedback. Registrierungs-Freischalten/Ablehnen-Buttons zeigen „…" während `busyId` aktiv ist. Die meisten anderen Aktionen hatten bereits einen Pending-State (`useTransition`/`useActionState`) oder werden implizit von Next.js' routenweitem `loading.tsx` abgedeckt — geprüft, aber nicht verändert.
    - **CSV-Import befüllt jetzt auch Adresse + Cuisine automatisch**: `resolvePlaceForImport()` (`app/actions/places.ts`) fragt zusätzlich `formattedAddress`/`primaryTypeDisplayName`/`primaryType`/`types` ab und liefert `address`/`cuisine` zurück (per neuem, aus `PlacesAutocomplete.tsx` extrahiertem `guessCuisine()` in `lib/cuisine.ts`); `confirmCsvImport()` persistiert beide Felder jetzt mit. Vorher bekamen importierte Entwürfe nur `google_place_id`/`lat`/`lng` automatisch, Adresse/Cuisine mussten immer manuell im Edit-Panel nachgetragen werden.
    - **Live-Öffnungszeiten-Hinweis im Admin-Edit-Panel**: neuer `hasLiveOpeningHours`-State (aus demselben `getPlaceDetails()`-Aufruf wie die Fotovorschau) zeigt über den „Kontakt & Öffnungszeiten"-Toggle-Chips einen grünen Haken, wenn Google für den gewählten Ort bereits Live-Öffnungszeiten liefert — vorher gab es dafür überhaupt keine Rückmeldung im Formular, obwohl `getPlaceDetails()` diese Information längst mitlieferte.
    - **Bugfix — Entwürfe waren für Admins außerhalb des Admin-Dashboards sichtbar**: `getRestaurants()`/`getRecentRestaurants()`/`getFeaturedRestaurants()`/`getCuisines()` (`app/actions/restaurants.ts`) verließen sich für „keine Entwürfe in Suche/„Neu hinzugefügt"/„Auswahl"" bisher rein auf RLS — die aber `is_admin()` vollen SELECT-Zugriff gibt. Ein Admin, der ganz normal (nicht im `/admin/dashboard`) die Startseite oder Suche besuchte, sah dadurch Entwürfe zwischen den echten Einträgen. Jetzt filtern alle vier explizit `.eq("status", "published")`; `getRestaurantById()` bleibt bewusst ungefiltert (Admins müssen Entwürfe über den Tabellen-Link weiterhin direkt aufrufen können).
    - **Bugfix — Karte reagierte nicht auf eine neue Suche aus der Ergebnisliste heraus**: `MapView.tsx`s `<Map>` nutzt unkontrolliertes `defaultCenter` (Absicht, s. Eintrag zu `MapView.tsx` oben) — dessen `key` bezog bisher nur die Map-ID (Light/Dark) ein, nicht die Center-Koordinaten. Eine neue Suche über die Suchleiste innerhalb von `SearchResultsView` aktualisierte dadurch die Trefferliste, ließ die Karte aber auf dem alten Ort stehen. `key` kombiniert jetzt Map-ID **und** Center-Koordinaten.
    - **Security-Audit**: alle Server Actions (`app/actions/*.ts`), `proxy.ts`/`auth-helpers.ts` und der Send-Email-Webhook auf fehlende Auth-/Ownership-Checks geprüft — keine weiteren Lücken über die beiden Bugfixes oben hinaus gefunden (RLS + `requireAuth`/`requireApproved`/`requireAdmin`-Muster durchgängig korrekt angewendet). Zwei konkrete Funde behoben: (1) der „Maps ↗"-Link im CSV-Import-Preview rendert `mapsUrl` jetzt nur noch bei `http`/`https`-Schema (`sanitizeMapsUrl()` in `csvImport.ts`) — vorher hätte eine präparierte CSV-Datei eine `javascript:`-URI in einen echten Link im Admin-Dashboard schmuggeln können; (2) `next.config.ts` setzt jetzt Baseline-HTTP-Security-Header (`X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy` — s. „HTTP-Security-Header" oben), vorher gab es keine.
    - **Performance**: `attachEmails()` (`app/actions/profiles.ts`) nutzte einen `getUserById()`-Call pro Profil (N Supabase-Admin-API-Requests bei N Accounts auf jedem Dashboard-Load) — jetzt ein einzelner `listUsers({ perPage: 1000 })`-Call mit lokalem Lookup. `next/image` bewusst weiterhin nicht eingesetzt (s. `eslint-disable @next/next/no-img-element`-Kommentare an den drei `<img>`-Stellen) — Googles Foto-URIs sind signiert/kurzlebig (`cache: "no-store"`) und würden den Optimierungs-Cache ohnehin ständig umgehen.
13. **Google-APIs auf Deutsch + verbleibende englische UI-Texte übersetzt** (2026-07-16) — ✅ umgesetzt, noch nicht per Browser-Test verifiziert. Keine Schema-Änderung, keine neue Migration.
    - **Google Places/Maps durchgehend mit `languageCode=de&regionCode=DE`**: server-seitig in `getPlaceDetails()` war das schon gesetzt (deutsche Öffnungszeiten-Strings), `resolvePlaceForImport()` (CSV-Import) hatte es aber gefehlt — dadurch kam der Cuisine-Vorschlag (`primaryTypeDisplayName`) beim CSV-Import auf Englisch zurück. Jetzt bei beiden REST-Lookups (direkte ID-Auflösung als Query-Param, Textsuche `places:searchText` als POST-Body-Feld) gesetzt. Client-seitig bekommt jedes `<APIProvider>` (`LocationSearch.tsx`, `MapView.tsx`, `AdminDashboard.tsx`) jetzt `language="de" region="DE"` — betrifft die komplette darüber geladene Maps-JS-API-Session (Autocomplete-Vorschläge, Geocoder, `Place.fetchFields()` im Admin-Edit-Panel). S. „Wichtige Muster" oben.
    - **Verbleibende englische UI-Texte gefunden und übersetzt**: v.a. im Admin-Dashboard (`AdminDashboard.tsx`) — Slide-Over-Titel/Buttons („Add Restaurant"/„Edit Restaurant"/„Save Changes"/„Saving…" → „Restaurant hinzufügen"/„Restaurant bearbeiten"/„Änderungen speichern"/„Speichert…"), Lösch-Dialog („Delete restaurant?"/„Keep it"/„Yes, delete"/„Deleting…" → „Restaurant löschen?"/„Behalten"/„Ja, löschen"/„Löscht…"), Tabellen-Header („Cuisine"/„Price"/„Rating"/„Place ID" → „Küche"/„Preis"/„Bewertung"/„Platz-ID"), Toasts („Restaurant added"/„Changes saved"/„Restaurant deleted"), Buttons/Placeholder („Edit"/„Delete"/„Add new"/„Cancel"/„Search…"/„Search establishment…"/„Find on Google Maps"/„No place ID"/„Latitude"/„Longitude"), Leerzustände („No restaurants match your search."/„No restaurants yet. Add one!"). Außerdem `MapView.tsx`s InfoWindow-Link („View details →" → „Details ansehen →") und die „Reader Experiences"-Überschrift auf der Restaurant-Detailseite (→ „Leser-Erfahrungen"). **Bewusst NICHT übersetzt**: die Spoon-Rating-Labels (`SPOON_RATINGS[...].label`/`labelShort` in `lib/ratings.ts`, „Absolute Recommendation"/„Worth Mentioning"/„Remembering"/„Not Recommended") — auf ausdrücklichen Wunsch weiterhin Englisch, sichtbar u.a. im Verdict-Badge auf der Restaurant-Detailseite, `RestaurantCard`, `SearchResultsView` und den Filter-Chip-Tooltips.
14. **„Standort verwenden" + Sortierung nach Entfernung/Preis/Bewertung** (2026-07-16) — ✅ umgesetzt, per Browser-Test (Playwright, headless Chromium mit gemocktem `geolocation`-Context) verifiziert: Login, Klick auf „Standort verwenden" (Hero **und** Suchleiste innerhalb von `SearchResultsView`), Navigation mit `own_location=1`, „Dein Standort"-Marker auf der Karte, Default-Sortierung „Entfernung", Umschalten der Sortier-Chips, Desktop- + Mobile-Viewport, Listen- und Kartenansicht. Keine Schema-Änderung, keine neue Migration.
    - **„Standort verwenden"-Button** (`components/LocationSearch.tsx`, `IconLocate` aus `components/icons.tsx`): geolokalisiert das Gerät per `navigator.geolocation.getCurrentPosition()` und navigiert direkt (kein Places-Autocomplete nötig) — ein `google.maps.Geocoder`-Reverse-Lookup liefert nur ein lesbares Label für die Query-Bar, schlägt aber nie den eigentlichen Suchvorgang fehl (Fallback „Mein Standort"). Neuer URL-Param `own_location=1` markiert die Suche als „von der eigenen Geräteposition ausgelöst" (im Gegensatz zu einer getippten/ausgewählten Adresse). In `app/page.tsx`s `SearchParams`-Typ ergänzt, als `ownLocation`-Prop an `SearchResultsView` durchgereicht.
    - **`next.config.ts`**: `Permissions-Policy` erlaubt jetzt `geolocation=(self)` (vorher blanket `geolocation=()`) — ohne diese Änderung hätte der neue Button nie funktioniert, unabhängig von der Browser-Berechtigung des Nutzers (stiller „Permissions policy violation"-Fehler statt eines Berechtigungs-Dialogs). Beim ersten Playwright-Testlauf genau so aufgefallen und gefixt.
    - **Sortierung** (`SearchResultsView.tsx`, neuer `SortMode`-Typ): „Sortieren"-Chip-Zeile (Standard/Entfernung/Preis/Bewertung) unter der Trefferzahl, wirkt sofort ohne „Übernehmen" (reines clientseitiges Re-Sortieren via `useMemo`, kein Refetch). „Entfernung" nutzt `haversineDistanceKm()` (neu: `lib/geo.ts`) gegen `center`; Restaurants ohne `lat`/`lng` landen ans Ende. Default „Entfernung" bei `ownLocation=true`, sonst „Standard" (Server-Reihenfolge).
    - **„Dein Standort"-Marker** (`components/map/MapView.tsx`): neuer optionaler `myLocation`-Prop rendert einen zusätzlichen `AdvancedMarker` (fixes Google-Blau statt `SPOON_RATING_COLORS`, s. Eintrag zu `MapView.tsx` oben) — nur gesetzt, wenn `ownLocation=true`, nicht bei einer beliebigen getippten Ortssuche.
15. **Bugfix — „Standort verwenden" lieferte oft 0 Treffer** (2026-07-17) — ⚠️ Zwischenschritt, **durch Schritt 16 abgelöst/generalisiert** (die dort eingeführte Unterscheidung `own_location=1` vs. getippte Suche existiert im Code nicht mehr — beide werden inzwischen identisch behandelt). Ursprünglicher Fund bleibt als Kontext dokumentiert: `app/page.tsx` wandte die in Schritt 14 eingeführte ±0.06°-Delta-Bbox bei `own_location=1` als strikten DB-Filter an — sobald kein Restaurant des kuratierten Bestands innerhalb von ~6-7km um den exakten GPS-Punkt lag, kam eine leere Trefferliste zurück. Ein Zwischen-Fix skippte den Bounds-Filter zunächst nur für `own_location=1`; getippte Ortssuchen filterten weiterhin per Google-Viewport.
16. **Bugfix — Ortssuche (getippt/ausgewählt, z.B. „Sächsische Straße, Berlin") lieferte oft 0 Treffer, obwohl die Karte einen ausreichend großen Bereich zeigte** (2026-07-17) — ✅ umgesetzt, noch nicht per Browser-Test verifiziert. Keine Schema-Änderung, keine neue Migration.
    - **Ursache**: derselbe strukturelle Fehler wie Schritt 15, nur für die normale getippte/ausgewählte Ortssuche statt „Standort verwenden". `getRestaurants()` (`app/actions/restaurants.ts`) filterte hart auf eine Bounding-Box (`filters.bounds`), die 1:1 aus Googles `place.viewport` übernommen wurde (`LocationSearch.tsx`). Für eine Stadt („Berlin") ist dieser Viewport groß genug, für eine einzelne Straße/Adresse aber oft nur ein paar hundert Meter breit — Restaurants, die klar „in der Nähe" liegen, fielen raus. Ein erster Zwischen-Fix (Mindestbreite ~6-7km um den Viewport-Mittelpunkt) reichte nicht aus: die Karte rendert unabhängig von der Bbox immer bei festem `zoom={11}` (`SearchResultsView.tsx`) — der sichtbare Kartenausschnitt war dadurch optisch oft größer als die tatsächlich verwendete (selbst aufgeweitete) Filter-Box, was den Eindruck erweckte, ein Treffer „müsste" erscheinen, obwohl die Bbox ihn weiterhin ausschloss.
    - **Fix**: das Bounds-Filtering wurde komplett entfernt, nicht nur aufgeweitet — `getRestaurants()`/`RestaurantFilters` kennen kein `bounds`/`LocationBounds` mehr, `app/page.tsx` setzt keinen Bbox-Filter mehr (für keine Art der Ortssuche), `LocationSearch.tsx`s `navigateToLocation()` hat keine Mindestbreiten-Logik mehr (war nur ein Workaround für den jetzt entfernten Filter). Jede Ortssuche lädt jetzt alle (durch Küche/Preis/Bewertung ggf. eingeschränkten) veröffentlichten Restaurants; der gesuchte Ort dient nur noch als Karten-Center und Eingabe für die bestehende clientseitige Distanz-Sortierung (`SearchResultsView.tsx`, `haversineDistanceKm()`) — exakt dieselbe Philosophie wie bei „Standort verwenden" (Schritt 15), jetzt konsequent für **jede** Ortssuche angewendet, da diese App einen kleinen, kuratierten Restaurant-Bestand hat (keine Performance-Sorge beim ungefilterten Laden) und ein Geo-Cutoff hier grundsätzlich mehr Probleme schafft als löst. `ne_lat`/`ne_lng`/`sw_lat`/`sw_lng` werden weiterhin in der URL mitgeführt (Round-Trip beim Ändern von Filtern/Sortierung in `SearchResultsView.tsx`), aber nirgends mehr als DB-Filter gelesen.
    - **Aber**: das komplette Weglassen jeder Eingrenzung ging zu weit — eine Adresse in Berlin darf zwar nicht durch einen zufällig zu engen Google-Viewport 0 Treffer liefern, aber ein Restaurant in München soll bei einer Berlin-Suche trotzdem nicht auftauchen. Direkt im Anschluss durch Schritt 17 korrigiert.
17. **Radius-Cutoff (distanzbasiert statt viewport-basiert) + Luftliniendistanz je Treffer + Adresse beim Aufklappen** (2026-07-17) — ✅ umgesetzt, per Browser-Test (Playwright, gegen den echten kuratierten Datenbestand — ein Restaurant in Berlin, eines in München) verifiziert. Keine Schema-Änderung, keine neue Migration.
    - **Hintergrund**: Schritt 16 entfernte jede geografische Eingrenzung komplett, was zu weit ging — eine Berlin-Suche darf keine Münchener Restaurants zeigen. Der eigentliche Fehler war nie „es sollte gar keine Eingrenzung geben", sondern „die Eingrenzung darf nicht von Googles je nach Ortstyp (Stadt vs. einzelne Straße) völlig unterschiedlich großem Viewport abhängen".
    - **Fix**: `app/page.tsx` filtert im `isLocationSearch`-Zweig jetzt per `haversineDistanceKm()` (`lib/geo.ts`) — Restaurants ohne `lat`/`lng` oder weiter als `MAX_SEARCH_RADIUS_KM = 30` (km, Luftlinie) vom Suchzentrum entfernt werden aus der an `SearchResultsView` übergebenen Liste rausgefiltert, bevor sie überhaupt beim Client ankommen. Der Radius ist bewusst fix und unabhängig vom gesuchten Ortstyp (Straße, Stadt, GPS via „Standort verwenden") — löst damit das Kernproblem aus Schritt 15/16 endgültig, ohne auf Googles Viewport-Größe angewiesen zu sein. Per Playwright gegen die echte DB verifiziert: Suche nach „Sächsische Str., Berlin" **und** nach „Berlin" (breite Stadtsuche) zeigen beide das Berliner Restaurant, nicht das Münchener; Suche nach „Marienplatz, München" zeigt umgekehrt nur das Münchener Restaurant; „Standort verwenden" (GPS-Mock in Berlin-Mitte) ebenso.
    - **Luftliniendistanz je Trefferzeile**: `ResultCard` (`SearchResultsView.tsx`) zeigt jetzt unter Spoon-Emoji/Preis die Distanz zum Suchzentrum (`formatDistanceKm()`, z.B. „8,7 km" oder „850 m") — vorher war die Distanz nur für die „Entfernung"-Sortierung intern verwendet, aber nirgends sichtbar.
    - **Adresse beim Aufklappen**: der bestehende lazy-`getPlaceDetails()`-Aufruf beim Aufklappen einer Trefferzeile (bisher nur für Öffnungszeiten) liefert jetzt zusätzlich `formattedAddress`; die Adresse steht über den Öffnungszeiten im aufgeklappten Panel, mit Fallback auf `restaurant.address`, falls kein `google_place_id` vorhanden ist oder der Live-Lookup fehlschlägt (identisches Live-mit-Fallback-Muster wie auf der Restaurant-Detailseite).
18. **Google-Places-Text-Infos persistieren statt live abrufen** (2026-07-17) — ✅ umgesetzt, noch nicht per Browser-Test verifiziert. Migration `20260717000001_google_opening_hours.sql` (gepusht).
    - **Hintergrund**: Adresse/Telefon/Website/Öffnungszeiten wurden bei **jedem** öffentlichen Seitenaufruf live von der Google Places API nachgeladen (Restaurant-Detailseite bei jedem Besuch, `SearchResultsView`s Trefferzeile bei jedem Aufklappen, s. Schritt 17), obwohl `address`/`phone`/`website` bereits als DB-Spalten existierten — der Live-Wert hatte aber immer Vorrang (`placeDetails?.phone || restaurant.phone`). Unnötig viele Places-Requests für Infos, die sich kaum ändern.
    - **Neue Spalte** `restaurants.google_opening_hours` (`text[]`, Googles `weekdayDescriptions`) — rein Google-abgeleitet, nicht admin-editierbar (im Gegensatz zum weiterhin manuellen `opening_hours`-Fallback). `address`/`phone`/`website` brauchten keine neue Spalte, nur eine geänderte Präferenz.
    - **Neuer Helper** `lib/openingHours.ts`s `getOpeningStatus(weekdayDescriptions, now?)` (ursprünglich `isOpenNow()`, seither um die Schließzeit erweitert) berechnet „Jetzt geöffnet"/„Geschlossen" lokal aus dem gespeicherten Wochenplan-Text (Regex-Parsing der Zeit-Ranges) statt aus Googles Live-`openNow`-Feld — inhaltlich äquivalent, da Google dieses Feld selbst nur aus derselben Wochenschablone ableitet (Feiertags-Sonderöffnungszeiten wurden auch vorher nicht berücksichtigt, da die App `regularOpeningHours` statt `currentOpeningHours` nutzt).
    - **Restaurant-Detailseite + `SearchResultsView.tsx`**: lesen Adresse/Telefon/Website/Öffnungszeiten jetzt aus `restaurant.*` statt aus einem Live-`getPlaceDetails()`-Aufruf. `SearchResultsView`s `ResultCard` hat dadurch **keinen** Places-API-Call mehr beim Aufklappen einer Trefferzeile (vorher: 1 Call pro Aufklappen, s. Schritt 17) — der `hoursLoading`/`liveAddress`-State + zugehöriger `useEffect` wurden komplett entfernt. Die Detailseite ruft `getPlaceDetails()` weiterhin auf, aber **nur noch für Fotos** (einzige Info, die nie persistiert wird, da Googles Foto-URIs signiert/kurzlebig sind).
    - **Befüllt wird nur noch, wo ein Admin aktiv mit Google interagiert**: Places-Autocomplete-Auswahl im Edit-Panel (bestehender Effekt schreibt jetzt zusätzlich `google_opening_hours` ins Formular), CSV-Import (`resolvePlaceForImport()`s Feldmaske um `internationalPhoneNumber,websiteUri,regularOpeningHours` erweitert, `confirmCsvImport()` persistiert alle drei mit), und neu: **„Von Google synchronisieren"**-Button im Admin-Dashboard-Toolbar → `syncGooglePlaceData()` (`app/actions/restaurants.ts`), geht sequenziell alle Restaurants mit `google_place_id` durch und aktualisiert Adresse/Telefon/Website (nur wenn leer) + Öffnungszeiten (immer). Dient sowohl als einmaliges Backfill für Bestandsdaten als auch als jederzeit wiederholbarer manueller Refresh.
19. **6-Monate-Verfallsdatum für persistierte Google-Daten** (2026-07-17) — ✅ umgesetzt, noch nicht per Browser-Test verifiziert. Migration `20260717000002_google_synced_at.sql` (gepusht). Baut direkt auf Schritt 18 auf.
    - **Hintergrund**: Schritt 18 macht Adresse/Telefon/Website/Öffnungszeiten dauerhaft DB-gelesen — ohne einen Auffrischungs-Mechanismus wären diese Werte aber für immer auf dem einmal gespeicherten Stand eingefroren, selbst wenn sich z. B. eine Telefonnummer bei Google ändert.
    - **Neue Spalte** `restaurants.google_synced_at` (timestamptz) — wann eine Zeile zuletzt tatsächlich mit Google abgeglichen wurde. Wird von allen drei Schreibpfaden aus Schritt 18 mitgeschrieben: Admin-Edit-Panel-Effekt (bei erfolgreichem `getPlaceDetails()`-Fetch), `confirmCsvImport()` (CSV-Import), `syncGooglePlaceData()` (manueller Bulk-Sync).
    - **Neuer Helper** `lib/googleSync.ts`: `GOOGLE_DATA_STALE_DAYS = 180` (~6 Monate) + `isGoogleDataStale(syncedAt, now?)` — `null`/kein Zeitstempel gilt als sofort abgelaufen.
    - **Automatischer Refresh beim Seitenbesuch** (mit Nutzer abgestimmte Variante, statt nur eines Admin-Hinweises oder eines separaten Cron-Jobs): `refreshGooglePlaceDataIfStale()` (`app/actions/restaurants.ts`, neu) läuft in der Restaurant-Detailseite direkt nach dem ohnehin für Fotos nötigen `getPlaceDetails()`-Aufruf. Ist `google_synced_at` abgelaufen (oder nie gesetzt), werden Adresse/Telefon/Website/Öffnungszeiten aus genau dieser bereits geladenen Antwort in die DB zurückgeschrieben (kein zusätzlicher Places-Request) und `google_synced_at` neu gestempelt; sonst No-op. Läuft für **jeden** Besucher, nicht nur Admins — dafür über `createAdminClient()` (Service-Role), da normale Nutzer laut RLS keine `restaurants`-Zeile updaten dürfen; sicher, weil ausschließlich bereits server-seitig von Google abgerufene Felder geschrieben werden, kein user-editierbarer Input. Ein Schreibfehler bricht die Seite nicht ab (stiller Fallback aufs bisherige `restaurant`, nächster Besuch versucht es erneut).
    - `syncGooglePlaceData()` (Schritt 18) bleibt bewusst unverändert im Verhalten — die manuelle Admin-Aktion aktualisiert weiterhin **alle** Restaurants sofort, unabhängig vom Verfallsdatum (expliziter Admin-Wunsch überstimmt die automatische Staleness-Prüfung).
20. **Diverse UX-Fixes + Admin-Filter-Ausbau + E-Mail-Bestätigungspflicht** (2026-07-18) — ✅ umgesetzt, per Browser-Test (Playwright) verifiziert. Keine Schema-Änderung, keine neue Migration.
    - **Bugfix — fixierter „Karte anzeigen"-Button überdeckte den letzten Suchtreffer** (mobil **und** auf schmalen Desktop-Fenstern): Ursache war `SearchResultsView.tsx`s `.sr-outer` mit hartkodiertem `calc(100vh - 64px)` — der globale `<Header>` hat aber nur ein `minHeight: 64`, keine feste Höhe, und bricht bei Platzmangel (`flexWrap`) auf eine zweite Zeile um, wird dann höher. `.sr-outer` ragte dadurch unbemerkt über den Viewport hinaus. Neue `components/HeaderHeightObserver.tsx` (Client Component, in `app/layout.tsx` neben `<Header>` gemountet) misst die tatsächliche Header-Höhe per `ResizeObserver` und schreibt sie als `--header-height`-CSS-Variable; `.sr-outer` nutzt jetzt `calc(100vh - var(--header-height, 64px))`. Zusätzlich bekam `.sr-results-list` (`globals.css`) ein `padding-bottom` in Höhe des fixierten Buttons, damit sich die letzte Zeile immer vollständig darüber hinausscrollen lässt.
    - **Bugfix — Restaurant-Karten (Landing-Page-Reihen) sahen nach dem Hover-Hebeeffekt abgeschnitten aus**: die horizontal scrollbaren „Auswahl"/„Neu hinzugefügt"-Container hatten `overflowX: "auto"` ohne `paddingTop` — sobald eine Achse nicht `visible` ist, berechnet CSS die andere automatisch als `auto` mit, was den `translateY(-4px)`-Hover-Effekt + Schatten am oberen Zeilenrand clippte. Fix: `paddingTop` auf beiden Reihen-Containern (`app/page.tsx`).
    - **Google-Maps-Info-Fenster** (Klick auf einen Karten-Marker): größer + größere Schrift; folgt jetzt Light/Dark-Mode statt immer weiß zu sein (`app/globals.css` überschreibt Googles eigenes `.gm-style-iw-c`/`.gm-style-iw-d`-Chrome mit `var(--c-surface)`, Name-/Link-Farbe im Inhalt selbst nutzt `var(--c-ink)`/`var(--c-gold)` statt hartkodierter Hex-Werte); ein permanenter, unstyled-heller Scrollbar-Balken (Google setzt `overflow: scroll` statt `auto` auf den Inhaltscontainer, obwohl nie gescrollt werden muss) wird per `overflow: hidden !important` unterdrückt.
    - **Landing-Page zeigt nicht mehr das volle, unfilterte Restaurant-Grid** (früher immer alle veröffentlichten Restaurants unterhalb der Suchleiste, bei wachsendem Bestand unübersichtlich) — das Grid rendert jetzt nur noch bei aktivem Filter/Namenssuche (`hasFilters`). `getRecentRestaurants()` zeigt die **6 neuesten** Restaurants statt aller aus den letzten 30 Tagen (Default-`limit` 8→6, kein zeitlicher Cutoff mehr) — bei einer ruhigeren Phase des Guides wäre die Reihe sonst wochenlang leer gewesen.
    - **Suchergebnis-Sortierung**: „Standard" (Server-Reihenfolge) als Sortieroption entfernt — „Entfernung" ist jetzt immer die Vorauswahl, unabhängig von `ownLocation`, da jede Ortssuche ohnehin ein Suchzentrum hat.
    - **Öffnungszeiten-Status zeigt jetzt auch die Schließzeit**: `lib/openingHours.ts`s `isOpenNow()` → `getOpeningStatus()`, gibt `{ open, until }` statt eines reinen Booleans zurück; „Jetzt geöffnet" wird auf der Restaurant-Detailseite und in `SearchResultsView`s aufgeklappten Trefferzeilen als „Jetzt geöffnet, bis 22:30 Uhr" angezeigt (kein „bis" bei durchgehend 24h geöffnet).
    - **Neue `<CuisineFilterDropdown>`-Komponente** (`components/CuisineFilterDropdown.tsx`): durchsuchbares Mehrfachauswahl-Dropdown für Küche, ersetzt die Küche-Chip-Reihe überall (`FilterBar.tsx`/Landing-Page, `SearchResultsView.tsx`, Admin-Filter-Panel s.u.) — eine Chip-Reihe skaliert nicht mehr, sobald der Restaurant-Bestand Dutzende unterschiedlicher Küchen enthält.
    - **Admin-Dashboard-Filter-Panel grundlegend überarbeitet**: (1) immer sichtbar, kein Auf-/Zuklapp-Button mehr, auf Mobile per `flex-wrap` statt Abschneiden; (2) neuer Status-Filter „Alle"/„Nur Entwürfe"/„Nur veröffentlicht" (Chips) ersetzt den alten einzelnen „Nur Entwürfe"-Boolean-Toggle; (3) Küche-Filter zeigt jetzt **alle** Küchen aus **allen** geladenen Restaurants unabhängig vom Status (`allCuisines`, lokal aus dem `restaurants`-State berechnet) statt der `cuisineSuggestions`-Prop (= `getCuisines()`, die nur veröffentlichte Restaurants zählt — bei einem Bestand aus überwiegend Entwürfen blieben so fast alle Küchen unsichtbar); (4) die Suche durchsucht jetzt zusätzlich Adresse und das aktuelle Fazit, nicht nur Name/Küche — dafür lädt `app/admin/dashboard/page.tsx` beim initialen Laden zusätzlich `reviews:restaurant_reviews(fazit, visited_at, created_at)` und baut daraus eine `fazitById`-Map (aktuellstes Fazit je Restaurant), die als `initialFazitById`-Prop an `AdminDashboard` geht und dort bei jedem Speichern/CSV-Import lokal aktualisiert wird (kein Refetch nötig).
    - **Registrierungs-Freischaltung erfordert jetzt eine bestätigte E-Mail-Adresse**: `approveProfile()` prüft `auth.users.email_confirmed_at`, s. „Auth & Berechtigungen" oben.
    - Auch die „Nutzerverwaltung"-Liste und die „Registrierungen ausstehend"-Liste im Admin-Dashboard sind jetzt mobil per `flex-wrap` responsive (vorher liefen Aktions-Buttons auf schmalen Viewports über den Rand hinaus und waren nicht erreichbar).
21. **Footer-Platzhalter entfernt, Admin-Filter-Zähler + CSV-Export + „Erweiterte Funktionen"-Dialog** (2026-07-18) — ✅ umgesetzt, noch nicht per Browser-Test verifiziert (TypeScript-Build + ESLint sauber). Keine Schema-Änderung, keine neue Migration.
    - **Footer**: die vier reinen Platzhalter-Textlinks („Über uns"/„Städte"/„Küchen"/„Kontakt", `<span>`s ohne Funktion) in `components/Footer.tsx` entfernt — die Fußzeile zeigt jetzt nur noch Wortmarke links und Copyright rechts.
    - **Admin-Dashboard — Filter-Zähler**: die Kopfzeile über der Restaurant-Tabelle zeigt `„{gefiltert} von {gesamt} Einträgen"`, sobald Suche/Status/Küche/Preis/Bewertung die Liste tatsächlich einschränken (sonst weiterhin nur die Gesamtzahl) — macht auf einen Blick sichtbar, wie viele Einträge den aktuell gesetzten Filterbedingungen entsprechen.
    - **Admin-Dashboard — CSV-Export** (`handleExportCsv`, `AdminDashboard.tsx`): baut clientseitig aus dem bereits geladenen `restaurants`-State (**alle** Status — „gesamte Datenbank", nicht nur die gefilterte Teilmenge) + `fazitById` eine CSV-Datei und löst per `Blob`+`<a download>` einen Browser-Download aus (UTF-8-BOM für korrekte Umlaute in Excel). Kein neuer Server-Request nötig, da das Dashboard beim initialen Laden ohnehin jedes Restaurant unabhängig vom Status lädt.
    - **Admin-Dashboard — Toolbar auf „Erweiterte Funktionen" + „Neu hinzufügen" reduziert**: die vormals drei getrennten Buttons „CSV-Import"/„Von Google synchronisieren" (plus das neue CSV-Export) sind jetzt hinter einem einzigen „Erweiterte Funktionen"-Button gebündelt, der `ImportModal` auf einem neuen ersten Schritt „choice" öffnet (drei Kacheln: CSV-Import/CSV-Export/Von Google synchronisieren) — der Toolbar-Button bündelt bewusst alles außer „Neu hinzufügen" (Anlegen eines neuen Eintrags bleibt als eigener, primärer Call-to-Action sichtbar). Das Suchfeld ist **nicht** Teil dieses Dialogs — s. nächster Punkt.
    - **Admin-Dashboard — Suche ins Filter-Panel verschoben**: das Namens-/Adress-/Fazit-Suchfeld stand vorher im Toolbar neben „Erweiterte Funktionen"/„Neu hinzufügen", ist inhaltlich aber ein Filter wie Status/Küche/Preis/Bewertung — steht jetzt als eigene erste Zeile im Filter-Panel. „Filter zurücksetzen" leert jetzt auch das Suchfeld mit.
    - **Cuisine-Filterlisten geprüft, kein Change nötig**: Admin-Küche-Filter (`allCuisines`) zieht bereits Werte aus **allen** geladenen Restaurants unabhängig vom Status (Entwürfe **und** veröffentlichte), öffentliche Küche-Filter (`getCuisines()`, an `FilterBar`/`SearchResultsView` durchgereicht) zieht bereits ausschließlich aus veröffentlichten Restaurants und ist dank serverseitigem Re-Fetch bei jedem Seitenaufruf + `revalidatePath("/", "layout")` nach jeder Mutation automatisch aktuell, sobald ein Restaurant mit neuer Küche angelegt/veröffentlicht wird — beides entsprach bereits der gewünschten Trennung, keine Code-Änderung nötig.
    - **Nebenbei-Fix**: `components/CuisineFilterDropdown.tsx` rücksetzte das Such-Feld beim Schließen bisher über einen separaten `useEffect`, der synchron `setState` in einer Effekt-Funktion aufrief (ESLint `react-hooks/set-state-in-effect`) — jetzt direkt in den beiden Stellen zurückgesetzt, die das Schließen auslösen (Toggle-Button-Klick, Klick außerhalb).
22. **Content-Security-Policy + Service Worker** (2026-07-18) — ✅ umgesetzt, per Playwright verifiziert (Login, Karten-/Ortssuche inkl. Kachel-Rendering, Restaurant-Detailseite inkl. Google-Fotos, Admin-Dashboard inkl. Places-Autocomplete — keine CSP-Verstöße in der Browser-Konsole; Service-Worker-Registrierung/Aktivierung/Precaching sowie Offline-Fallback per simuliertem Offline-Modus verifiziert). Keine Schema-Änderung, keine neue Migration.
    - **CSP**: s. „HTTP-Security-Header" oben für die volle Directive-Liste und Begründung. Kurzfassung: `proxy.ts` generiert pro Request einen Nonce, `script-src` nutzt ihn zusammen mit `'strict-dynamic'` (lässt Next' Framework-Scripts und die von `@vis.gl/react-google-maps` dynamisch injizierte Maps-JS-SDK-Script-Tag zu, ohne `maps.googleapis.com` namentlich allowlisten zu müssen), `style-src` bleibt bewusst **ohne** Nonce bei `'unsafe-inline'` (die App nutzt durchgängig `style={{...}}`-Inline-Attribute, auf die CSP-Nonces grundsätzlich nicht anwendbar sind — ein Nonce in `style-src` hätte `unsafe-inline` app-weit für alle Inline-Styles deaktiviert). `app/layout.tsx` liest den Nonce über `headers().get("x-nonce")` und setzt ihn explizit auf das einzige handgeschriebene Inline-`<script>` (Theme-Flash-Vermeidung).
    - **Korrektheits-Detail beim Session-Cookie**: `utils/supabase/proxy.ts`s `updateSession()` musste umgebaut werden, damit der CSP-Nonce nicht auf Kosten der von Supabase im selben Request nachgezogenen Session-Cookie-Aktualisierung geht — jedes `NextResponse.next({ request: { headers } })` baut die `headers`-Option jetzt frisch aus dem zum jeweiligen Zeitpunkt aktuellen `request.headers` auf (Helper-Funktion `nextWithNonce()`), statt einen einmal zu Beginn geclonten `Headers`-Snapshot wiederzuverwenden, der die später von Supabase gesetzte Cookie-Änderung nicht mehr enthalten hätte.
    - **Service Worker** (`public/sw.js`): registriert von `components/ServiceWorkerRegister.tsx` (neu, in `app/layout.tsx` gemountet). Bewusst kein Offline-Cache für Restaurant-/Kommentar-Daten (auth-/session-getrieben) — nur ein Offline-Fallback für fehlgeschlagene Navigationen (`app/offline/`, neue Route) und Cache-first für die eigenen, nicht-personalisierten PWA-Icons/das Manifest. `/sw.js` und `/offline` mussten in `PUBLIC_PATHS` (`utils/supabase/proxy.ts`) aufgenommen werden, s. „Auth & Berechtigungen".
23. **Admin-Dashboard: Restaurant-Tabelle paginiert** (2026-07-18) — ✅ umgesetzt, per Playwright (Desktop + Mobile) verifiziert. Keine Schema-Änderung, keine neue Migration.
    - Bei mittlerweile 245 Einträgen rendert die Tabelle/Kartenliste nicht mehr alle gefilterten Treffer auf einmal. Neuer State `page`/`pageSize` (Default `pageSize = 20`, wählbar 10/20/50/100 über ein `<select>`), abgeleitete `pageCount`/`currentPage`/`pageItems` (s. „Admin-Dashboard" oben, Abschnitt „Paginierung", für Details zur Clamping-Logik statt `useEffect`-Reset). Eine gemeinsame Pagination-Leiste unter der Tabelle bedient sowohl die Mobile-Kartenliste als auch die Desktop-Tabelle.
    - Nebeneffekt: die „Alle auswählen"-Checkbox (Mobile-Label bzw. Tabellen-Header) wählt jetzt nur noch die aktuell sichtbare Seite statt aller gefilterten Treffer über alle Seiten hinweg — Auswahlen aus verschiedenen Seiten addieren sich weiterhin (bleiben beim Blättern erhalten), s. „Mehrfachauswahl + Bulk-Löschen" oben.
24. **Security-Audit: zwei Server-Action-Autorisierungslücken gefunden und gefixt** (2026-07-18) — ✅ umgesetzt, `tsc --noEmit` + `npm run lint` sauber, noch nicht per Browser-Test verifiziert. Keine Schema-Änderung, keine neue Migration. Auslöser: gezielte Prüfung „jede exportierte Funktion in einer `\"use server\"`-Datei ist ein öffentlicher HTTP-Endpunkt, unabhängig davon, ob sie je von Client-Code referenziert wird" (Next.js' eigene Sicherheitsempfehlung für Server Actions) — bislang hatten die Security-Audits aus Roadmap-Schritt 12/22 nur fehlende `requireAuth`/`requireApproved`/`requireAdmin`-Aufrufe bei tatsächlich UI-verdrahteten Actions geprüft, nicht aber Actions, die nur **intern** von einer anderen (bereits gegateten) Action aufgerufen werden.
    - **Kritisch — `refreshGooglePlaceDataIfStale`** (`app/actions/restaurants.ts`, Roadmap-Schritt 19) nahm bisher fertige `restaurant`/`details`-Objekte vom Aufrufer entgegen und schrieb daraus über `createAdminClient()` (Service-Role, RLS-Bypass) direkt in die `restaurants`-Tabelle. Da die Funktion exportiert und ohne eigenen Auth-Check war, hätte jeder eingeloggte + freigeschaltete (aber nicht-Admin-)Nutzer den zugrunde liegenden Server-Action-Endpunkt direkt mit frei erfundenen `restaurant`/`details`-Werten aufrufen können — inklusive einer beliebigen `restaurant.id` — und so `address`/`phone`/`website`/`google_opening_hours` **jedes** Restaurants mit beliebigem Text überschreiben, ganz ohne Admin-Rechte oder RLS-Prüfung. Fix: die Funktion nimmt jetzt nur noch eine `restaurantId` entgegen und lädt Restaurant (RLS-gebundener Client) **und** Google-Daten (eigener `getPlaceDetails()`-Aufruf) selbst — der einzige noch vom Aufrufer kontrollierte Input ist die ID, womit sich höchstens ein vorgezogener, aber inhaltlich echter Sync für ein einzelnes (laut RLS sichtbares) Restaurant auslösen lässt. Kostet im (seltenen) Stale-Fall einen zusätzlichen Places-Request statt des bisher wiederverwendeten; im Normalfall (nicht abgelaufen) weiterhin ein reiner No-op ohne jeden Request. `app/restaurant/[id]/page.tsx` entsprechend angepasst.
    - **Mittel — `resolvePlaceForImport`** (`app/actions/places.ts`) war nur als interner Helper von `confirmCsvImport()` (`csvImport.ts`, `requireAdmin()`-gated) gedacht, hatte aber selbst keinen Auth-Check — jeder freigeschaltete Nicht-Admin hätte den Endpunkt direkt aufrufen und damit das Google-Places-API-Budget der App belasten können (zwei Requests pro Aufruf: Detail-Lookup + ggf. Textsuche). Fix: `await requireAdmin()` direkt in der Funktion ergänzt (zusätzlich zum bereits vorhandenen Check in `confirmCsvImport`, harmlos redundant).
    - Restliche Server Actions (`comments.ts`, `csvImport.ts`, `profiles.ts`, `restaurants.ts`, `reviews.ts`) sowie alle RLS-Policies erneut durchgesehen — kein weiterer Fund dieser Art. `getPlaceDetails()` (`places.ts`) bleibt bewusst ohne eigenen Auth-Check (läuft für jeden Besucher der Restaurant-Detailseite, liefert nur öffentliche Google-Daten, kein DB-Write) — deckungsgleich mit der bereits dokumentierten Begründung bei `refreshGooglePlaceDataIfStale`.
25. **Admin-Opt-in: Entwürfe auch in der öffentlichen Suche zeigen** (2026-07-18) — ✅ umgesetzt, per Playwright verifiziert (Toggle ein-/ausschalten, Draft erscheint mit „Entwurf"-Badge in Namenssuche/Startseite bei aktiviertem Toggle, verschwindet nach dem Ausschalten wieder; Cookie-Wert per `page.context().cookies()` geprüft). `tsc --noEmit` + `npm run lint` sauber. Keine Schema-Änderung, keine neue Migration.
    - **Hintergrund**: Roadmap-Schritt 12 hatte `getRestaurants()`/`getRecentRestaurants()`/`getFeaturedRestaurants()`/`getCuisines()` bewusst auf ein hartes `.eq("status", "published")` festgelegt, weil ein Admin sonst versehentlich Entwürfe zwischen echten Einträgen auf der normalen Website sah. Neuer Wunsch: Admins sollen sich Entwürfe bei Bedarf trotzdem gezielt in der echten Suche/Startseite/Karte ansehen können (z. B. um zu prüfen, wie ein frisch importierter CSV-Entwurf später aussehen würde) — aber nur, wenn sie das aktiv anfordern, nicht als neuer Standardzustand.
    - **Cookie-Toggle statt Server Action**: `AdminDashboard.tsx` bekam eine neue Box direkt unter dem `<BackButton>` mit einer Checkbox „Entwürfe auch in der öffentlichen Suche zeigen" (State `showDraftsInSearch`, Default aus). Setzt/löscht beim Klick direkt `document.cookie` (`gp-show-drafts=1`, 1 Jahr, `SameSite=Lax`) — exakt das gleiche clientseitige Cookie-Muster wie `ThemeToggle.tsx`/`gp-theme`, kein Server-Roundtrip nötig, da der Cookie selbst keine sicherheitsrelevante Information ist (s. nächster Punkt).
    - **Serverseitige Absicherung**: neue, nicht-exportierte Helper-Funktion `shouldIncludeDrafts()` in `app/actions/restaurants.ts` liest den Cookie und lässt den `.eq("status", "published")`-Filter in allen vier oben genannten Funktionen nur weg, wenn **zusätzlich** `isCurrentUserAdmin()` (neu, `utils/supabase/auth-helpers.ts`) — ein non-throwender Rollen-Check gegen `profiles.role`, im Gegensatz zu `requireAdmin()` — den eingeloggten Nutzer tatsächlich als Admin bestätigt. Ein Nicht-Admin, der sich den Cookie z. B. per DevTools selbst setzt, bekommt dadurch weiterhin nur veröffentlichte Restaurants. Der Cookie-Name selbst konnte nicht als `export const` aus `restaurants.ts` exportiert werden — eine `"use server"`-Datei darf laut Next.js ausschließlich async Functions exportieren (harter Build-Fehler beim ersten Versuch), deshalb ist die Konstante privat und `AdminDashboard.tsx` setzt den Cookie-Namen als eigenes String-Literal.
    - **„Entwurf"-Badge überall, wo ein Entwurf jetzt auftauchen kann**, damit er nie mit einem echten veröffentlichten Eintrag verwechselt wird: `RestaurantCard.tsx` (Landing-Page-Reihen + Grid), `SearchResultsView.tsx`s `ResultCard` (neben dem Namen), `MapView.tsx`s `InfoWindow` (dafür bekam der bisher stark reduzierte `MapRestaurant`-Typ ein neues optionales `status`-Feld, durchgereicht von `SearchResultsView.tsx`s `mapRestaurants`-Mapping), und die Restaurant-Detailseite (Badge über dem `<h1>`-Namen).
    - `getRestaurantById()` war schon vorher ungefiltert (s. Roadmap-Schritt 12) und musste nicht angepasst werden.
26. **Google-API-Kostenoptimierung + Landing-Page-Scroll-Bugfix** (2026-07-19) — ✅ umgesetzt, `tsc --noEmit` + `npm run lint` sauber, per Playwright verifiziert (Details unten). Keine Schema-Änderung, keine neue Migration. Auslöser: Nutzer-Frage nach Google-Maps-Preismodell-Abgleich (erste reale Places-API-(New)-Kosten während des Testbetriebs) + gemeldeter Scroll-Bug auf der Landing-Page.
    - **`getPlaceDetails()` Field-Mask für die Restaurant-Detailseite reduziert**: s. Eintrag bei `app/actions/places.ts` oben — neuer optionaler `{ photosOnly }`-Parameter, Detailseite fragt jetzt nur noch `photos` (Basic-Data-SKU) statt zusätzlich Contact-Data-Felder (Adresse/Öffnungszeiten/Telefon/Website) an, die sie ohnehin nicht mehr aus der Live-Antwort liest (kommen aus `restaurant.*`, s. Roadmap-Schritt 18). Admin-Edit-Panel/`syncGooglePlaceData()`/`refreshGooglePlaceDataIfStale()` unverändert (brauchen weiterhin das volle Feldset). Per Playwright gegen die echte Google-API verifiziert: Detailseite lädt weiterhin normal, Hero-Foto erscheint.
    - **Bestandsaufnahme beim Debuggen**: aktuell sind alle 493 Restaurants in der DB `status: 'draft'` (0 veröffentlicht) — vermutlich Ergebnis eines großen CSV-Testimports. Das erklärt einen erheblichen Teil der vom Nutzer beobachteten Places-API-Kosten (jede CSV-Import-Zeile löst über `resolvePlaceForImport()` bis zu zwei Places-Requests aus, s. `csvImport.ts`) — kein Bug, aber als Kontext hier festgehalten, da es auch erklärt, warum „Auswahl"/„Neu hinzugefügt" auf der echten Seite aktuell leer sind (0 veröffentlichte/featured Einträge), solange der „Entwürfe zeigen"-Toggle (Roadmap-Schritt 25) aus ist.
    - **Bugfix — horizontale Landing-Page-Reihen nur teilweise erreichbar**: `Auswahl`/`Neu hinzugefügt` (`app/page.tsx`) waren reine `overflowX: auto`-Flex-Zeilen ohne jede Scroll-Interaktions-Logik — ein normales vertikales Mausrad bewegt `scrollLeft` in keinem Browser automatisch (nur eine echte Trackpad-Horizontal-Geste oder ein gezogener Scrollbar-Balken taten das), wodurch Karten jenseits der sichtbaren Breite für reine Maus-Nutzer schlicht nicht erreichbar waren — genau das vom Nutzer gemeldete Symptom. Per Playwright reproduziert (`mouse.wheel()` ließ `scrollLeft` bei 0, obwohl `scrollWidth > clientWidth`) und nach dem Fix verifiziert. Neue Client Component `components/HorizontalScrollRow.tsx` (s. Eintrag oben) kapselt beide Reihen: nicht-passiver `wheel`-Listener wandelt vertikales Wheel-Delta in `scrollLeft` um (ein normaler React-`onWheel`-Handler ist für Wheel-Events immer passiv, `preventDefault()` darin wirkungslos — deshalb `addEventListener(..., { passive: false })` statt JSX-`onWheel`), plus zwei Pfeil-Buttons, die nur erscheinen, wenn in die jeweilige Richtung noch etwas zu scrollen ist.
    - **Autocomplete-Session-Token für die Orts-Suche ergänzt**: s. Eintrag bei `LocationSearch.tsx` oben — bislang wurde jeder debouncte Tastenanschlag der Orts-Autocomplete einzeln abgerechnet, zusätzlich zum separaten Place-Details-Call beim Auflösen einer Auswahl. Mit `AutocompleteSessionToken` (neuer `sessionTokenRef`, über `PlacePrediction.toPlace()` automatisch in den abschließenden `fetchFields()`-Call übernommen) bündelt Google Autocomplete + Details einer Suche zu einer abgerechneten Sitzung. Nach der Recherche vermutlich die größere der beiden Kostenquellen, da sie bei **jeder** Ortssuche jedes Nutzers greift (nicht nur bei Detailseiten-Aufrufen). Per Playwright gegen die echte API verifiziert (abgefangene RPC-Requests trugen identisches Token für Autocomplete+Details, danach ein neues für die nächste Suche).
27. **Filter-Zeile in der Suchergebnisliste einklappbar** (2026-07-19) — ✅ umgesetzt, `tsc --noEmit` + `npm run lint` sauber, per Playwright (Desktop + Mobile-Viewport) verifiziert. Keine Schema-Änderung, keine neue Migration. Auslöser: die Küche-/Preis-/Bewertungs-Filterzeilen in `SearchResultsView.tsx` nahmen dauerhaft vertikalen Platz ein, der auf schmalen/kurzen Viewports der eigentlichen Trefferliste fehlte.
    - Neuer State `filtersOpen` (`SearchResultsView.tsx`) — die Filter-Sektion (Küche-Dropdown, Preis-/Bewertungs-Chips, „Übernehmen"/„Filter zurücksetzen") ist jetzt hinter einem klickbaren „Filter"-Header versteckt, der einen rotierenden `IconChevronDown` (gleiches Muster wie die `<details>`-Öffnungszeiten auf der Restaurant-Detailseite) sowie — sobald mind. ein Filter tatsächlich aktiv ist (`activeFilters`, nicht der noch ungespeicherte `pending`-State) — einen kleinen Zähler-Badge zeigt.
    - Default: eingeklappt, außer beim Laden ist bereits ein Filter aktiv (dann offen, damit die aktuelle Auswahl nicht unsichtbar hinter dem Header verschwindet). Die „Sortieren"-Zeile bleibt unverändert immer sichtbar — sie ist nur eine einzelne Chip-Reihe und ändert sich unabhängig vom Filter-Zustand.
    - **Bugfix — Abstand + Küche-Zeile wirkte mit dem Header verschnitten** (unmittelbar nach der obigen Einführung, gleicher Tag): Header-Button und Filter-Inhalt hatten ursprünglich je einen eigenen `borderBottom` (zwei Trennlinien statt einer) und der Inhalt hatte kein Top-Padding — dadurch saß die erste Zeile (Küche) direkt an/unter der Trennlinie des Headers, ohne Luft. Fix: Header-Button und Inhalt teilen sich jetzt einen gemeinsamen äußeren `borderBottom` (nur noch eine Trennlinie, am Ende der ganzen Filter-Box statt zusätzlich mittig zwischen Header und erster Zeile), der Inhalt hat jetzt eigenes Top-Padding (`"6px 24px 16px"`) für sauberen Abstand zum Header. Per Playwright (Desktop + Mobile, inkl. geöffnetem Küche-Dropdown) verifiziert.
    - **Kollabiert automatisch nach „Übernehmen"** (unmittelbar danach, gleicher Tag): `filtersOpen` startet jetzt immer `false`, auch wenn beim Laden bereits ein Filter aktiv ist (das vorherige „öffnet automatisch, wenn ein Filter aktiv ist" wieder entfernt). Klick auf „Übernehmen" navigiert zu einer URL mit neuen `activeFilters`, was den `filterKey` (s. `SearchResultsView`-Wrapper oben) ändert und einen kompletten Remount von `SearchResultsViewInner` erzwingt — der neue Mount initialisiert `filtersOpen` wieder mit `false`, wodurch die Filter-Sektion nach jedem „Übernehmen" (und nach „Filter zurücksetzen", da beide denselben Navigations-/Remount-Pfad nutzen) automatisch eingeklappt ist. Der Zähler-Badge neben „Filter" bleibt davon unberührt und zeigt weiterhin die Anzahl aktiver Filter. Per Playwright verifiziert (Preis-Chip auswählen → „Übernehmen" → Filter-Zeilen nicht mehr sichtbar, Badge zeigt „1").
28. **Bugfix — Öffnungszeiten-Status ignorierte die Zeitzone des Restaurants** (2026-07-20) — ✅ umgesetzt, `tsc --noEmit` + `npm run lint` sauber, noch nicht per Browser-Test verifiziert. Keine Schema-Änderung, keine neue Migration. Auslöser: Nutzer-Meldung, dass „Jetzt geöffnet"/„Geschlossen" auf der Restaurant-Detailseite manchmal falsch war.
    - **Ursache**: `getOpeningStatus()` (`lib/openingHours.ts`) las die aktuelle Uhrzeit bisher über `now.getHours()`/`now.getDay()` — das liefert die Zeitzone des **Servers** (auf Vercel z.B. UTC), nicht die des Restaurants. Googles gespeicherte „Tag: Zeiten"-Strings sind aber immer in der lokalen Zeit des jeweiligen Standorts. Nahe Öffnungs-/Schließgrenzen (z.B. kurz vor/nach 22 Uhr) kippte der Status dadurch je nach Tages-/Jahreszeit (CET vs. CEST) fälschlich.
    - **Erster Fix-Versuch (hartkodiert `Europe/Berlin`) verworfen**: löst das Server-Zeitzonen-Problem für deutsche Restaurants, ignoriert aber Restaurants in anderen Zeitzonen (falls der Bestand sich je über Deutschland hinaus erweitert) — auf Nutzerwunsch stattdessen ortsbasiert gelöst.
    - **Finaler Fix**: neue Dependency `tz-lookup` (+ `@types/tz-lookup`) — rein lokale/offline Geo-Koordinaten→IANA-Timezone-Auflösung (kein Netzwerk-/API-Call, passt zur „Google-Requests sparen wo möglich"-Linie aus Roadmap-Schritt 26). `getOpeningStatus()` hat eine neue optionale `coords`-Parameter-Position (`{ lat, lng } | null`, vor `now`); `resolveTimeZone()` löst daraus per `tzlookup()` die Zeitzone des Restaurant-**Standorts** auf (Fallback `Europe/Berlin` ohne Koordinaten, z.B. bei einem manuell erfassten Restaurant ohne Kartenpin), `getLocalDateParts()` (vormals `getBerlinDateParts()`) berechnet Wochentag+Uhrzeit „jetzt" per `Intl.DateTimeFormat` in genau dieser Zeitzone. Beide Call-Sites (`app/restaurant/[id]/page.tsx`, `components/SearchResultsView.tsx`s `ResultCard`) reichen jetzt `{ lat: restaurant.lat, lng: restaurant.lng }` durch — beide hatten die Koordinaten ohnehin schon im Scope (Kartendarstellung bzw. Distanzberechnung).
29. **Bugfix — Signup-/Passwort-Bestätigungslink meldete „abgelaufen" schon beim allerersten Klick** (2026-07-20) — ✅ umgesetzt, `tsc --noEmit` + `npm run lint` sauber, noch nicht per Browser-Test verifiziert (Fix lässt sich nur mit einem echten E-Mail-Client/Sicherheits-Scanner zuverlässig end-to-end nachstellen). Keine Schema-Änderung, keine neue Migration.
    - **Ursache**: GoTrues gehosteter `/auth/v1/verify`-Endpunkt verifiziert (und verbraucht damit) den Einmal-Token bei einem simplen GET-Request — der Bestätigungslink zeigte bisher direkt dorthin (`${SUPABASE_URL}/auth/v1/verify?token=...&type=...&redirect_to=...`). E-Mail-Sicherheits-Scanner (u.a. Microsoft Outlook „Safe Links", andere Firmen-Mail-Gateways) rufen automatisiert jeden Link in einer eingehenden Mail per GET auf, um ihn zu prüfen — das verbrauchte den Token, bevor der eigentliche Nutzer je geklickt hatte, wodurch dessen echter (erster) Klick sofort „Link ungültig/abgelaufen" sah. Bekanntes, von Supabase selbst dokumentiertes Problem bei GET-getriggerter Auto-Verifizierung.
    - **Fix**: s. „Klick-gated Verifizierung" unter „E-Mail-Versand (Resend statt Supabase-SMTP)" oben für die volle Beschreibung. Kurzfassung: `app/api/auth/email/route.ts` verlinkt jetzt die eigene `/auth/confirm`- bzw. `/auth/reset-password`-Seite mit dem rohen `token_hash` (+ `type`) statt GoTrues Auto-Verify-Endpunkt. Die Verifizierung (`supabase.auth.verifyOtp({ token_hash, type })`) passiert dort nicht mehr beim Laden der Seite (GET, das Scanner auslösen), sondern erst bei einer expliziten Nutzer-Aktion: ein „Konto bestätigen"-Button (`app/auth/confirm/ConfirmButton.tsx`, neu) bzw. das Abschicken des „neues Passwort"-Formulars (`ResetPasswordForm.tsx`) — beides eine Server Action (POST). Neue Server Action `confirmEmailToken` (`app/actions/auth.ts`); `updatePassword` hat einen neuen optionalen `tokenHash`-Parameter und verifiziert den Recovery-Token vor dem `updateUser()`-Aufruf.
    - **Nebeneffekt**: `exchangeCodeForSession`/PKCE-`code`-Flow komplett entfernt aus beiden Confirm-Seiten — vereinfacht den Ablauf (ein Verify-Aufruf statt Verify-durch-GoTrue-dann-Code-Exchange) und macht den Bestätigungs-Screen bei Signup jetzt zweistufig (Link öffnen → Button klicken), während der Passwort-Reset weiterhin einstufig bleibt (Formular abschicken verifiziert und setzt das Passwort in einem Aufruf).
30. **Signup-Flow: kaputter „Zurück zur Startseite"-Link, doppelte E-Mail-Eingabe, Bestätigungslink erneut anfordern** (2026-07-20) — ✅ umgesetzt, `tsc --noEmit` + `npm run lint` sauber, per Playwright verifiziert (E-Mail-Mismatch-Fehler, Resend-Tab über Footer-Link und über `/auth/confirm`-Fehlerscreen erreichbar, Resend-Formular zeigt Erfolgsscreen, „Zurück zum Login" landet wieder auf dem Login-Tab). Keine Schema-Änderung, keine neue Migration.
    - **Bugfix — „Zurück zur Startseite" auf dem „Bitte bestätige deine E-Mail"-Screen (`app/login/LoginForm.tsx`) führte ins Leere**: der Screen erscheint direkt nach `signUp()`, bevor die E-Mail bestätigt (und damit `auth.users.email_confirmed_at` gesetzt) ist — es existiert also noch keine Session. Ein `<Link href="/">` traf dadurch `proxy.ts`s Auth-Gate (s. „Auth & Berechtigungen") und landete sofort wieder auf `/login` mit leerem Formular, was wie ein toter Link wirkte. Ersetzt durch einen Button, der clientseitig `setTab("login")` aufruft (gleiches Muster wie der bestehende „Zurück zum Login"-Button auf dem „Passwort vergessen"-Erfolgsscreen) — kein Routen-Wechsel nötig, kein Gate-Bounce.
    - **E-Mail-Adresse muss beim Registrieren zweimal eingegeben werden**: neues Feld „E-Mail bestätigen" (`name="emailConfirm"`, `autoComplete="off"`) im Signup-Formular, direkt unter „E-Mail". `signUp()` (`app/actions/auth.ts`) vergleicht beide (getrimmt) serverseitig und bricht mit „Die E-Mail-Adressen stimmen nicht überein." ab, bevor überhaupt ein Supabase-Call ausgelöst wird — gleiches Muster wie der bestehende Passwort/Passwort-bestätigen-Vergleich in `updatePassword`. Kein Client-seitiges Pre-Check-JavaScript (konsistent mit dem bestehenden Passwort-Bestätigen-Feld, das ebenfalls nur serverseitig validiert).
    - **Bestätigungslink erneut anfordern**: neue Server Action `resendConfirmationEmail(email)` (`app/actions/auth.ts`) ruft `supabase.auth.resend({ type: "signup", email, options: { emailRedirectTo } })` auf und meldet — wie `requestPasswordReset` — immer Erfolg, unabhängig davon, ob die Adresse existiert/bereits bestätigt ist (Anti-Enumeration). Neuer vierter Tab-Zustand `"resend"` in `LoginForm.tsx` (`Tab = "login" | "signup" | "forgot" | "resend"`, kein eigener Route-Wechsel, gleiches clientseitiges Tab-Pattern wie `"forgot"`), erreichbar über: (1) einen dezenten Footer-Link „Bestätigungslink nicht erhalten oder abgelaufen?" unter dem Login-/Signup-Formular, (2) einen Link „Kein Link erhalten? Erneut anfordern" auf dem „Bitte bestätige deine E-Mail"-Erfolgsscreen (vorbefüllt mit der gerade eingegebenen Adresse, s.u.), (3) einen neuen „Neuen Link anfordern"-Button auf `app/auth/confirm/page.tsx`s „Bestätigung fehlgeschlagen"-Screen (verlinkt `/login?tab=resend` — `app/login/page.tsx`s `defaultTab`-Auflösung unterstützt jetzt auch `?tab=resend`).
    - **E-Mail-Vorausfüllung fürs Resend-Formular**: `Field` (`LoginForm.tsx`) hat jetzt optionale `onChange`-/`defaultValue`-Props (vorher rein unkontrolliert); das Signup-E-Mail-Feld spiegelt seine Eingabe in einen `signupEmail`-State, mit dem das Resend-Formular vorbefüllt wird, wenn der Nutzer direkt vom Erfolgsscreen aus „Kein Link erhalten?" klickt — rein clientseitiger Komfort, keine Server-Kommunikation.
31. **Admin-Dashboard: Autosave-Entwurf im Restaurant-Edit-Panel** (2026-07-20) — ✅ umgesetzt, `tsc --noEmit` + `npm run lint` sauber, per Playwright verifiziert (Autosave nach Debounce in localStorage geschrieben, Entwurf übersteht einen Reload, Wiederherstellen-Banner erscheint und befüllt sowohl ein kurzes Feld (Name) als auch ein langes Textfeld (Fazit) korrekt zurück, Entwurf wird beim Schließen/Abbrechen des Panels gelöscht). Keine Schema-Änderung, keine neue Migration. Auslöser: Sorge, dass ein langer Fazit-/Kategorie-Text beim versehentlichen Neuladen der Seite verloren geht.
    - **Rein clientseitig (kein neuer DB-Zugriff)**: das Admin-Dashboard wird von jeweils einem Admin gleichzeitig benutzt, ein Server-seitiger Draft-Sync über Geräte hinweg ist nicht nötig — `localStorage` reicht. Neue Helfer in `app/admin/dashboard/AdminDashboard.tsx`: `DraftPayload`-Typ (`{ form, manualEntry, visibleContactFields, savedAt }`), `draftStorageKey(editingId)` (liefert `"gp-admin-draft-new"` fürs Anlegen bzw. `"gp-admin-draft-edit-{id}"` fürs Bearbeiten — verhindert, dass sich ein neuer Entwurf und ein Bearbeiten-Entwurf gegenseitig überschreiben), `loadDraft()`, `formatDraftTimestamp()`.
    - **Debounced Schreiben**: neuer `dirty`-State wird von `patchForm`/`patchReview`/`patchCategory` (den einzigen Stellen, die tatsächliche Nutzereingaben ins Formular schreiben) auf `true` gesetzt — grenzt echte Änderungen vom bloßen Befüllen des Formulars beim Öffnen (`openNew`/`openEdit`) ab, damit nicht sofort ein "Entwurf" aus unveränderten Daten entsteht. Ein `useEffect` (abhängig von `form`/`manualEntry`/`visibleContactFields`/`panelOpen`/`dirty`/`editingId`) schreibt 1.5s nach der letzten Änderung (klassisches Debounce-Pattern über `setTimeout` + Cleanup-`clearTimeout`) den aktuellen Formular-Zustand nach `localStorage` — kein Schreiben pro Tastenanschlag.
    - **Wiederherstellen-Banner**: `openNew()`/`openEdit()` prüfen nach dem Befüllen des Formulars per `loadDraft(draftStorageKey(...))`, ob bereits ein Entwurf für genau diesen Fall (neues Restaurant bzw. dieses bestehende) in `localStorage` liegt, und zeigen ihn — falls vorhanden — als `draftBanner`-State. `EditPanel` rendert dafür oben im Formular eine Gold-Box („Nicht gespeicherter Entwurf vom {Datum} gefunden.") mit „Wiederherstellen"/„Verwerfen". Kein Auto-Restore ohne Nutzer-Bestätigung — bei „Bearbeiten" hat `openEdit` bereits die aktuellen DB-Werte geladen, ein stiller Overwrite damit wäre überraschend.
    - **Aufräumen**: `closePanel()` (der einzige Sink für alle Schließen-Wege — Speichern-Erfolg, „Abbrechen", Backdrop-Klick, X-Button, s. `onClose`-Prop von `EditPanel`) löscht den Entwurf aus `localStorage` und setzt `dirty`/`draftBanner` zurück — jedes bewusste Verlassen des Formulars gilt als „Entwurf nicht mehr gebraucht". Nur ein Reload/Tab-Schließen ohne explizite Aktion lässt ihn (per Design) stehen. `discardDraft()` (Klick auf „Verwerfen") räumt genauso auf, ohne das Panel zu schließen.
